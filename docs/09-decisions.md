# 9. Decision Register

Every binding decision as a statement. A decision is changed by a new entry
that supersedes the old one; entries are never edited after acceptance.

Status values: `decided` (stated by the project owner), `proposed`
(awaiting confirmation), `superseded by D-nn`.

| ID | Decision | Status | Date |
|----|----------|--------|------|
| D-01 | AuDHSOS is a microkernel. The kernel provides address spaces, threads with scheduling, IPC, capabilities, and interrupt forwarding, and nothing else. | decided | 2026-09-04 |
| D-02 | Everything else runs in userland: drivers including the console driver, program loading, naming, memory policy, fault handling, file systems, networking. | decided | 2026-09-04 |
| D-03 | The system is written in Rust. No assembly files, no `global_asm!`. Inline assembly only in adapter crates, inventoried and budgeted. Memory management contains no assembly. | decided | 2026-09-04 |
| D-04 | No external code. `Cargo.lock` lists only workspace members. No crates.io, git, or vendored third-party code in any dependency section, including build and dev dependencies. No external Cargo subcommands. The only external components are the Rust toolchain and QEMU with its bundled UEFI firmware. | decided | 2026-09-04 |
| D-05 | `unsafe` is confined to the allowlisted adapter crates `kernel-hal-x86_64`, `boot-uefi-x86_64`, `audhsos-sync`, `user-sys-x86_64`, and `fuzz-support`. Every other crate has `#![forbid(unsafe_code)]`. Every `unsafe` block has a `SAFETY:` comment and counts against a budget checked in CI. | decided | 2026-09-04 |
| D-06 | Every algorithm is written against a trait and tested on the host with doubles. Adapters implement the traits for the loader and the kernel. One implementation per algorithm. | decided | 2026-09-04 |
| D-07 | First target: QEMU `q35` on `x86_64`, booted through UEFI. Legacy BIOS boot is not supported. Second target: `aarch64` on QEMU `virt` under HVF. Third target: `riscv64` on QEMU `virt`. | decided | 2026-09-04 |
| D-08 | The loader is the project's own UEFI application `boot-uefi-x86_64`. UEFI structure layouts live in the logic crate `audhsos-uefi`. The loader reads `AUDHSOS/KERNEL.ELF` and `AUDHSOS/BOOT.IMG` from the boot volume, builds page tables with the kernel's mapper, and enters the kernel with the boot information structure. | decided | 2026-09-04 |
| D-09 | The disk image is written by the xtask: GPT with a protective MBR and one EFI system partition, FAT32 only, 8.3 names, no long file name entries. | decided | 2026-09-04 |
| D-10 | The toolchain is one pinned nightly. Unstable features: `abi_x86_interrupt`, `custom_test_frameworks`, `-Zsanitizer=fuzzer` on the host. Adding a feature requires a register entry. | decided | 2026-09-04 |
| D-11 | The kernel has no heap and does not link `alloc`. Kernel objects live in typed pools sized at boot from the kernel reserve, addressed by generation-checked ids. Every allocation is fallible. Each process has a kernel-object quota. | decided | 2026-09-04 |
| D-12 | The kernel allocates user memory never after boot. All free memory becomes memory objects of the root task; memory objects are split, never merged, by the kernel. The memory server overwrites every memory object with zeros before handing it out and again immediately when it is returned. | decided | 2026-09-04 |
| D-13 | Capabilities: per-process handle tables with generation-checked handles, rights that only decrease, badges on endpoint capabilities, no recursive revocation in the first release. A handle is 64 bits: 32-bit table index, 32-bit generation. | decided | 2026-09-04 |
| D-14 | IPC: synchronous rendezvous on endpoints (`call`, `send`, `recv`, `reply`, `reply_recv`, `try_recv`) plus notifications with 64 signal bits. Messages carry up to 480 words and 4 handles through IPC buffers. Bulk data uses shared memory objects. | decided | 2026-09-04 |
| D-15 | System calls enter through interrupt vector `0x80` with the `x86-interrupt` ABI. Arguments and results live in the calling thread's IPC buffer. No user pointers in the interface. The system call table is one declarative macro in `audhsos-abi`. The `syscall` instruction path is decided in Phase 8 by measurement. | decided | 2026-09-04 |
| D-16 | Faults in user mode become IPC calls to the process's fault handler endpoint. | decided | 2026-09-04 |
| D-17 | The kernel is non-preemptible and runs on one CPU in the first release. System calls are bounded and return `Partial` for range operations. The SMP path is per-CPU run queues and a big kernel lock first. | decided | 2026-09-04 |
| D-18 | Scheduling: 32 fixed priorities, FIFO run queue per priority, round-robin time slices in timer ticks, idle thread on the boot stack. | decided | 2026-09-04 |
| D-19 | I/O port access from userland is a system call on an `IoPortRange` handle. Userland drivers contain no inline assembly. | decided | 2026-09-04 |
| D-20 | Allocators compute offsets, not pointers. The frame allocator and the userland heap allocator are project code in safe Rust; the only `unsafe` is the `GlobalAlloc` adapter in `user-sys-x86_64`. | decided | 2026-09-04 |
| D-21 | Global state uses `audhsos-sync::Global<T>` with a runtime borrow flag. No spinlock crate. | decided | 2026-09-04 |
| D-22 | The ELF parser `audhsos-elf` and the UART register logic `driver-uart16550` are single logic crates shared by loader, kernel, and userland. | decided | 2026-09-04 |
| D-23 | Testing: logic on the host with the project's property-test engine and model-test runner; adapters in QEMU with the custom test framework, the serial protocol, and `isa-debug-exit`; parsers fuzzed with the toolchain's sanitizer runtime; coverage with the toolchain's `llvm-profdata` and `llvm-cov`. The edge-case catalog is the definition of done. | decided | 2026-09-04 |
| D-24 | Policies (layering, unsafe budgets, SPDX header, fuzz targets) are Rust tables in `xtask/src/policy.rs`. No configuration file parsers. | decided | 2026-09-04 |
| D-25 | License `AGPL-3.0-only` without exceptions, SPDX header in every file, copyright line `Copyright (C) 2026 Manuel Baesler and contributors`. | decided | 2026-09-04 |
| D-26 | Everything is written in English: code, comments, documentation, commits, tests. | decided | 2026-09-04 |
| D-27 | The boot image is a fixed header, the root task as a flat binary at `ROOT_TASK_BASE`, and a ustar tar archive. The kernel validates the header and never reads the archive. | decided | 2026-09-04 |
| D-28 | The boot information structure is `#[repr(C)]` in `audhsos-abi`, versioned, with a bounded region array. | decided | 2026-09-04 |
| D-29 | Graphics output and input devices are in scope after release 0.1.0 as Phases 9 to 11 of the roadmap. Sound stays out of scope. | decided | 2026-09-05 |
| D-30 | Graphics output is the linear framebuffer that the UEFI Graphics Output Protocol reports. The loader reads the mode the firmware has set and does not change it. The boot information carries the framebuffer description; the root task creates a `Device` memory object for it and hands it to a userland display server. No virtio-gpu in the first graphics release. | decided | 2026-09-05 |
| D-31 | Input devices are the PS/2 keyboard and mouse behind the i8042 controller, driven by a userland input driver through `IoPortRange` and `Interrupt` capabilities. Events reach clients through a ring buffer in a shared memory object plus a notification, never through one IPC call per event. | decided | 2026-09-05 |
| D-32 | The boot information structure carries the framebuffer fields from Phase 2 on. Version 1 is extended, not bumped, because no image with the old layout has been released. An absent framebuffer is reported with every framebuffer field zero. | decided | 2026-09-05 |
| D-33 | Graphical tests drive QEMU through QMP from the xtask: input events are injected with `input-send-event`, the screen is read with `screendump`. The xtask contains its own JSON subset and PPM reader. CI runs without a display window. | decided | 2026-09-05 |
| D-34 | The project name is `AuDHSOS`; the crate prefix is `audhsos`. | decided | 2026-09-05 |
| D-35 | The build entry point on the development machine is rustup's Cargo proxy, `~/.cargo/bin/cargo xtask ...`. The xtask refuses to run under any other Cargo, including the shell alias. | decided | 2026-09-05 |
| D-36 | HTTPS support is prepared as a self-contained track of logic crates: `crypto-ct`, `crypto-hash`, `crypto-aead`, `crypto-ec`, `crypto-rng`, `audhsos-der`, `audhsos-x509`, `audhsos-tls`. They perform no I/O and no allocation, are host-tested, and are built independently of the kernel phases. The design is document 11. | decided | 2026-09-05 |
| D-37 | The first version is a TLS 1.3 client only (RFC 8446). No TLS 1.2 or earlier, no server role, no session resumption, no pre-shared keys, no 0-RTT, no client certificates, no renegotiation, no DTLS. | decided | 2026-09-05 |
| D-38 | Algorithms of the first version: cipher suites `TLS_AES_128_GCM_SHA256`, `TLS_AES_256_GCM_SHA384`, `TLS_CHACHA20_POLY1305_SHA256`; key exchange `x25519` and `secp256r1`; signature verification `ecdsa_secp256r1_sha256`, `ecdsa_secp256r1_sha384`, `ed25519`. RSA verification and the bignum crate it needs are later work. | superseded in part by D-56 | 2026-09-05 |
| D-39 | Symmetric primitives are constant-time by construction: bitsliced AES without lookup tables, GHASH without tables, no branch and no index on a secret value anywhere. The asymmetric product surface is verification only; signing exists solely behind test features. Each crate documents which functions see secrets and why their control flow is on public data. | decided | 2026-09-05 |
| D-40 | Test vectors transcribed from RFCs and NIST publications into Rust source files are specification data, not external code under R8; each table names its source. Certificates, keys, and chains used in tests are generated by project code behind test features, never vendored. | decided | 2026-09-05 |
| D-41 | The TLS library is sans-I/O. The caller supplies the buffers and the transport; the library exposes `read_tls`, `write_tls`, `poll`, `send`, `recv`, `close` and allocates nothing. | decided | 2026-09-05 |
| D-42 | The repository contains no certificate trust anchors. Anchors are an operator-supplied build input that the xtask converts into a DER table for the boot image; `audhsos-x509` takes them as a parameter. | decided | 2026-09-05 |
| D-43 | Randomness is the `Entropy` trait plus the `ChaChaRng` generator in `crypto-rng`. The platform source is `RDSEED` in `kernel-hal-x86_64` behind a `random_bytes` system call and is specified when the network stack is scheduled; until then no product code constructs a generator. | decided | 2026-09-05 |
| D-44 | Certificate validation covers the RFC 5280 subset stated in document 11 plus RFC 6125 name matching against `dNSName` entries. Revocation checking and name constraints are not implemented in the first version and are documented limits. | decided | 2026-09-05 |
| D-45 | Work parallel to the kernel phases is admitted only for crates that pass the admission test of document 12: pure logic without I/O, the lint profile of a logic crate, no dependency on kernel, loader, or userland crates, and a catalog entry that can be written and tested on the host today. At most one side track besides the cryptography track is active at a time, and a track is worked on between phases, never instead of one. | decided | 2026-09-05 |
| D-46 | Time is a crate: `audhsos-time` owns `UnixTime`, `CivilTime`, `Instant`, and `Duration`. `audhsos-der` imports `UnixTime` instead of defining it, which amends document 11 section 11.9. No crate of the project reads a clock; time enters every interface as a parameter. | decided | 2026-09-05 |
| D-47 | Base64, hex, and PEM live in `audhsos-encoding` at layer 0. The trust-anchor conversion of D-42 and the test certificate builder use it; the xtask contains no second decoder. | decided | 2026-09-05 |
| D-48 | `audhsos-collections` provides fixed-capacity, index-based containers without `unsafe` and without allocation: `ArrayVec`, `RingBuffer`, `BitSet`, `IndexList`, `IndexMap`. Kernel run queues, endpoint wait queues, and the network stack use them instead of an index scheme written per crate. | superseded in part by D-74 | 2026-09-05 |
| D-49 | The network stack is a track of sans-I/O logic crates: `net-wire`, `net-eth`, `net-ip`, `net-udp`, `net-tcp`, `net-dns`, `net-dhcp`, `net-http`, `net-stack`. They perform no I/O and no allocation, take time and randomness as parameters, and are host-tested. The design is document 12. The virtio-net driver, the network server, the socket protocol, and the entropy system call are integration and stay unscheduled. | superseded in part by D-119 | 2026-09-05 |
| D-50 | Scope of the first network version: no IPv6 — superseded by D-69, which puts it in; Ethernet II without VLAN tags; TCP options limited to the maximum segment size, so no window scaling, no selective acknowledgment, and no timestamps; Reno congestion control; no IP forwarding and no multicast beyond what ARP and DHCP need; HTTP/1.1 without content encodings and without following redirects. | decided | 2026-09-05 |
| D-51 | TCP draws its initial sequence numbers, UDP its ephemeral source ports, and DNS and DHCP their transaction ids, from the `Rng` trait of `crypto-rng`. This is the only dependency edge from the network track into the cryptography track; `audhsos-tls` and the network crates never reference each other. | decided | 2026-09-05 |
| D-52 | `virtio-queue` implements the split virtqueue of virtio 1.x and the device initialization state machine over a memory access trait, with no register access in the crate. No packed rings, no indirect descriptors, and no `EVENT_IDX` in the first version. | decided | 2026-09-05 |
| D-53 | The FAT32 structural logic moves from the xtask into `fs-fat` over a `BlockDevice` trait; the image writer becomes a user of that crate, as does a later file system server. FAT32 only with 8.3 names, unchanged from D-09; timestamps go through `audhsos-time`. | decided | 2026-09-05 |
| D-54 | `fuzz-support` provides the fuzzer entry glue, the `fuzz_target!` macro, and the regression corpus handling for every fuzz target in the catalog. `audhsos-symbols` resolves an address to function, file, and line from an ELF symbol table and a DWARF line program of version 4 or 5; the xtask uses it for panic addresses. No inline frames and no stack unwinding in the first version. | decided | 2026-09-05 |
| D-55 | The kernel binary `audhsos-kernel` is an allowlisted adapter crate. Its `unsafe` is the entry the loader jumps to, the panic handler, the reads of the window during the memory bring-up, and the same in the test images it carries. This amends D-05, which named the crate nowhere although the crate has held `unsafe` since Phase 2. | decided | 2026-09-05 |
| D-56 | The key exchange of the TLS 1.3 client is `x25519` alone; this supersedes the key exchange named in D-38. A key exchange multiplies a secret scalar, and the P-256 of `crypto-ec` is a verification implementation whose branches follow public values by design. Offering `secp256r1` would need a second scalar multiplication written to the constant-time discipline, in a module whose clarity comes from not needing one. X25519 is mandatory to implement in RFC 8446, so a client that offers it alone reaches every conforming server; the cost is servers that accept P-256 only. Signature verification over P-256 is unaffected and stays in scope. | decided | 2026-09-05 |
| D-57 | The object pools live in the `.bss` of the kernel image as `static` cells, not in the kernel reserve: a `Pool<T, N>` is a typed array, and putting one into raw frames would need `unsafe` in a logic crate. This amends 2.4.1, which said the reserve holds them. The reserve holds what the count of is not known before boot: page tables, kernel stacks, and IPC buffers. The numbers follow from that: `THREADS` and `KERNEL_STACKS` are 256, because every thread costs five frames of the reserve — nine after D-73 raised the kernel stack to eight pages — and 1024 of them do not fit the reserve of the reference machine; `PROCESSES` is 64 and `HANDLES_PER_PROCESS` is 1024, because the handle table lives in the `Process` object and 256 tables of `1 << 16` entries would be hundreds of mebibytes of `.bss`. The reference machine keeps its 256 MiB. | decided | 2026-09-05 |
| D-58 | The handle slots of the machine are one `static` arena of `HANDLE_ENTRIES` (16384, two per kernel object the machine can hold), and a process holds a `HandleList` of what it has and what its creator granted it. `HANDLES_PER_PROCESS` (4096) becomes the ceiling the quota enforces against that arena instead of memory set aside per process; this supersedes the handle part of D-57, which put a table of 1024 entries inside every `Process`. The demand that decides the number is `server-memory`, which holds one handle per memory object it hands out and can therefore reach `MEMORY_OBJECTS`; 1024 was too few for it and 4096 inline would have been 8 MiB of `.bss` for one process that needs it. A slot names its owner, and a lookup checks the owner as well as the generation. This is what 2.3.2 already required of a handle table: a capacity fixed at process creation within the creator's quota. | decided | 2026-09-05 |
| D-59 | The standards this system implements are kept verbatim under `docs/rfc/`, with their source, the date they were retrieved, and a checksum recorded beside them. They are documents: nothing there is compiled, linked, or read at run time, so rule R8 is untouched, and they are not covered by this repository's licence. A test vector is still transcribed into Rust source with its document and section named, as D-40 requires; the file is there so that the transcription can be checked without a network and so that the source cannot change under a test that cites it. | decided | 2026-09-05 |
| D-60 | The physical window covers memory, and the kernel maps the device apertures it needs into it itself. The loader sizes the window from the memory map, because a window over the whole address space of the reference machine would be a terabyte of page tables; a device the kernel drives, which in this release is the local APIC and the I/O APICs, is therefore mapped by the memory bring-up at `PHYS_WINDOW_BASE + address`, uncached, and registered as a region of kind `Device`. One rule reaches all of physical space, and the tables that reach the apertures are the kernel's own. This states what 4.6 left open when it said the adapter obtains the APIC register block through the window. | decided | 2026-09-05 |
| D-61 | Signature verification of the TLS client covers ECDSA over P-256 and over P-384 with SHA-256 or SHA-384, and `ed25519`; this supersedes the signature list of D-38, which named `ecdsa_secp256r1_sha384`, a code point RFC 8446 does not assign. In the `CertificateVerify` the scheme is checked against the curve of the certificate's key, because RFC 8446 section 4.2.3 has an ECDSA scheme name the curve as firmly as the hash: `ecdsa_secp384r1_sha384` over a P-256 certificate is refused with `illegal_parameter` even though the signature verifies. X.509 keeps the freedom the protocol gives up — `ecdsa-with-SHA384` binds no curve, and a P-256 key signing with SHA-384 is a certificate this system must read, `GTS Root R4` being above one. P-384 is what carries a chain to a root of that curve; it is verification only, so D-56 is untouched and no group is added to the key exchange. | decided | 2026-09-05 |
| D-62 | A trust anchor is a subject and a key, and `TrustAnchor::from_certificate` reads a certificate no further than its key: the certificate's own signature is never verified, nor is its window, its authority bit, or its key usage. A self-signature says nothing about whether a root should be trusted, since the party that signed it is the party being judged, and requiring one to be readable would rule out the certificates that carry an anchor's key without being self-signed — a root cross-signed by an older authority is signed with that authority's algorithm, which may be one this system has no arithmetic for. `GTS Root R4` in the chain `google.de` presents is such a certificate, signed by GlobalSign with RSA. What is checked is that the bytes are a certificate as far as the key and that the key is one this system can verify with, so that an unusable anchor is refused where the caller still holds the file rather than becoming a path that reaches nothing. 11.9 already gives the type as a subject and a key; this states what follows when one is read out of a certificate, which `verify_chain` assumed and nothing offered. | decided | 2026-09-05 |
| D-63 | The fuzzing engine is this project's own, ported from libFuzzer, and lives in `fuzz-support`. The compiler still emits the counters and the comparison callbacks; what a runtime would have supplied — the loop, the mutator, the table of values the target was seen comparing against, the corpus and its features, the merge, and the shrink — is Rust in this workspace. The reason is that there is no runtime to link: Apple's clang ships the address and thread sanitizers and not libFuzzer, so `-Clink-arg=-fsanitize=fuzzer` fails at the link step on the machine this system is written on, and D-3 rules out taking a dependency to get one. Porting was the smaller of the two remaining moves: libFuzzer's engine proper is under two thousand lines of C++, the rest of its ten thousand being platform support this project needs none of. The port is a derived work, so the files that carry one name both licences in their header and `NOTICE` records it; Apache-2.0 is one-way compatible with this project's AGPL-3.0-only. Three things depart from the original and are written down where they are done: the value profile keys a comparison on the constant the compiler knew rather than on the address of the comparison, which Rust will not hand out; a corpus file is named by a fast hash and not by a digest; and the fork mode, the leak detection, the memory limit, and `merge_control_file` are not there, and a run that asks for one of them stops and says so. The instrumentation is set per package rather than in `RUSTFLAGS`, so that it lands on the code under test and not on the engine that measures it. | decided | 2026-09-05 |
| D-64 | The entry point for the commands of this project on the development machine is `sh tools/xtask.sh <subcommand>`, with `sh tools/xtask-check.sh` for the full check; this amends D-35, which named rustup's Cargo proxy directly. The proxy is still what runs underneath and the xtask still refuses every other Cargo. What the scripts add is the `PATH` in which that proxy finds the rustup toolchain instead of MacPorts' `rustc`, and a pager and colors turned off; they are tracked, so a worktree session has them. They add nothing to the run itself: each ends in an `exec`, so the output and the exit status are the xtask's, and a caller may rely on `$?`. Shortening a run's output belongs to the xtask, not to a pipe in the script: `check --quiet` leaves one line per step and prints what a step wrote only when that step failed, whereas a script that pipes into `tail` returns the status of `tail`, which is zero however the run went, and cuts the failure off with everything else. CI keeps calling the proxy directly, where the `PATH` is already right. | decided | 2026-09-05 |
| D-65 | The address space of a process lives in the `Process` object: a page-table root and a `RegionTable` of `REGIONS_PER_PROCESS` regions, with no address-space object, no pool, and no `AddressSpaceId`. This corrects plan 10.5.2, which named an id it defined nowhere. A process has exactly one address space and nothing shares one, `ObjectType` carries no address space, and an id would have been an indirection in every mapping path with no second user. The kernel half is shared by copying page-map level four entries: the window stays inside entry 256, because `MAX_PHYS_WINDOW_BYTES` is 509 GiB and one entry covers 512, and the kernel stacks, the boot information page, and the image are all above `0xFFFF_FF80_0000_0000`, which is entry 511. Both entries exist once the memory bring-up is done, so a new root copies two of them, and a kernel stack allocated later changes only tables below entry 511 and is therefore visible in every address space at once; a QEMU test holds that no further kernel entry appears after boot. Loading a root is the HAL trait `AddressSpaceControl` with a double, not the free `unsafe fn activate` the adapter has today, and `kernel-core` loads one only when the incoming thread belongs to another process, because writing `CR3` costs the translation lookaside buffer. | decided | 2026-09-05 |
| D-66 | An empty object pool is all zeros and its constructor is `const`, so the pools reach the `.bss` without passing through the boot stack. `Pool::new` builds its slots with `core::array::from_fn` and `Global::init` takes its value by move; a `Pool<MemoryObject, 4096>` is around 230 KiB and the handle arena 512 KiB against a boot stack of 256 KiB, which a debug build, and every test kernel is one, would overflow. Two changes make the zero: the generation of a slot counts up in `allocate` instead of starting at one, so a generation handed out is still never zero, and the free list is implicit through a high-water mark, so a fresh pool links nothing while released slots keep the FIFO chain 6.6.6 requires. `RegionTable` carries `kernel: bool` instead of `user: bool` for the same reason. `audhsos-sync` gains a second cell beside `Global`, `const`-initialized and without the `Option`, because `Some(value)` in a `static` carries a non-zero discriminant and would move a megabyte out of the `.bss` into the image file; the crate grows past its two `unsafe` sites and 4.3 and 4.5 record what it has then. The pools live in one cell of their own and not in `KernelState`, which stays the counter structure it is. | decided | 2026-09-05 |
| D-67 | The saved machine context of a thread is one word: `Thread.context` is a `VirtAddr`, the kernel stack pointer of the thread while it is not running. `kernel-objects` therefore keeps its dependencies `kernel-types` and `audhsos-abi`, and no type parameter for the context travels through `Pool`, `kernel-sched`, `kernel-syscall`, and `kernel-core`. Plan 10.5.4 already says the saved context is that pointer, while 2.5.1 named an associated type of the HAL trait `Context`, which every crate that touches a thread would have had to carry. The trait works on the word and on the stack as a slice: `prepare_user` takes the kernel stack the caller reaches through the physical window, writes the callee-saved registers, the trampoline address, and the interrupt frame as `u64` values, and returns the stack pointer the first switch loads; `switch` takes a `&mut VirtAddr` and a `VirtAddr`. Writing that frame is arithmetic over words and is host-tested. An architecture that needs more than one word puts the rest on the stack the word points at. | decided | 2026-09-05 |
| D-68 | `net-wire` reads and writes one text per address and computes the internet checksum with the end-around carry applied to every word. Both are departures worth naming. The parse is the canonical form and nothing else: `010.0.0.1` is refused rather than read as ten or as eight, because a host that accepts both spellings can be shown one address by a check and another by a connection, and the same reasoning already made `audhsos-encoding` strict. A prefix length above 32 is `PrefixLength`, and a number above 255 after the slash is not a prefix length at all but a malformed text. The checksum departs from RFC 1071 section 2, which recommends letting the carries pile up in the high half of a wider accumulator and folding once at the end: `Checksum` folds at every word, so its accumulator is sixteen bits that provably never leave sixteen bits and every addition in the crate is a checked one under a lint profile that forbids an operation which could overflow. The deferred form is an optimization for hand-written assembly, and this crate is checked Rust on a path that carries at most an interface MTU per call. The accumulator also holds the odd byte a call ended on, so a caller may feed it a pseudo-header, a header, and a payload in any number of calls without an odd length shifting the words of the next one, which is what RFC 1071 section 2 (A) requires and what a transport checksum over three separate buffers needs. | decided | 2026-09-05 |
| D-69 | IPv6 is in the first network version beside IPv4, which supersedes the first clause of D-50; the rest of D-50 stands. `net-ipv6` joins the crate list of D-49 at layer n2 beside `net-ip`, which holds what the two families share — the routing table over `IpCidr`, the reassembly buffers, and the interface the transports send through — so that everything above n2 depends on `net-ip` alone and knows no family. The layers above the wire carry `IpAddr` and `IpCidr` and are written once for both families rather than twice, because the alternative is to widen eight crates and their tests after D8 rather than to write them wide. Three things follow and are decided here. The neighbor cache of `net-eth` is keyed by `IpAddr` and holds the states of RFC 4861, of which ARP's three are a subset; ARP fills its IPv4 half and Neighbor Discovery, which is `ICMPv6` and therefore sits above IP rather than beside it, fills the other, so the cache is driven from `net-ip` and not from the frame layer. Address configuration for IPv6 is the router advertisement of RFC 4861 with SLAAC and the recursive DNS server option of RFC 8106; there is no DHCPv6, because it is a second protocol of the size of `net-dhcp` for a capability the advertisement already carries. And there are no IPv4-mapped addresses: the two families are distinct values everywhere, an operation over one of each is `MixedFamilies` and never a conversion, and the dotted text form of RFC 4291 is refused, since `::ffff:1.2.3.4` and `::ffff:102:304` would otherwise be two texts for one address. The IPv6 text form is the canonical one of RFC 5952, section 4 in both directions, which is the same strictness D-68 applies to IPv4. What this costs is roughly D2 and D3 again, and it is paid before `net-tcp` rather than after `net-stack`. | decided | 2026-09-05 |
| D-70 | A page table is emptied where it lies. `PageTable::clear` writes the empty entry into every slot in place; nothing assigns `PageTable::default()` to a table reached through the physical window. A table is four kilobytes, a kernel stack is sixteen, and a build without optimization materializes the new value before it copies it — more than once, as it turned out: `Mapper::walk` carried a twenty-kilobyte frame and `kernel_half::free_subtree` carried one per level of its recursion. The concurrency tests of Phase 5 found it, because they are the first tests in which the reaper runs on the kernel stack of a user thread and not on the boot stack: the machine answered with a double fault at the guard page. The kernel stack stayed at four pages (D-57) for this; what changed was that the paging code no longer needed five of them. D-73 raised it to eight all the same, for a path this fix does not reach. | decided | 2026-09-05 |
| D-71 | The unsafe budget of `audhsos-kernel` rises from 22 to 26 and that of `user-test-programs` from 8 to 17. The four new sites of the kernel are the interrupt bring-up, the start of the interval timer, and the two instructions that turn interrupts off and on again, all in the test kernel the Phase 5 images share: a test of preemption needs a timer, a timer needs the first three, and the fourth is what keeps a tick out of the window between the scheduler's decision and the switch that carries it out. The nine new sites of the user programs are the reads and writes `two_threads`, `spin`, and `every_syscall` make through the pointers the kernel handed them — their own IPC buffer and the page their process shares — and the calls `every_syscall` makes through the gate. Every one of them carries a safety comment naming what the kernel promised. | decided | 2026-09-05 |
| D-72 | The IPv6 half of the first network version ends where D4 ends, and four things are outside it. There is no ICMPv6 redirect: a redirect is a router naming a better router, which is a field an attacker writes for a gain a host with one interface and one default route has no use for, so the message is not read. There are no temporary addresses of RFC 4941: an address formed here is the modified EUI-64 of RFC 4291, appendix A over the hardware address, which is stable and follows the machine, and the alternative needs a generator in a crate that has none and a rotation policy that belongs with the address selection of RFC 6724 in D9. There are no jumbograms of RFC 2675: a payload length of zero is an empty payload and not a length hidden in a hop-by-hop option, because no link this system carries traffic over has an MTU near 65535. And the flow label is written as zero and not read: RFC 6437 has a host draw one from something unguessable to help routers keep a flow together, and zero is the value the document defines for a host that does not. The extension header chain is walked to a bound of eight headers and 512 bytes, which is wider than any correct sender needs — RFC 8200 recommends at most one of each of six kinds — and is the end of a walk over a linked list an attacker writes. | decided | 2026-09-05 |
| D-73 | A kernel stack is eight pages, not the four of D-57, and `Pool::release` no longer hands the object back. Both come out of one measurement, taken when the system call tests of 6.6.21 first made `process_create` from ring three and the machine answered with a double fault at the guard page. The deepest path from the system call gate carries a `Process` — four kilobytes of region table and thread slots — through three frames in a row of the object pool, and measured 33 KiB in the unoptimized build the tests run. `Pool::release` returning the value was two of those three frames and bought nobody anything: a pool holds what nothing else does, so there is nowhere for the object to go, and every caller in the kernel already threw it away. It now returns whether the last reference is gone, which leaves the deepest path at 23 KiB, and the stack is eight pages to hold it. A thread therefore costs nine frames of the reserve instead of five, which amends the table of plan 10.5.0: 2304 frames of the reference machine's 4048, leaving 1744 for page tables. The precedent is `BOOT_STACK_PAGES`, which has been sixty-four for the same reason since Phase 1; the number is what the build under test needs, and a later measurement of a release build may lower it. | decided | 2026-09-06 |
| D-74 | The thread queues of the kernel are intrusive lists over fields of `Thread` and not `audhsos_collections::IndexList`; this supersedes the kernel clause of D-48, which the run queues of Phase 5 already departed from without saying so. `IndexList` walks a contiguous `&mut [Link]` indexed by node number, and a thread is a slot of a `Pool<Thread, N>` behind a generation, so the links would have to live in a second array beside the pool and be kept in step with it; and the order of a wait queue is priority and then FIFO, which `IndexList` cannot express, having no insertion between two nodes. A thread therefore carries two link pairs, `queue_links` for the run queues of `kernel-sched` and `wait_links` for the wait queues of `kernel-ipc`, and is in at most one of the two at a time, because a thread in a run queue is `Ready` and a thread in a wait queue is blocked. They are two fields and not one because `Scheduler::dequeue` unlinks through `queue_links` and then corrects the head, the tail, and the ready bitmap of a run queue: handed a thread whose links pointed into an endpoint queue it would splice that queue and leave the endpoint naming a thread that is no longer in it, which is what `thread_kill` and `thread_suspend` of a blocked thread would do on every call. Beside the links a thread carries `wait`, which names the object and the queue it waits in, so a cancellation finds the queue without searching every endpoint; this is the `ipc_state` plan 10.5.2 sketched and Phase 5 had nothing to put in. The network stack keeps `IndexList`, and so does every user of D-48 outside the kernel. | decided | 2026-09-06 |
| D-75 | A thread blocked on an object holds no reference to it; this supersedes the clause of 2.3.4 that counted blocked threads among the references, and 2.3.4 changes with it. References come from handles, from mappings, and from bindings. The reason is that the other rule makes the wake-up the same section requires unreachable: if a waiter holds a reference, closing the last handle to an endpoint under waiters destroys nothing, the endpoint outlives its last capability, and the only way left to reach a reference count of zero is for every waiter to be killed first — at which point there is nobody left to wake with `ObjectDestroyed`, and the wake path is dead code under a branch coverage gate that would rightly refuse it. With this rule closing the last handle destroys the endpoint, the notification, or the reply object and wakes everyone who waited, with `ObjectDestroyed` for the first two and `ReplyDropped` for the last, which is what 6.6.8 asks for and what a server that exits owes its clients. A queue entry is not a claim on the object's existence; it is a thread waiting for something that may never come. | decided | 2026-09-06 |
| D-76 | Miri runs the tests of the modules that hold host-executable `unsafe`, not every test of the crates those modules live in. What Miri buys is the aliasing and provenance check on `unsafe`; what it costs is interpretation instead of execution, one to two orders of magnitude, and safe logic pays that price for nothing, being already covered by `test --host` and by the coverage gate. `MIRI_TARGETS` in `policy.rs` therefore carries a test-name filter per crate: `audhsos-sync` runs whole, because it is two `unsafe` sites and the cell around them, and `fuzz-support` runs `tests::counters` and `tests::sancov`, the modules that test its two files holding `unsafe`. The line is drawn where the `unsafe` is written and not where it is reached: `tests::mutate` reaches the same global through `sancov::with_trace`, which `tests::sancov` interprets directly, and it does so over sixty thousand mutation rounds — which was almost the entire cost of the step. Measured on the development machine, `miri` went from 243 seconds to 4 and the full check from just over six minutes to two. `unsafe_budget::miri_gaps` holds the filters to the code: a product file with an `unsafe` site whose module no filter names fails the step and names the filter that is missing, so `unsafe` cannot appear in a new module and quietly leave Miri's reach. Miri itself is not parallel — it interprets one step at a time and schedules emulated threads cooperatively, so a test harness with sixteen threads occupied one core — and there is nothing left in a four-second step to spread over cores anyway. | decided | 2026-09-06 |
| D-77 | RSA verification is two crates, not an addition to `crypto-ec`. `crypto-bignum` at `crates/crypto/bignum` holds the limb arithmetic, and `crypto-rsa` at `crates/crypto/rsa` holds the key, the two encodings, and MGF1. The move is forced rather than tidy: `montgomery::Params` carries the modulus, its inverse, and the conversion constants as associated **constants**, which is right for four moduli that are known when the code is compiled and unusable for a modulus that arrives in a certificate. What survives the move is the part that already takes its modulus as an argument — the `montgomery`, `add_limbs`, and `subtract` of the current module — and what is new is a `Modulus` that derives `n0inv` and `R2` at run time. `crypto-ec` keeps its four `Params` implementations and imports the arithmetic; its P-256 and P-384 test suites are the regression net for the move, which changes no behaviour. The alternative, letting `crypto-rsa` depend on `crypto-ec`, would have RSA depend on a crate named for elliptic curves for the one part of it that has nothing to do with them. | decided | 2026-09-06 |
| D-78 | One width, chosen at run time. `crypto-bignum` holds a modulus in a fixed array of sixty-four limbs, 4096 bits, together with the count of limbs actually in use, and every loop runs over that count. Limbs above it are zero, which is an invariant the type keeps and the tests check. The alternative was a const generic width per key size, as `crypto-ec` has for its curves, and it does not fit: the caller does not know the width, the certificate does, so a const generic would have to be resolved by a match over three instantiations of every function, which is three copies of the arithmetic to review instead of one. The cost is that a 2048-bit verification carries a 4096-bit array. The cost is a stack frame, not time — the loops run over the used limbs — and the frame is about a kibibyte for the modulus with its conversion constant, and about three for a verification with its temporaries. | decided | 2026-09-06 |
| D-79 | What this system accepts as an RSA key: a modulus of 2048 to 4096 bits, odd, with its top bit set, and a public exponent that is odd and at least three and below `2^64`. Everything else is refused where the key is read and never reaches the arithmetic. The lower bound is a certificate rule and lives in `audhsos-x509`, not in `crypto-rsa`: the primitive verifies what it is given, and the layer that judges a certificate is the layer that decides a key is too small. That separation is what lets the RFC 8448 trace exercise the primitive at 1024 bits (D-83) while no chain with such a key is ever walked. The upper bound is what D-78 can represent. `TrustAnchor::from_certificate` applies the same rule, so an anchor whose key is out of range is refused while the operator still holds the file, which is D-62 unchanged. | decided | 2026-09-06 |
| D-80 | A PKCS #1 v1.5 signature is verified by building the encoding and comparing it, never by parsing the one that arrived. RFC 8017 section 8.2.2 gives both shapes: steps three and four construct `EM'` and compare, and the note after them offers a decoder that recovers the hash instead, weighed by storage against code size. This project takes the first, for a reason the note does not give: a decoder is where a signature with slack in its padding is accepted, and a construction has no slack to accept. Two consequences are stated rather than discovered. A `DigestInfo` encoded under BER but not DER — an indefinite length on the `SEQUENCE`, say — is refused, though PKCS #1 v1.5 permitted it; RFC 8017 calls the case unlikely in practice and this project agrees. And the algorithm identifier of `sha256WithRSAEncryption` and its two siblings takes parameters that are NULL **or** absent, both accepted, per RFC 4055 section 5, which is a rule for these three algorithms alone: ECDSA and Ed25519 still require the field to be absent, and `SignatureAlgorithm::parse` gains a rule per algorithm rather than one for all of them. | decided | 2026-09-06 |
| D-81 | PSS is supported in the `rsae` shape and no other. The three schemes `rsa_pss_rsae_sha256`, `rsa_pss_rsae_sha384`, and `rsa_pss_rsae_sha512` are offered and verified, each with MGF1 over its own hash and a salt as long as that hash's output, which is what RFC 8446 fixes for them. In a certificate, `id-RSASSA-PSS` is read only with those same parameter sets and refused otherwise. What is out of scope, and stated so that the boundary is not mistaken for an oversight: `rsa_pss_pss_sha256` and its two siblings, which name a key restricted to PSS by an `id-RSASSA-PSS` algorithm in the subject public key information, together with the `RSASSA-PSS-params` such a key carries. They are rare on the public web, and reading them means a second key kind whose parameters constrain every later verification. | decided | 2026-09-06 |
| D-82 | The two lists of signature schemes are not the same list, and RSA makes the difference sharper than D-61 did. The `ClientHello` offers `rsa_pkcs1_sha256`, `rsa_pkcs1_sha384`, and `rsa_pkcs1_sha512` alongside the three PSS schemes, because RFC 8446 section 4.2.3 gives the `rsa_pkcs1_*` code points exactly one meaning — the client can verify a certificate signed that way — and a client that stays silent about them tells a server its chain may not be signed the way nearly every chain is signed. The same section forbids those three in a `CertificateVerify`. So a `CertificateVerify` naming `rsa_pkcs1_sha256` is refused as an illegal parameter, by a client that asked for the code point one message earlier. The offer is about the chain, the refusal is about the handshake signature, and they do not contradict each other. | decided | 2026-09-06 |
| D-83 | No RSA key is generated by project code. Key generation needs random primes and a primality test, which would be the largest piece of arithmetic in the track and would serve test data alone; signing with a key that is already known needs only the exponentiation the verification already has. So the test certificates of `audhsos-x509` are signed with fixed key pairs held as constants in test source, and the register records where each came from. The 1024-bit pair is not a made-up number: it is the one RFC 8448 section 2 prints, with the traces that carry real `rsa_pss_rsae_sha256` signatures under it, which makes it a vector rather than a fixture. It is below what a certificate may carry (D-79), so it exercises `crypto-rsa` directly and no chain. The 2048-bit and 4096-bit pairs used for certificates are generated once outside this repository and recorded with the procedure that produced them. This is the one place where a number this project did not compute enters its tests, and it is narrower than the rule it bends: `crypto-ec` generates nothing either, and every certificate is still built and signed by project code. | decided | 2026-09-06 |
| D-84 | A transport of the network stack does not send. It writes its segment into the caller's buffer and names the two addresses it wrote it for; which of the two senders carries it out is the decision of the facade of 12.6.12. `net-udp` therefore depends on `net-wire` and `crypto-rng` and on no internet layer at all, which refines the clause of D-69 that has everything above n2 depend on `net-ip`: what that clause is about is knowing no family, and reaching no further down than the wire is the stronger form of it. Four points of `net-udp` follow and are decided here. The length field of a datagram is what is believed: bytes behind it are padding a link layer left standing and are cut away, and a length that reaches past what arrived is an error, so that the checksum and the payload always cover the same bytes. The receive ring holds self-describing records in the caller's memory rather than fixed slots — each record carries the two addresses, the source port, and the payload, and a record that no longer fits behind the last begins at the front of the buffer instead of being cut in two — so a payload comes back as one slice and no slot is larger than the datagram in it; a full ring drops the newest datagram and counts it, and one that would not fit an empty ring is refused at entry. A port is held once, whichever of this host's addresses holds it: a socket bound to one address takes only what named it, a socket bound to none takes everything that reaches the port including broadcast and multicast, and two sockets on one port with different local addresses is a distinction this system has no use for. An ephemeral port that is taken is answered by drawing again and not by walking to the next port, as RFC 6056, section 3.3.1 asks, bounded at eight draws; the dynamic range of RFC 6335 is exactly `2^14` ports wide, so the low fourteen bits of two random bytes name one of them without bias. | decided | 2026-09-06 |
| D-85 | Five points of `net-tcp`, which document 12 left open. The send and receive windows are two byte rings the caller supplied; a segment that arrives out of order is written straight to its place in the receive ring, where its sequence number says that place is, and a fixed list of four ranges remembers which parts have arrived until the gap in front of them closes — a range that does not fit is forgotten and the peer sends its bytes again, which is sound because a sender without selective acknowledgment learns about the first gap and about no other. Passive open has no backlog: one connection may stand in `LISTEN` and the first matching `SYN` makes it that connection, which is what two complete machines back to back need and what nothing else in this project asks for. The connection table lives in this crate rather than in the facade, because a segment that matches no connection is answered by the processing of state `CLOSED` in RFC 9293, section 3.10.7.1, and which numbers that reset carries is decided by header fields nothing above this crate reads. The urgent pointer is read and never acted on, as RFC 6093 recommends for a receiver that offers no interface for asking. And the two send pointers are kept apart: a segment that carries nothing carries `SND.MAX` and an acknowledgment is judged against `SND.MAX`, because a retransmission timeout winds `SND.NXT` back and a bare acknowledgment at the older number is one the peer discards as an old duplicate while an acknowledgment of what went out before the rewind would look like an acknowledgment of what was never sent. | decided | 2026-09-06 |
| D-86 | Phase 6 raises four unsafe budgets, and a process and a thread each hold one reference to themselves. The budgets: `kernel-hal-x86_64` from 129 to 142 unsafe sites and from 24 to 27 `asm!` sites, which are the word and double-word port reads and the word write beside the three widths that existed, and the six methods of `Ports` that reach them; `audhsos-kernel` from 26 to 27, because the forwarding of a device interrupt reads the buffer of the thread it woke through the physical window; `user-sys-x86_64` from 11 to 13, for the `returning` that hands out both return words, which `ipc_recv` needs for its reply handle; and `user-test-programs` from 17 to 65, for the five new programs — `ipc_client`, `ipc_server`, `fault_handler`, `write_unmapped`, and `notification_waiter` — each of which reaches the gate through `user-sys-x86_64` and writes what it observed into the page its process shares with the test. Every site carries a safety comment naming what the kernel promised. The second half of this entry is the reference model, which amends 2.3.2 in one place: a handle is a reference and the last one destroys what it names, except for a process and a thread, which hold one further reference of their own. A process ends when it is killed and a thread when the reaper has given back its kernel stack and its buffer, whatever handle still names either of them; the handles that named one afterwards name nothing. The reason is the reaper: it needs the `Thread` object after the last handle to the thread has closed, to find the kernel stack and the IPC buffer to give back, and a reference count that let the last handle free the pool slot would leak both. `Pool::force_release` is what a kill and the reaper do, and it is the only operation that frees a slot without counting. | decided | 2026-09-06 |
| D-87 | Six points of `net-dns` and `net-dhcp`, which document 12 left open, and one correction to their catalog entries. Both crates reach the network the way D-84 has a transport reach it, one layer up: a poll writes a complete UDP datagram into the caller's buffer and names the two addresses it was written for, and what comes back in is a payload with the address and port it arrived from, which is exactly what a receive record of `net-udp` carries. The randomness of D-51 is therefore drawn once in each crate that owns a field: `net-dns` draws its transaction id and takes its source port from the ephemeral socket the caller bound through `net-udp`, and `net-dhcp` draws its transaction id and the jitter on its backoff, both its ports being fixed by RFC 2131. The correction is that both crates depend on `audhsos-time` and `audhsos-collections` besides `net-udp`, `net-wire` and `crypto-rng`: a state machine with a deadline needs an `Instant`, which the catalog entry of 12.6.2 had left out. The resolver asks `A` and `AAAA` at once and not one after the other — two transaction ids, two attempt counters, two places in the server rotation, one deadline — because a family that never answers must cost its own attempts and not the whole deadline, which is what asking in sequence would spend on the first question. An alias the answer does not resolve is asked on its own, with the same deadline and the same budget of eight links, so that the budget governs the chain however many messages it is spread over; a record the chain has already stepped through ends it as a loop rather than spending the budget. A decoded name is an owned value of at most 255 bytes and never a borrow into the message: a compressed name is not contiguous in those bytes, and the resolver has to hold the name it is asking across the datagrams it asks in. A transaction id is kept across retries, because a fresh one per retry would make every answer that is merely late unusable and would leave an attacker the same window. In `net-dhcp` the BROADCAST flag of RFC 2131, section 4.1 is set until the address stands and clear from the bound state on, which is what keeps a host that cannot yet accept a unicast datagram from being unable to learn the address it is being given, at a cost of two broadcast frames per lease; the first offer that carries a server identifier is taken and a second is ignored, because collecting offers would need a timer and a policy for a choice this system has no basis to make; and an acknowledgment carrying no subnet mask, no lease time or no server identifier, a mask whose bits do not run together, or an address no host can hold — the unspecified one, the limited broadcast, a multicast group, a loopback address — is one no lease can be made of, so it is ignored and the request is asked again; `Lease::from_reply` is public so that which of those it was is a question a caller can ask. Two bounds are stated here rather than left implicit: a response longer than the 512 bytes of RFC 1035, section 4.2.1 is not read, because this resolver announces no buffer of its own; and in both crates the attempt counter and the backoff move only after the datagram is in the caller's buffer, so a buffer with no room for one costs nothing. | decided | 2026-09-06 |
| D-88 | Nine points of `net-http` and `net-stack`, which document 12 left open. Two framings in one HTTP message are an error and not a preference: RFC 9112, section 6.3, point 3 lets a recipient prefer `Transfer-Encoding` over `Content-Length` and says in the same paragraph that such a message ought to be handled as an error, and this client does the latter, because a preference rule is a second reading with a tie-breaker rather than one reading and two readings of one message is the whole of request smuggling; two `Content-Length` fields that disagree go the same way and two that agree are the value they agree on, as point 5 has it. The obsolete line folding of section 5.2 is refused although that section has a user agent unfold it, for the same reason: a folded value is a value whose length is not its line's length. `obs-text` is refused although RFC 9110, section 5.5 allows it, which is what makes every field value this crate hands out ASCII and therefore text rather than bytes a caller has to decide the meaning of. The response decoder takes at most one line of the head per call, which is what keeps a byte of the body from ever being copied into the buffer the head is assembled in and what makes a response split at any boundary decode to what the whole of it decodes to; and a body with no declared length ends at the close, which is point 8 of the same list and the reason `finish` exists, because without a caller saying the peer closed, a body that ended and a body that was cut off are the same bytes. In `net-stack` the frames that have been written wait in a queue in the caller's memory as self-describing records and the layers are asked for new work only when it is empty, which is what makes a transmit buffer of one frame enough — nothing is produced while there is a backlog, so nothing is lost and nothing overtakes anything — and one received frame can make several go out, which is why a single slot would not do. A handle is an index and the generation of the slot it names, as the kernel's handles are, and a slot that has been through every generation hands out no more rather than letting one come back to life. Two limits are the caller's, sockets and connections; routes, neighbors, held datagrams, reassembly buffers and prefixes are constants of the crate, because they are properties of a host with one interface and not of what runs on it. Neighbor Discovery and the address configuration client do not go through either sender: the first because RFC 4861, section 7.1 requires a hop limit of 255 and needs no route and no neighbor resolved, the second because a client with no address has no route either. And connecting to a list of addresses and connecting to a name are two operations — the loop is `connect_to_any` and the ordering is `connect_to_name` over it — with the policy table of RFC 6724, section 2.1 read with its `::ffff:0:0/96` row standing for the whole IPv4 family, since D-69 refuses the mapped form that row is keyed by; the rules a host with one interface, no deprecated addresses, no Mobile IPv6, no privacy extensions and no tunnels cannot decide are named where they are not implemented, and Happy Eyeballs stays above the stack. | decided | 2026-09-06 |
| D-89 | The userland crates hang the other way round, the `GlobalAlloc` adapter waits, and a typed handle does not close itself. 05 5.2 had `user-rt` depend on `user-sys-x86_64`, and that edge cannot hold: `user-sys-x86_64` is an adapter for `x86_64-unknown-none` with an `asm!` in it, so everything above it is built for the machine and tested nowhere else — while 6.6.12 wants the allocator host-tested, 6.6.13 wants a fuzz target over the tar reader, and 6.6.23 wants the memory server driven by a recording double. 10.7.1 made the same edge a cycle by putting a `GlobalAlloc` adapter over `user_rt::heap::Allocator` into the adapter crate. So `user-rt` is a logic crate at layer u0 that depends on `audhsos-abi` and `audhsos-collections` and makes no system call at all, and `user-sys-x86_64` sits above it at u1 and uses its message writer. The assembly is unchanged: one `int 0x80`, as before. The `GlobalAlloc` adapter is not built. No product code in this workspace uses `alloc` — the only `extern crate alloc` sites are the `strategies` modules that generate test cases on the host — and 8.16 and 8.17 write down for the network and the cryptography that their crates allocate nothing; the crates of phases 9 to 11 are fixed-capacity by design. The allocator *logic* is built and tested, because 6.6.12 asks for it and because it is arithmetic without a pointer in it; the shell that turns its offsets into raw pointers is written the day a program wants `Box`, and it costs two or three `unsafe` sites then rather than now. Last, the typed handles of 10.7.2 do not close themselves in `Drop`. `handle_close` is a system call, a system call needs the IPC buffer of the calling thread, and `Drop::drop` is handed nothing but the value; the alternative is a global holding the buffer address, which is wrong the moment a process has two threads, because each thread has its own buffer page and this system has no thread-local storage. The handles are `#[must_use]` and a program closes what it is done with through the gate. | decided | 2026-09-06 |
| D-90 | The kernel joins two memory objects again. `memory_merge(lower, upper)` is a forty-second system call: the two must lie side by side in that order, agree in kind and cache policy, carry the same rights, and be held by the caller and by nothing else; the lower object grows to cover both, the upper one ceases to exist, and the kernel object it occupied goes back to the quota. This supersedes the clause of D-12 that has objects split and never merged. What forced it is not a test: an object that can only become smaller means a memory server that hands memory out and takes it back grinds its objects down to single pages and can never serve a large request again, which is fragmentation with no floor under it. The catalog saw it — 6.6.23 has asked since it was written that two released neighbours be handed out as one object for a request of their combined size — and with a kernel that only splits, that item is unsatisfiable. The condition that neither object may have a second reference is what keeps the operation safe: a mapping is a reference (D-86), so an object that something maps cannot be dissolved under it, and the check is one comparison against one, not a search through every address space. The rest of D-12 stands: the kernel still allocates no user memory after boot, and the memory server still zeroes an object before it hands it out and again when it comes back. | decided | 2026-09-06 |
| D-91 | The creator of a thread may supply the page its IPC buffer goes in. The fifth argument of `thread_create` was reserved and had to be zero; it is now a memory object of one page of `Ram`, and zero still means what it meant — the kernel takes a frame out of its own reserve. What forced it is that 2.10 has always required the root task to write the startup message into the buffer of the child before it starts the thread, and there was no way to do it: the kernel allocated the frame and mapped it in the child, and the parent held nothing that named it. Everything else the parent gives the child it installs as a handle, and a handle is a number the child cannot read the meaning of; the message is what says which number is which, and without it a child holds capabilities it cannot tell apart. With this the parent asks the memory server for a page, maps it, writes the message, unmaps it, and hands the object to `thread_create`. Two things follow. The buffers of userland threads come out of userland memory instead of the kernel reserve, which is the scarcer of the two. And the parent keeps a handle to the buffer of its child, so it can read and write it while the child runs — which is no authority it did not have, because a parent holds `MAP` on the process and can map anything into it anyway. The reaper gives the frame back to the object rather than to the reserve, so `Thread` carries which of the two it was. | decided | 2026-09-06 |
| D-92 | The root task is an ELF, and its wrappers are written out by hand. This supersedes the clause of D-27 that has the boot image carry the root task as a flat binary; the rest of D-27 stands, header and archive alike. 10.7.5 and 10.7.6 had the kernel map the root task as a flat binary the xtask converts with `llvm-objcopy -O binary`. A flat image says nothing about which of its pages may be written and which may be executed, so either the whole program is writable and executable, or `.data` and `.bss` are unwritable and the program cannot hold a variable. Neither is a program. The kernel therefore reads the root task through `audhsos-elf`, which it already carries for the loader, and maps every segment with the permissions its header names, under the constraints of user space; the linker script puts every section on a page of its own, because two segments in one page would need one set of permissions for both, and `kernel_core::root` refuses an image where that happens rather than guessing. The second half of this entry is the system call wrappers of `user-sys-x86_64`: 10.7.2 and 05 5.2 left open how the forty-two of them are written, and a macro over the table of `Syscall::ALL` was the obvious answer. It is not the answer: what a wrapper does with the buffer is the whole of the seam between a program and the kernel, and a reader who cannot see it cannot check it. The wrappers are written out one by one, and a constant assertion holds the list of them to the table: `COVERED` names every call the gate is meant to cover, `Syscall::ALL` names every call there is, and a build fails when the two differ in length or in order. It caught the missing wrapper for `memory_merge` the hour the call was added. It does not prove that a method exists for each entry or that each method passes the entry it belongs to — a list of values cannot, and enumerating the methods of a type needs the macro this entry rules out. That half is proved by running them, which D-98 records. | decided | 2026-09-06 |
| D-93 | A bound interrupt line is armed, and an answer consumes the reply handle with the reply object. Both are corrections the first real userland run forced, and both were invisible until something used them. `interrupt_create` writes a masked redirection entry, because a line the plan routes but no notification names would assert into nothing; nothing unmasked it afterwards but `interrupt_ack`, which a driver reaches only after the first interrupt. So a driver that bound its line and waited waited forever. The binding is the moment the line has somewhere to go, and it is where the line is armed; from there the cycle of 2.7 runs as it is written. The second: 2.6 says `ipc_reply` consumes the reply object, and it did — but the handle that named it stayed in the server's table, pointing at a destroyed object, holding the reference that would have let the object return to its pool. A server lost one handle and one object slot per call and stopped receiving after as many calls as its table has slots, which for the console driver is sixty-four. The answer now closes the handle as part of itself, and a second reply through it says `InvalidHandle` rather than `InvalidState`: the capability is gone, not merely spent. | decided | 2026-09-06 |
| D-94 | A child reports to its parent over the endpoint its faults go to, and the root task ends the machine. The kernel cannot end it any more — once the root task runs the kernel only answers calls — so the end of a run has to come from the userland if it is to come at all. `Role::Parent` is an eleventh role of the startup message: a capability to the endpoint the kernel sends this process's faults to, badged with what the parent knows the child by. One endpoint therefore carries both kinds of news about a child, told apart by the label, and a parent that waits on it hears of a program that finished and of one that broke in the same place. The `parent` protocol has one message, `Finished { status }`, and no reply: the child sends it and exits, so there is nobody left to answer. When every child that reports has reported, the root task takes the port of the exit device through its `SystemControl` and writes to it. On a machine with no such device the write reaches nothing and the thread exits, which is right: a system whose work is done and that cannot switch the machine off stops. `test --e2e` waits for that end instead of killing the machine, so the last step of a run is tested like every step before it. | decided | 2026-09-06 |
| D-95 | The badge of a fault handler travels with it, and a memory object is recognized by the memory it covers. Both are what the first system with more than one child forced. A fault is a message like every other, and what a server knows a sender by is the badge of the capability the message came through; `process_set_fault_handler` kept the endpoint and dropped the badge, so every fault reached the handler under badge zero and a root task serving five children could not tell whose fault it was. The process now keeps both. The second: a handle that travels through a message is copied into the receiver's table under a number of the receiver's own, so the number a client gives back to the memory server is not the number the server handed out. An object is therefore recognized by the region it covers — which is what an object is — and the store closes the second name once it has matched it, because an object with two names in one server can never be joined to its neighbours. The client gives its own capability up when the release succeeds, for the same reason, and a join the kernel refuses leaves the two objects where they are: two free pieces side by side are no worse than one, only smaller, and a release that failed over it would be worse than both. Last, `OutOfMemory` is a twenty-sixth error of the interface. A memory server with no region large enough answered `OutOfKernelMemory`, whose message speaks of the kernel reserve and is a false statement in the mouth of a userland server. | decided | 2026-09-06 |
| D-96 | Phase 7 raises three unsafe budgets and opens a fourth. `kernel-hal-x86_64` from 142 to 144, for `memory::physical_slice`, which is what reads the root task out of the boot image: the physical window maps memory contiguously, so a range of frames is a range of bytes, and the conversion from the one to the other is the one `unsafe` plus the `unsafe fn` that carries the promise. `audhsos-kernel` from 27 to 31, for the bring-up of the root task in `task.rs`: the windows over the boot image and over the buffer of a thread the kernel writes into before it runs. `user-sys-x86_64` from 13 to 21, for `Gate`: `adopt`, which turns the address the kernel put in a register into a reference to the thread's buffer, the reader and the writer over it, and the `_start` and entry-section attributes the two program macros emit. And `user-programs` opens at 18 with no assembly at all: the mappings the root task writes a startup message through, the window the memory server zeroes in, and the three sites of the two applications that read or write memory they have mapped themselves. Every site carries a safety comment naming what the kernel promised. The count of a program crate is the count of every one of its binaries together, which is why one number covers seven programs. | decided | 2026-09-06 |
| D-97 | Every program of the userland is a binary of one crate. 05 5.1 had `server-init`, `server-name`, `server-console`, `server-memory` and `app-hello` as crates of their own, five manifests beside the three that hold what is worth testing. A program of this system is a loop — receive, decode, ask the policy, encode, reply — around a logic crate, and what is only in the program is the part that needs a capability or a pointer; five crates of that are five manifests, five entries in the policy table, five unsafe budgets and five places to keep in step, for at most a few hundred lines each. `user-programs` is one crate with one binary per program under `src/bin/`, one manifest, one policy entry and one budget, and the logic they wrap stays where it was: `server-name`, `server-console` and `server-memory` remain host-tested crates that make no system call. What the split buys elsewhere — a crate boundary that keeps `unsafe` out of the logic — is bought here by the logic crates, not by the program crates. | decided | 2026-09-06 |
| D-98 | The wrappers of the gate are checked by running them. It raises the unsafe budget of `user-test-programs` from 65 to 66, for the one site of `every_wrapper` that turns the address the kernel put in a register into the gate of the thread, as every program of that crate does. D-92 writes them out by hand and holds the *list* of them to the system call table with a constant assertion; what no list of values can see is whether a method exists for each entry and whether each method passes the entry it belongs to. Two wrappers that swapped their calls pass the assertion, and the kernel does not notice either: it checks the argument count of the call it was given, and two calls of the same arity have the same count. So the seam between a program and the kernel had a hole in it that nothing in the system could find. The test image `wrappers` closes it. The user program `every_wrapper` calls all forty-two methods in the order of the table, each with a handle that names nothing so that every call is refused and none of them waits for a partner or ends the thread; the kernel writes down the number each call arrived under, which is the number the wrapper wrote into the buffer. The sequence has to be the table, with `thread_exit` moved to the end because it does not come back. A swapped pair, a wrapper the program forgot, and a call added to the table without one all fail there, each with the number it passed and the number it should have. | decided | 2026-09-06 |
| D-99 | Nine points of `virtio-queue`, which document 12 left open. The free set is the queue's own `BitSet` and not a list threaded through the `next` fields of the descriptor table, which is how the classic implementation does it: the device cannot write that table, but it writes the used ring, and a used element naming a descriptor that is already free is exactly what tears a free list kept in shared memory — here it is one bit test and a refusal. The same bit bounds the chain walk, so the walk needs no step counter: every step frees one descriptor that was in use, there are at most `size` of those, and a step onto one already free is refused. A region is measured where it is used and not up front, so that every refusal names the byte it wanted; the descriptor table is the one exception, measured before the first descriptor is taken, so that a short table costs none. `QueueMemory` carries a `barrier` beside its three accessors, because the ring entry has to reach the device before the index that publishes it and the used index before the element it names; the default does nothing, which is what a host test wants, and leaving the method out would have left the crate correct on a host and wrong on a machine. A used index is believed only as far as the driver has chains outstanding: the distance from the last index the driver read is compared against that count, which refuses a backwards move and a forward jump with one comparison, there being no way to tell them apart in sixteen bits that wrap. The size of a queue is bounded from above by its own type — the largest power of two a `u16` holds is 32768, which is what virtio allows — so the only checks are zero, a power of two, and the width of the free set, and the number of bits is a const parameter because a `BitSet` is parameterised by words and not by bits (see `audhsos-collections`). Every ring operation takes the `Device`, and the boundary is not one line but two, which section 3.1.1 draws: filling a queue is step 7 and `DRIVER_OK` is step 8, so buffers go in from `FEATURES_OK` on — a receive queue is filled before the device is live and consumed after, and a queue that waited for `DRIVER_OK` would refuse the one thing step 7 is for. What waits for `DRIVER_OK` is the notification, which that section forbids earlier in as many words, and the used ring, which section 2.1.2 forbids the device to have written earlier. Queue construction takes no device at all. The length a used element reports is believed only as far as the chain gave the device room to write: the walk that frees a chain adds up the lengths of its device-writable descriptors, and a report above that sum is refused. Section 2.7.8.2 lets a device report fewer bytes than it wrote, because one that failed part way may not know how far it got and reporting too few is what keeps a driver from handing out memory that was never overwritten; it does not let a device report more than it was given room for, which is true in no failure case and which a caller that indexed with it would follow out of its own buffers. The chain is freed and the element consumed either way, so only the number is refused and the queue does not stall. A chain whose buffers put one the device writes before one it reads is refused and not quietly sorted, the specification requiring the readable ones first: the order of the buffers is the order of the bytes in the request, so reordering them would answer a different request than the one asked. And `negotiate` refuses `INDIRECT_DESC`, `EVENT_IDX` and `RING_PACKED` by name instead of masking them away, requires `VERSION_1` and fails a device that does not offer it, and treats a feature the driver wanted and the device does not offer as neither — the accepted set comes back and whether the device is usable without it is the driver's judgement. | decided | 2026-09-06 |
| D-100 | The reference documents of a second standards body are kept the way D-59 keeps the first, under `docs/oasis/`, with the same rule and the same reason: nothing there is compiled, linked, or read at run time, rule R8 is untouched, and the file is there so that a constant can be checked without a network and so that the source cannot change under a crate that cites it. Three points are particular to this one. The version kept is 1.4, which is a Committee Specification and not an OASIS Standard, because the last version of the virtio specification to become a standard is 1.1 of 2019 and citing a document two versions behind what devices are built against would be worse than naming the stage plainly; a Committee Specification is published at a fixed address and does not change, which is the property this directory needs, so the stage directory `cs01/` is what was fetched and not the latest-stage alias one level up. The copy did not come from the document's own address: a content delivery network in front of it rewrites every address in the front matter with a key drawn afresh per response, so two fetches differ in 144 lines and a checksum of either records nothing. It came from the archive the same directory publishes, which is served as a file and is byte-identical across fetches, and the README records the checksum of both the archive and the file taken out of it, so the copy can be checked. And what is kept is the HTML rendering rather than the LaTeX sources the document names as authoritative, because a constant is looked up by reading and the sources are a build; the archive holds them and its address and checksum are recorded. | decided | 2026-09-07 |
| D-101 | Phase 8 measures the two round trips of this system from inside the kernel, and raises two unsafe budgets to do it. The entry of the system call gate is the one point of the kernel that a round trip passes through exactly once, so the difference between two entries of the same call, made by a thread that does nothing else in between, is one round trip and needs neither a counter in user mode nor a second instruction in the interface. What that costs is one wrapper in the adapter — `instructions::read_tsc`, `rdtsc`, one `asm!` — which raises `kernel-hal-x86_64` from 144 to 145 unsafe sites and from 27 to 28 assembly sites; and three programs of the measured pair, which raise `user-test-programs` from 66 to 78. The measurement is inside what it measures: every round trip carries the lookup of the hook and one `rdtsc`, and that is not subtracted from the figures, because a number that has had something taken out of it is a number of nothing that ran. The `[bench]` line of 03 3.1.7 carries the median, which is what the plan asks for; the mean over the whole run is reported beside it because the counter of the reference machine advances in steps of a thousand ticks and a single difference is quantized by them. | decided | 2026-09-07 |
| D-102 | The `syscall` instruction path is not implemented; system calls keep entering through vector `0x80`, and D-15 stands unchanged. The measurement D-101 provides is what decides it, and it decides it twice over. First by what it shows: a call and its reply cost about eight times a bare entry and return (08 8.10), so the entry instruction is a fraction of a fraction of the work of an IPC, and no share of it that `syscall` could win would be visible in what this system does. Second by what it cannot show: the reference machine is an emulator without hardware virtualization, every instruction of the measured path is translated, and the difference between `int` and `syscall` on a processor — tens of cycles — is below the resolution of a counter that steps by a thousand ticks. A path that costs a second entry with its own stack switch, three model-specific registers to program, and more assembly and unsafe in the adapter is not taken on evidence that this machine cannot produce. What would reopen it: this system running on hardware, where the two can be measured against each other, or a workload whose cost is dominated by short calls. | decided | 2026-09-07 |
| D-103 | The startup message carries values as well as handles. A role names either a handle installed in the process's table or a plain word; `Role::carries_value` says which, and the reader checks the second word of a pair as a handle only for the roles that name one. What makes it necessary is the framebuffer: the display server needs the width, the height, the stride, and the format of the mode the firmware set, and those are numbers and not capabilities — the memory object over the framebuffer says nothing about them, and `system_info`, which does, needs the root authority the display server must not have. The two alternatives were weighed. A reply on the parent protocol would give that protocol its first answer, and its one message has none by design, because a child sends it and exits. A page of memory holding six words would be a second description format, written by one program and read by one other. What this costs is the reader and the writer of `audhsos_abi::startup` and one variant of `StartupError`; what it buys is that every later device whose description is numbers travels the way the first one does. | decided | 2026-09-07 |
| D-104 | `memory_map` merges a mapping that continues one the process already holds, instead of adding a region for every call. A call maps at most `MAX_PAGES_PER_CALL` pages and inserted one region each, so a full screen of 1280 by 800 pixels — a thousand pages, which is what the firmware of the reference machine sets — cost sixteen of the sixty-four regions a process may hold, and the display server, which maps the framebuffer, a back buffer, and one surface per client, would have run out of regions at the second client. A region is extended when the new range begins where it ends, names the same memory object, continues its offset, and carries the same permissions; anything else is inserted as before, and nothing about splitting, protecting, or unmapping changes. The alternative, raising `REGIONS_PER_PROCESS`, multiplies a fixed array in every process object and leaves the number of regions proportional to the size of a mapping, which is the property that made the limit reachable. | decided | 2026-09-07 |
| D-105 | The unsafe budget of `user-programs` rises from eighteen to twenty-one sites for the two programs of phase nine. All three are the same operation: turning a mapping this process made into the bytes of it, which is `Mapping::bytes` and is unsafe because nothing but the caller knows the mapping still stands. The display server does it twice — once for the framebuffer, which it maps at its start and holds for as long as it runs, and once per presentation for the surface of the client that presented — and `app-paint` once, for the surface it draws into. No new kind of unsafe enters the crate: D-97 already covers the volatile reads and writes of shared buffers and the calls of the gate, and a program that draws is a program that writes into memory it mapped. | decided | 2026-09-07 |
| D-106 | A server learns that a program has ended through a notification the kernel signals: `process_watch(process, notification, bit)` binds the end of a process to one bit, exactly as `interrupt_bind` binds a line to one. The end of a process is the moment its last thread is gone, whether it exited or was killed, and it is told once. A process that has already ended signals at once, so there is no window in which the end is lost between the ending and the watching. The right the process capability must carry is `INFO`, the least a capability to a process can carry: a watcher learns who ended and can do nothing to the program. This makes the client of a server the one who decides to be watched — it hands the server a duplicate of its own process capability reduced to `INFO`, which is why every program now holds its own process with `DUPLICATE` and `TRANSFER` as well. A watch holds no reference to the notification: one that is destroyed first cannot be signalled, and the watch goes with the process. The alternatives were the client saying goodbye, which a program that crashes never does, and the root task passing the news on, which would make every server's cleanup wait on the one program that starts everything. The display server needs a thread of its own to wait on the notification, because a thread of this kernel waits on exactly one thing and its first thread waits on its endpoint; that thread is the same shape as the interrupt thread of the console driver, and it raises the unsafe budget of `user-programs` from twenty-one to twenty-three — the entry point a thread is started at, and the gate over its IPC buffer. | decided | 2026-09-07 |
| D-107 | Six points of `fs-fat`, which D-53 left to the writing. A name has two rules and not one: `Name::new` takes what a caller writes in either case and keeps it upper-cased, because that is the only case the 8.3 form has room for and because the image writer hands it paths as they stand, while `Name::from_entry` refuses eleven bytes this crate would not have written, a lower-case letter included — the byte that says what a lower-case letter meant belongs to the long file name entries, which this crate skips rather than reads, so such a name in an entry is one whose meaning is not there to read. Every walk of a chain is bounded by the number of clusters the volume has, as the free set of `virtio-queue` bounds its own walk, so a chain that points back into itself is an error and not a hang; four things end a walk and only the end-of-chain marker is success, the other three being a free cluster, which means the table and the entry that named the chain disagree about what is in use, the bad-cluster marker, and a number outside the table. Every write of a table entry goes into every copy of the table within the one call, so the copies cannot drift apart, and the four bits of an entry that are not this crate's are read back and written through untouched. The free count is counted from the table at `mount` rather than read out of the file system information sector, because that sector is a note the last writer left and the table is what the volume is; `flush` writes the note back, and nothing believes it. A file carries a cursor, because a chain has no way back and a write that found its place from the first cluster every time would walk the whole file again for every cluster it added, which makes writing an eighty-megabyte image quadratic. And `remove` refuses a directory rather than freeing its first cluster, because a directory removed without walking into it leaves everything below it in the table with nothing naming it. The xtask keeps what is the image's rather than the format's — the partition as a block device, the format options of a boot volume, and the fixed moment every entry is stamped with — and its own FAT32 code is gone. | decided | 2026-09-07 |
| D-108 | A notification may carry more than one interrupt, each on a bit of its own. `interrupt_bind` refused a notification another interrupt already named, and `Notification::bound_interrupt` held that rule and nothing else: no other code reads the field, and destroying an interrupt does not clear it, so a notification whose interrupt was gone could never be bound again. The field goes with the rule. What made the rule visible is the PS/2 controller, which has one output buffer and two lines: the byte in that buffer belongs to whichever device the status register names, so a driver that waited on two notifications would need two threads reading one buffer, racing each other for the byte in it. The alternative was that shape, and it trades a kernel field nothing depends on for a race in every driver of a controller with more than one line. Nothing in delivery changes: an interrupt already carries the notification and the bit it signals, and one notification already carries a bound interrupt and a process watch at once (D-106), so the exclusive case was the odd one. | decided | 2026-09-08 |
| D-109 | The startup message gains `AuxInterrupt` and `InputServer`. A role that names a handle may appear once — `Startup::read` reports a second as `ReadError::Duplicate`, so that a role given twice is an error and not a field that silently keeps the last of them — and a driver of the PS/2 controller is given two interrupt objects, the keyboard line and the mouse line. `AuxInterrupt` is the second device of a controller the way `Interrupt` is the first, and a driver that serves one line is given none of it. `InputServer` is the badged endpoint of the input server, given to a program that listens exactly as `DisplayServer` is given to one that draws, and for the same reason: a capability found under a name carries no badge, and a server that keeps a ring per client cannot tell two of nobody apart. The alternative was a list, as `Ram` is one, which would make every driver read a list of one and would lose which line is which — the roles say that, an order would not. | decided | 2026-09-08 |
| D-110 | The reference machine's CPU model gains `+rdrand,+rdseed`. `qemu64` carries neither, which was checked against QEMU 11.1 through `query-cpu-model-expansion` rather than assumed, so D-43's `RDSEED` would have failed on the machine this system is developed on. TCG provides both instructions once they are asked for, and `-cpu qemu64,+rdrand,+rdseed,enforce` starts without a warning. The alternative, a software entropy source built from timing jitter, would be a cryptographic construction this project would have to argue for; asking the machine for the instruction it can give is not. | decided | 2026-09-08 |
| D-111 | PCI devices raise interrupts through MSI-X, not through INTx. Which I/O APIC input an INTx pin lands on is written in the `_PRT` object of the ACPI namespace, which is AML, which needs an interpreter this system does not have and will not get. `interrupt_create_msi` on `SystemControl` allocates one vector out of the same space the lines are allocated from and answers with the `Interrupt` handle, the message address and the message data, and the driver writes those into the device's MSI-X table, which lies in the device's own BAR and therefore in the driver's address space. `interrupt_bind` and `interrupt_ack` are unchanged. One consequence is accepted rather than hidden: for a line `interrupt_ack` unmasks at the I/O APIC and the kernel can hold off a device that misbehaves, but the MSI-X mask bit is in memory the kernel does not map, so `interrupt_ack` on such an object clears the outstanding flag and touches no hardware. A device that raises interrupts faster than its driver services them is quieted by its driver — for virtio, through the used ring suppression flag `virtio-queue` implements — and not by the kernel. Masking a vector the kernel does not own would mean the kernel mapping device registers on a driver's behalf, which is a larger hole than the one it closes. The I/O APIC path stays for the ISA lines the system already drives, the UART and the i8042. | decided | 2026-09-08 |
| D-112 | The `MCFG` table is read by the kernel and the PCI bus is walked by userland. The table lies among the other ACPI tables in memory the boot information calls `AcpiReclaim`, which is not MMIO, so `memory_create_device` refuses it and no userland process can reach it; the alternatives were a second ACPI reader in userland with a new way to hand out memory the kernel did not allocate, or this. `kernel-acpi` gains `mcfg.rs` beside `madt.rs` and nothing else about PCI enters the kernel. `system_info` reports the first allocation as four further result words — base address, segment group, first bus, last bus — and four zero words on a machine whose firmware published no `MCFG`, which is the shape the framebuffer description got in Phase 9. The kernel records the ECAM range beside the `MmioReserved` regions so that `memory_create_device` admits the window whether or not the firmware's memory map marked it, because a window the firmware did not mark is a bus nothing can reach. Enumeration, base address register decoding, the capability list and the MSI-X capability are the crate `pci`, a layer-1 logic crate over a `ConfigSpace` trait with a recorded configuration space as its double. Bridges are read and reported and not descended into: the machine of 3.1.1 puts its devices on bus 0, and a walk that follows secondary bus numbers is work with no consumer. | decided | 2026-09-08 |
| D-113 | Memory-mapped registers are reached through a volatile accessor in `user-sys-x86_64` and nowhere else. A device register is not memory that behaves, and a compiler may fold, reorder or drop accesses through the plain byte slice that `server-display` writes its framebuffer through. The accessor offers read and write of `u8`, `u16`, `u32` and `u64` at an offset checked against the length of the region it was made from, one `read_volatile` or `write_volatile` each; an out-of-range offset answers `None` and writes nothing, which is the bound that makes the `unsafe` local and testable on the host. Everything above it keeps `#![forbid(unsafe_code)]`: `pci` implements its `ConfigSpace` over the accessor and `driver-virtio-net` reaches its registers through a trait the accessor implements, as `driver-uart16550` reaches its own registers through its `Registers` trait and `virtio-queue` reaches queue memory through `QueueMemory`. | decided | 2026-09-08 |
| D-114 | `driver-virtio-net` negotiates `VIRTIO_F_VERSION_1` and `VIRTIO_NET_F_MAC` and refuses every other offered feature by name, as D-52 has `virtio-queue` refuse packed rings, indirect descriptors and `EVENT_IDX`. `VIRTIO_NET_F_MRG_RXBUF` is among the refusals, which is what makes one receive buffer hold one whole frame and the header a fixed twelve bytes; `VIRTIO_NET_F_CTRL_VQ` is among them, which is what makes the device two queues and not three; every offload, multiqueue, and the link status are among them too. The refusals are a table pairing the bit number with the name, so an omission reads as a decision and a log line can name it. The crate depends on `pci` and `virtio-queue` and on nothing of the network track: it hands out frames as byte slices and takes them as byte slices, and its MAC address is a `[u8; 6]`, so what a frame means stays `server-net`'s business. | decided | 2026-09-08 |
| D-115 | DMA is one `Ram` memory object per driver, granted whole. The root task creates it, maps it into the driver's process, and installs the handle with `READ \| WRITE \| MAP \| INFO`; `memory_info` answers with the physical start, and because a memory object is one contiguous physical range by construction, an offset into the mapping is the same offset into physical memory and the driver's `QueueMemory` is that arithmetic and nothing more. The region holds both queues' rings and the frame buffers, each at a named constant offset. There is no IOMMU in the first release, so a driver that can write a physical address into a device can make that device write anywhere; what bounds it is that every address the driver programs is derived from `memory_info` of the one object it holds, and the crate that derives them has no other source of an address. | decided | 2026-09-08 |
| D-116 | The socket protocol is one endpoint and one ring per socket, built the way the input protocol of Phase 10 is built: a request is a type with `encode` and `decode` and no system call, and bulk data moves through a ring in a shared memory object with a header of write and read sequence numbers rather than through one IPC call per byte. A client that stops reading fills its ring and the stack stops advancing that connection's window, which is the back pressure TCP already has, so nothing in the server grows without bound. `audhsos-tls` is a client of this protocol and not a peer of it: the TLS transport of Phase 15 moves record bytes between the client library and a socket ring and adds nothing to `user-proto`. | decided | 2026-09-08 |
| D-117 | PCI-SIG does not publish its specifications freely and their terms do not allow a copy in this repository, so the rule of D-59 — every implemented standard kept verbatim beside the code — cannot hold for the crate `pci`. What takes its place is written down rather than left implicit: every constant of the crate names, in its own documentation comment, the document, the revision and the section it comes from, and a number without that provenance is a defect the review treats as one; `docs/pcisig/README.md` records which documents those are and how to obtain them; and the second check is empirical, a configuration space captured from a real `q35` machine kept as a fixture, so that a layout that is wrong fails in the tests whether or not anybody can read the specification. What the virtio specification defines is unaffected and is cited the ordinary way from the OASIS document the repository holds. | superseded by D-124 | 2026-09-08 |
| D-118 | The reference machine gains `-netdev user` with a forwarded port and `-device virtio-net-pci,disable-legacy=on,mq=off`. `disable-legacy=on` makes it a non-transitional virtio 1.0 device, which is the only kind `virtio-queue` and `driver-virtio-net` implement, and its PCI device id is then `0x1041` rather than the transitional `0x1000`; `mq=off` is the default and is written down because the driver depends on it. QEMU's user-mode network is a DHCP server, a gateway and a DNS forwarder inside the process, and `hostfwd` gives a test a loopback port that reaches a listener in the guest, so the end-to-end tests of the network need no host network, no bridge, and no privileges. The device is on the machine from Phase 13, one phase before anything drives it, because what that phase has to show is a bus walk finding a real device with real base address registers and a real MSI-X table. The run without a network drops both lines, and both phases are accepted on it as well, as `-vga none` is the second run Phase 9 is accepted on. | decided | 2026-09-08 |
| D-119 | The last sentence of D-49 is superseded. The virtio-net driver, the network server, the socket protocol and the entropy system call are still integration and still need the kernel, but they no longer stay unscheduled: they are Phases 14 and 15, with Phases 12 and 13 before them for the kernel capabilities and the bus they cannot do without, and [document 13](13-the-network-on-the-machine.md) specifies all four. Everything else D-49 states about the network track — sans-I/O logic crates, no I/O, no allocation, time and randomness as parameters, host-tested, designed in document 12 — is unchanged. What changed is not the design but the finding that the parts a driver needs are enumerable: seven of them, five with nothing to do with networking, and every one of them absent from the running system. | decided | 2026-09-08 |
| D-120 | Userland gets a monotonic clock and a wait with a deadline, because every timer of the network stack is a deadline compared against a `now` the caller passes in and `Stack::poll_at` answers with the instant at which the stack next has work. `clock_now` answers with the microseconds since the kernel started, which is what `Instant::from_micros` takes, computed from the tick count `KernelState` already keeps and the frequency `system_info` already reports; the resolution is therefore the tick and the documentation says so rather than implying a precision the timer does not have. `notification_wait_until` is `notification_wait` with a deadline and answers zero bits when the deadline came first; the existing call is untouched, because a call that works keeps working and a new argument belongs to a new call. In the scheduler a waiting thread carries a deadline field on the state it already has, and the deadlines live in one `IndexList` ordered by instant which the tick handler walks from the front and stops at the first that has not passed, so a tick that wakes nothing costs one comparison and nothing allocates. This is not the tickless timer that stays unscheduled: a deadline between two ticks waits for the later one, which for a stack whose shortest timer is a hundred milliseconds is not worth a design. | decided | 2026-09-08 |
| D-121 | The entropy system call of D-43 is specified as `random_bytes`, which takes no handle and answers with the four words a `ChaChaRng` seed is. The adapter runs `RDSEED` once per word with a bounded number of retries and answers `Unavailable` when a word cannot be filled inside that bound, rather than looping or returning a word the hardware did not give. A process seeds one generator at startup and draws everything else from it; nothing in the system calls `random_bytes` per packet. | decided | 2026-09-08 |
| D-122 | Finite-field Diffie-Hellman is the crate `crypto-dh` at `crates/crypto/dh`, and the secret exponent it needs is a second arithmetic inside `crypto-bignum` rather than a second copy of the first. RFC 9142, table 12, makes `diffie-hellman-group14-sha256` the one key exchange method an SSH implementation MUST have, and that method is the 2048-bit MODP group of RFC 3526, section 3, with SHA-256; nothing in the crypto track could carry it, because every exponentiation there was written for a value that is on the wire — `Modulus::pow` takes sixty-four bits of exponent, `pow_wide` says of itself that nothing in the product calls it, and both are left-to-right square-and-multiply, which branches on every bit of the exponent. `Modulus::pow_secret` is a Montgomery ladder over the whole exponent buffer: eight rounds per byte whatever the bytes are, one squaring and one multiplication in each, and the two working values exchanged by a mask rather than by a branch; `limbs::montgomery_secret` gives it products whose final subtraction happens always and is masked by whether it counts. Both live beside the public pair because `Modulus` derives `n0inv` and `R2` once, and a second copy of that derivation is a second thing to get wrong. What separates the two arithmetics is the name of the function and nothing else: a caller that hands a secret to `pow` is wrong and no check in the crate will say so, which is why the README states the boundary, and why D-39's requirement that each crate document which functions see secrets is met there rather than by a type. D-39's other clause is untouched — the asymmetric signature surface is still verification only, and a key agreement is neither signing nor verification, which is the distinction D-56 drew when it named the secret scalar of `x25519`. The group and the two operations over it are a crate of their own rather than a module of `crypto-bignum`, which is arithmetic and holds nothing about what a value means, and rather than one of `crypto-ec`, because a finite field is not an elliptic curve; `crypto-dh` sits above `crypto-bignum` and `crypto-ct` where `crypto-ec` sits. The check on a public value is the open interval `1 < e < p-1` of RFC 8268, section 4, and not the closed one that RFC 4253, section 8, prints: that document is in `docs/rfc` under D-59 and is wrong in this one point, RFC 8268 amends it in as many words, and the closed form admits the values that force the exchange into a subgroup of one or two elements. It is applied to the value this side sends as well as to the one it receives, because the amended sentence forbids both. What stays public is what a length is — the width of the modulus and the length of the exponent buffer, both of which set loop counts — and the crate says so rather than leaving a reader to find it. Two things this does not settle. The SSH method is not here: the two messages, the `mpint` encoding of RFC 4251, section 5, the exchange hash and the key derivation of RFC 4253, sections 8 and 7.2, and the negotiation all sit above this crate. And the crate is deliberately in no track and no plan document. Track C is cryptography for the TLS client, and this serves no client that exists; a document of its own would be a plan for Secure Shell, which has not been decided. It stands in the crate catalog of document 5 and in the changelog, where a thing that exists belongs, and it enters a plan when there is a decision to put it in. | decided | 2026-09-09 |
| D-123 | Secure Shell enters this project as a client, and [document 14](14-secure-shell-as-a-client.md) is its design. A client and not a server: a server is section 6 of RFC 4254 and presumes a process server that can start a program on request, a file system, user accounts, and a pseudo-terminal, and this system has none of the four, while a client presumes a TCP connection and a program that wants one and both are Phase 14. It is track S of the roadmap, steps S1 to S8, built between phases as the whole of document 11 was, because S1 to S7 need nothing from another track; S8 is the integration and waits on the network on the machine. This picks up the last sentence of D-122: `crypto-dh` stood in no plan because there was none, and it is step S2 together with `curve25519-sha256`, which makes the client meet the one MUST of RFC 9142's table 12 and still prefer the curve. The algorithm set is the one of section 14.5 and it needs no cryptographic primitive that does not already exist, which is what makes this protocol work and not cryptography: `curve25519-sha256` and `diffie-hellman-group14-sha256`, `ssh-ed25519` for the host key and for `publickey` authentication, `chacha20-poly1305@openssh.com` as the cipher, and no compression. Three algorithms RFC 4253 makes REQUIRED — `ssh-dss`, `3des-cbc`, `hmac-sha1` — are refused, and that is a departure and not an omission: RFC 9142 withdrew the two SHA-1 key exchange methods of the same document but not these three, so the departure is this project's and is written out with the rest of the refusals in 14.5. What measures it is that no document publishes a complete SSH handshake with the keys that made it, so there is no RFC 8448 to replay here and the only evidence from outside is a handshake against a live OpenSSH; that interop test is part of the definition of done of step S8 rather than an extra. Three questions this does not settle, each a precondition of the one step it belongs to and none of them blocking the steps before it: where the specification of `chacha20-poly1305@openssh.com` is kept, since it is a file of the OpenSSH source and not a standard, which is either a `docs/openssh/` under the rule D-100 used for a second body or the regime D-117 was forced into (S3); how a host key is trusted on a system with no writable storage, where the client's interface is settled — it is given the rule at construction and never decides on its own — and the rule is not (S4); and whether the client's private key comes out of the boot archive of D-27 or is generated per boot (S5). | decided | 2026-09-09 |
| D-124 | What decides whether a reference document is kept in this repository is whether it can be obtained, not whether its licence permits the copy. Three cases, and every one of them keeps the requirement of D-40 that a transcribed constant name its document, revision and section. First, documents whose licence permits redistribution: the RFCs under D-59, OASIS under D-100, and `docs/w3c/` and `docs/ecma/`. They are kept verbatim and there is nothing further to state. Second, documents that the publishing body serves to anyone at no charge but does not licence for redistribution: ITU-T T.81, T.871 and T.84 under `docs/itu/`, which print that no part may be reproduced without permission in writing from the ITU, and CIPA DC-008 under `docs/cipa/`, which reserves all rights. They are kept verbatim as well. The copy is what D-59 is for — a clause readable without a network, at wording that cannot change under what cites it — and no licence-respecting arrangement delivers that for these standards. The obligations that follow are stated rather than assumed: the files are unmodified so that their notices travel inside them, the README of each such directory quotes the restriction it stands against, and a copy is removed if the body objects. Third, documents that cannot be obtained at all: the PCI Express Base Specification and the PCI Firmware Specification, which PCI-SIG releases to members or against payment. No copy exists to keep, so what replaces it is what D-117 established and this decision carries forward unchanged — every constant of the crate `pci` names its document, revision and section in its own documentation comment, a number without that provenance is a defect the review treats as one, and the second check is empirical: a configuration space captured from a real `q35` machine is a fixture, so a wrong layout fails in the tests whether or not anybody can read the specification. The boundary between the second and third case is obtaining and not price: PCI-SIG offers one specification free of charge, the PCI Code and ID Assignment Specification, but it is released through a form that requires a name, a company and an email address, and it is not among the documents the crate cites. PCI-SIG also forbids the use of its specifications for building or training commercially available artificial-intelligence systems without its prior written consent, which is recorded in `docs/pcisig/README.md` because it constrains use and not only the copy. | decided | 2026-09-09 |
| D-125 | The unsafe budget of `user-programs` rises from 31 to 33, for the two sites `app-canvas` costs. A program of the userland reaches memory a server mapped for it through `Mapping`, whose `bytes` and `ring` are unsafe because only the caller knows the mapping still stands; every program that draws pays one and every program that listens pays the other. The canvas is the first that does both, so it pays both, and neither is a site the logic crate could take over: `crates/user/apps/canvas` forbids `unsafe` and takes the surface as a parameter. The alternative, a safe wrapper that revalidates the mapping on every access, would put a check in the path of every pixel and could not answer the question it was checking, which is whether another process has unmapped it. | decided | 2026-09-10 |
| D-126 | The canvas presents nothing until an event reaches it. Two programs of the boot archive create a surface the size of the screen — `app-paint`, whose picture the end-to-end run checks while the machine still runs, and `app-canvas` — and the screen holds whatever was presented last. Starting one before the other decides it by a race, and the run would check whichever won. Waiting for an event decides it by what happened instead: until the pointer moves or a key is pressed, the screen holds the picture the painting program put there, and from the first event on it holds the canvas. In an interactive run that is the first movement of the mouse, which is what anyone does with a canvas; in the end-to-end run it is the first thing injected after the picture has been checked. | decided | 2026-09-10 |
| D-127 | A console read that finds nothing waits. `Request::Read` answered what the ring held and nothing more, so a program that wanted a line asked again, and asking again is a loop: this system has no timer a program can ask to be woken by, so a thread that polls is always runnable and the kernel's idle thread never reaches `hlt`. `app-hello` did exactly that and held the machine at full host load for as long as nobody typed, on an otherwise empty system. The driver now keeps the reply object of a read it cannot serve and answers it when the next byte arrives, which is what the reply object is for and costs the driver one slot. It holds one such read at a time; a second one is answered at once with what has arrived, which is nothing, so no client can be held by another client's silence. A client that ends while its read is held leaves a reply object whose caller is gone: the kernel refuses the answer and does not consume the handle, so the driver closes it itself. | decided | 2026-09-10 |
| D-128 | A program that watches the pointer says a move only when no later event is already waiting for it. A console line of this system leaves the driver byte by byte and every byte is two system calls — one to see that the transmitter is free, one to hand it the byte — so the forty-nine characters `app-input` and `app-canvas` write for one pointer packet cost ninety-eight of the hundred and twenty-one system calls that packet costs altogether. A mouse of this machine sends a hundred packets a second and the machine served sixty, so the backlog grew for as long as the pointer moved: after five seconds of movement the cursor was one and a quarter seconds behind it. Both programs now drop the line of a motion event that another event of the same buttons already follows, because the one behind it says where the pointer is now and says it sooner. Nothing else is thinned: every key, every button, every character, and the last event of every wake-up are said. A machine that keeps up empties the ring on every wake-up and therefore says everything, which is what the end-to-end run sees, because it injects one event and waits for its line. | decided | 2026-09-10 |
| D-129 | A console line leaves the driver in bursts, and a burst is one system call. Two things made it forty-nine characters at two calls a character. The driver waited for the transmitter before every byte, though SLLS597E page 41 says of the holding register that it "is actually a 16-byte FIFO" and page 37 says of the status bit that "In the FIFO mode, THRE is set when the transmit FIFO is empty": one THRE therefore licenses the sixteen characters page 34 spends, and the driver now spends them. And every register access of `server-console` is a system call, so a run written a byte at a time cost one call a byte whatever the wait did. `ioport_write_string` takes the run in the message area and the count in an argument, checks the port against the range capability at width one, and writes every byte to that port; the payload words are little-endian, so the area read as bytes is the run in order and the kernel copies nothing. Together a twenty-five character line costs four calls where it cost fifty. Whether the part has the FIFO is asked and not assumed: `init` reads the identification register after enabling it, because page 33 says those two bits "are always cleared in TL16C450 mode" and are set when the request took, and a part that answers no gets one byte a THRE. The bound matters beyond tidiness — QEMU's model overwrites the oldest byte of a full transmit FIFO and says nothing — so sixteen is a correctness bound and not a tuning knob. What this does not buy is time on real hardware: THRE returns only when the whole FIFO has drained, so the line takes as long as it always did and it is the calls, not the wait, that disappear. | decided | 2026-09-10 |
| D-130 | The scheduler counts the threads that have ended, so the sweep can answer without looking. `reaper::reap` runs after every system call and after every context switch, and its search was a walk of the thread pool — O(N) in the pool's capacity, which is two hundred fifty-six, paid whether or not anything had ended. Under a moving pointer that walk was some twenty-nine per cent of what the machine did. It is now O(1) when nothing has ended, which is every call on a running machine. The count is not a second record of what is left to clear away — `ThreadState::Exited` is still that — but the answer to whether there is anything to look for, which is a weaker thing and has two writers: `Scheduler::apply`, the one place a thread's state changes and therefore the one place it can become `Exited`, and `reaper::clear`. A count that stood too high would cost a walk that finds nothing; one that stood too low would leak a thread, and that is what the two writers are for. A thread that has ended but is still the one the processor stands on keeps the count raised and is not cleared, so the kernel comes back for it after the switch, which is what it did before. Beside it, `Pool::iter` now stops at the high-water mark: a slot comes either from the free list or from the mark itself, so no slot above it was ever occupied. That takes every walk of every pool in the kernel from O(N) in the capacity to O(H) in the number of objects ever allocated, which on this machine is a few dozen against two hundred fifty-six. The two are independent — the count keeps the walk off the hot path, the mark bounds what a walk costs when one happens. | decided | 2026-09-10 |
| D-131 | The deadline list of the scheduler is threaded through the thread entries and not held as an `IndexList` over them. 13.3 and 10.12.2 name `IndexList` for it, and `audhsos-collections` gains `insert_after` in this phase because that list needs it. What the list would also need is the slice of `Link` it runs over, and there is nowhere to put it. A `Link` in no list is three words of `u32::MAX`, so an array of them in the `Scheduler` is not all zeros; the `Scheduler` is a field of the `Machine` that `kernel_core::machine::MACHINE` holds in one `static`, and one non-zero byte moves that whole symbol — the object pools with it, which are megabytes — out of the `.bss` of the kernel image and into its `.data` (D-66). The pool cannot hand out a contiguous slice of links either: a thread lives inside an `Option<Occupant<Thread>>` of a pool slot. So the list is what the run queues already are — a head and a tail in the `Scheduler`, a `Links` and a deadline word in the `Thread` — which is zero-initialized, needs no second array, and reads beside the queues it sits next to. `IndexList::insert_after` is built and tested all the same: it is a deliverable of this phase, it is the operation the type was missing, and the list of Phase 13's bus enumeration is the next caller in sight. | decided | 2026-09-11 |
| D-132 | The unsafe budget of `kernel-hal-x86_64` rises from 145 to 149 and its `asm!` budget from 28 to 30, for the two instructions `random_bytes` costs. `RDSEED` is one site: the instruction reports its success in the carry flag and leaves the destination register undefined when it fails, so the flag is read back in the same block that ran it. `CPUID` is the other: a value of `HardwareRandom` says the processor has the instruction, and asking `CPUID` is what makes that true rather than assumed — a machine without `RDSEED` raises an invalid opcode, and a kernel that discovered it that way would have discovered it in an interrupt handler. `CPUID` writes `EBX`, which the compiler reserves, so the one site saves it, runs the instruction, takes the answer out, and puts it back; that accounts for the fourth `unsafe`, which is the caller in `random.rs` that draws a word inside its retry bound. | decided | 2026-09-11 |
| D-133 | The timer takes the processor in the shipping kernel, and a tick that finds a cell of the kernel held switches nobody. Time-slice preemption was built and proved in Phase 5 and wired into the test harness only: `Scheduler::tick` charges the running thread its tick and asks for a switch when the slice runs out, and `crates/kernel/bin/src/main.rs` never called it, so the one kernel that ships handed the processor over only when a thread asked it to. Every program of the boot archive blocks on IPC or a notification, which is why nothing had shown it. The tick handler now charges the slice, and `task::run` gains the guard `run_threads` of the harness has carried since the `ipc` image wedged on it about one run in six: a switch out of a thread that holds the memory, the machine, or the console leaves that cell borrowed by a thread that is no longer running, and nothing gets it back. The console is the one this phase found. `task::start` prints `root task at ...` after the root task is ready, so a tick between the borrow of the console and the end of the line switched into the root task and left the console held for good — the machine ran on and every line it had left to say went nowhere, which from outside is indistinguishable from a machine that stopped. The harness has no third cell to check — its `say!` builds a console value rather than borrowing one — which is why the two it checks were enough there and are not here. | decided | 2026-09-11 |
| D-134 | The cipher `chacha20-poly1305@openssh.com` is kept in `docs/openssh/`, and two documents are kept for it because one replaced the other. This answers the question 14.13 held against step S3, in the first of the two shapes D-123 offered: a directory of its own under the rule D-100 used for OASIS, not the regime of D-124's third case, because both documents are served freely and both permit the copy. What is kept is the OpenSSH file `PROTOCOL.chacha20poly1305` at revision 1.5, which is the last one: OpenSSH removed it on 2025-08-05 because the extensions it documented now have IETF documents that are more complete, so the file is taken at the parent of that commit and the README names both commits. Beside it is `draft-ietf-sshm-chacha20-poly1305-04`, which is where section 1.7 of the `PROTOCOL` file now points, and which the crate is written against. That it is an Internet-Draft is stated and not hidden: a draft asks not to be cited except as work in progress, and what makes it usable here is that a numbered revision is archived and cannot be replaced, which is the same property D-59 wants of an RFC. Its number is in the file name so that a citation says which text it read, and when the draft becomes an RFC that RFC goes to `docs/rfc/` and this copy goes with the citations that point at it. The OpenSSH file stays either way, because the algorithm name carries a domain and RFC 4251, section 6, makes the owner of that domain the authority on what the name means. One thing the draft gives that the file does not: appendix A is a worked example — one packet, its padding, its sequence number, the 64 bytes of key material, and the bytes on the wire — which is the only published vector this track has for a whole packet, and D-40 has it transcribed rather than recomputed. | decided | 2026-09-11 |
| D-135 | Ed25519 signing is product surface, and the arithmetic under it is constant-time. D-39 made the asymmetric product surface verification only because the client of document 11 checks signatures and makes none; the Secure Shell client of D-123 authenticates with a key of its own, so step S5 signs, and 14.13 held this open against that step. `ed25519::sign` and `ed25519::public_key` therefore lose their `#[cfg]` and compile into a product build. The feature `test-signing` stays and keeps ECDSA signing over P-256 and P-384, because nothing outside the test certificate builder of 11.9 creates those signatures. Removing the gate alone would have shipped the timing of a long-term private key. `Point::mul` adds where a bit of its scalar is set, `Scalar::mul` does the same, and `subtract_order` stopped as soon as the value had fallen below the order: all three are variable-time on the secret scalar, on the nonce of the signature, and on every intermediate of their reduction, and the module said so in as many words — the point arithmetic of that file was written for a verifier, where every value is on the wire. What ships instead is the shape D-122 gave `pow_secret`. `Point::mul_secret` doubles and adds at all 256 positions and keeps the sum behind a mask of `crypto-ct`; `Scalar::mul_secret` does the same in the scalar field; and `subtract_order`, which every addition and every doubling of a scalar ends with, runs both of its rounds always and selects its difference with a mask, so the reduction of a nonce costs what the reduction of any other value costs. The public pair stays where a verifier needs it: `Point::mul` and `Scalar::mul` are shorter, about a quarter quicker, and correct for the public scalars verification reduces. What separates the two is the name and nothing else — no check in the crate catches a caller that hands a secret to the public one — which is why the boundary is documented in the module rather than enforced by a type, the same answer D-122 gave for the same question, and which is how D-39's requirement that each crate say which functions see secrets is met here. The cost is the addition each bit used to skip: twice the additions, about a third more work in all, and no change to the order of growth, signing being O(1) in the message beyond its two SHA-512 passes. The same reason took the short-circuit out of `sub_limbs`, where `||` over two borrows was a branch on a secret the moment the reduction above it stopped being one. Three things this does not change. The nonce stays the deterministic one of RFC 8032, section 5.1.6, so signing takes no randomness and no generator. `crypto-rsa` signing stays behind `test-signing`. And the constant-time claim is the one 11.4 makes for the whole track — no branch and no index on a secret value, which is a property of the source and can be checked by reading it — and not a claim about what a compiler emits. | decided | 2026-09-12 |
| D-136 | What the system writes, it writes to a second disk, and the boot volume stays what the firmware and the loader read. The image of D-09 is one FAT32 partition holding the loader, the kernel and the boot image, and a file system written by the system on that volume is the shape the machine already has room for — the EFI system partition has no rule against it, and 3.1.4 puts every file the machine needs in it already. What it has no room for is a mistake: FAT32 has no journal, so a write torn by a crash loses a chain, and on that volume a lost chain is a machine that no longer boots. A run therefore gets a second `virtio-blk-pci` disk of its own, blank, with no partition table, sixty-four mebibytes and sparse, which is the 65525 clusters of one sector FAT32 needs with the two tables above them; the system formats it over the whole disk, so nothing of the partition table has to be read before a file system can be. One disk per run name, because two machines writing one file tear it and QEMU's image lock refuses the second one anyway, and the same path across runs of that name, because a test that boots twice to see what survived is the test this disk exists for. No run carries the disk unless it asks: a device that neither a driver nor a bus walk looks at is one more thing for an unrelated test to trip over, which is what 3.1.1 already says of the network device in the phase before its driver. The alternatives are named rather than left implied. Rebuilding the image before every run is what the runner does today and it is why nothing has been corrupted yet, but it makes a persistence test impossible: the second boot would read the bytes the writer put there, not the bytes the first boot wrote. `snapshot=on` has the same effect for the same reason, and costs an interactive run whatever it wrote. Neither leaves the boot volume safer than a separate disk does. | decided | 2026-09-12 |
| D-137 | The machine has a wall clock, and the loader is what reads it. `EFI_RUNTIME_SERVICES.GetTime` is called once, before `ExitBootServices`, and the moment travels to the kernel as data in the boot information; after the exit a runtime service needs the virtual address map of `SetVirtualAddressMap`, which this system never sets, so the reading has to happen while the firmware is still in hand. D-46 is untouched in what it was for: no logic crate reads a clock, time still enters every interface as a parameter, and the one place that touches the device is an adapter, which is where a device read belongs. Six things follow and are decided here. The boot information goes to version 2, its fixed part from 136 bytes to 144: `reserved` becomes `wall_clock`, the code of a `WallClockSource`, and `boot_unix_seconds` is appended; version 1 is refused rather than accepted with a zero clock, because the loader and the kernel ship together and there is no machine on which the two versions meet. The seconds are a plain count and not a `UnixTime`, so that `audhsos-abi`, which everything depends on, keeps depending on nothing — the arrangement D-120 already made for the deadlines of the scheduler. The conversion from `EFI_TIME` lives in `audhsos-uefi` and not in the loader, because a calendar conversion is arithmetic that should be host-tested under a coverage gate rather than written beside an `unsafe` call; what the loader gains is the call and the null check, two `unsafe` sites, and its budget rises from 39 to 41 as `user-test-programs` rises from 98 to 105 for the image that exercises the result. The source travels beside the seconds and is not dropped: UEFI 2.11, section 8.3.1 has `EFI_UNSPECIFIED_TIMEZONE` mean a local time whose zone the firmware does not name, which is what OVMF reports, so a value read as universal time may be wrong by a zone and the caller is told which of the two it holds. `clock_wall` is call 51 and answers two words, the microseconds since 1970-01-01T00:00:00Z and the source code; it is a call of its own and not a word of `system_info` because all thirty of that result's words are taken, and the widest result of the interface should not grow for this. And a machine whose firmware reported no clock, or reported one this system refused, is answered `Unavailable` rather than a guess, so that a certificate is never judged against a made-up moment. There is no `clock_set`: nothing would call it, and a call arrives with its user, which is the rule D-120 stated when it left `notification_wait` alone. What this does not fix is drift. The wall clock is the boot moment plus the microseconds `clock_now` answers, and that count comes from an APIC timer calibrated against the interval timer once at boot with nothing correcting it afterwards; for a certificate window, which is days wide, that is enough, and the documentation says so rather than implying a precision the machine does not have. The UEFI specification is kept in `docs/uefi/` under the second case of D-124, beside the ACPI specification, which the same body publishes and which `kernel-acpi` has been citing as "the specification" for want of a copy; the README of that directory records one thing the others have not had to, that the Forum's site answers an automated request with a bot check, so both files were fetched by hand and the checksum is what stands in for the fetch. | decided | 2026-09-12 |
| D-138 | The GUID partition table moves out of the xtask into `fs-gpt`, a logic crate over the block device trait `fs-fat` defines, and the image writer becomes a user of it as D-53 made it a user of `fs-fat`. The reason is the one D-53 gave: the file system server that reads the boot volume has to find the partition first, and a second implementation of the header would be a second place for an offset to be wrong. The crate reads as well as writes, so the offsets exist once for both directions, and the xtask keeps what is the image's rather than the format's — where the partition begins, what it is called, the identifiers that are fixed so that two runs produce the same bytes — while its own `gpt.rs` and `crc32.rs` are gone. The bytes of the image did not change, which the tests of 6.6.15 hold and a comparison of the table before and against after confirmed. Three things the writing settled. The crate depends on `fs-fat`, which makes it the one layer-1 crate that depends on another: the trait is the contract a device offers and it is defined there, and a crate of its own for that contract would move a public type out of `fs-fat` and the device half of its error with it, for one trait and two methods. Reading checks everything section 5.3.2 asks for — the signature, the header checksum, that the header lies in the block it names, and the array checksum — and one thing more, the protective record, because a device whose first block is a legacy table is one whose partitions are described there and a table behind it is what an older tool left standing; that check is endorsed by the same section and is what makes a disk that is not GPT fail early rather than oddly. And the order of a write is the protective record, then both arrays, then the backup header, then the primary: the specification asks for the backup before the primary, which is the recovery it describes, and the record goes first because a reader that does not find one reads no table at all, so a write cut short between them would leave a whole table nothing would look at. Entry sizes are the format's 128 times a power of two narrowed to those that also divide a block, so that no entry straddles two of them and a walk needs one block at a time; every writer uses 128. | decided | 2026-09-12 |
| D-139 | `driver-virtio-blk` is a logic crate over a register trait of its own, and it depends on `virtio-queue` and on nothing else. 13.9 sketches the shape for `driver-virtio-net` — a `Registers` trait naming one of the four structures the virtio capabilities located, an offset and a width, so that the crate computes no address and the adapter interprets no field — and this crate is that shape for the block device of virtio 5.2. Where it departs from the sketch is the dependency on `pci`: the network driver may need it, this one parses no capability, and a dependency for nothing is worse than the symmetry is worth. What the two do share is the device identifier, and `BLOCK_DEVICE` therefore stands in `pci::virtio` beside `NETWORK_DEVICE`, because what reads a device identifier is a bus walk. Three request types are implemented — read, write and flush — and the other five are named refusals: each needs a feature this driver does not take or a framing rule of its own, and a file system server needs none of them. Three features are taken and every other bit of 5.2.3 is named and turned down. `VIRTIO_BLK_F_FLUSH` is taken because a system that writes a file system and cannot ask for the write to reach the disk is telling its caller something it does not know; `VIRTIO_BLK_F_RO` because 5.2.6.1 asks a driver to accept it if offered, and a write refused before it is sent says more than a status byte does after; `VIRTIO_F_VERSION_1` because nothing here reads a legacy device. `VIRTIO_BLK_F_MQ` is refused, which is what leaves 5.2.2 with one request queue and this crate with one to drive, and `VIRTIO_BLK_F_SEG_MAX` and `VIRTIO_BLK_F_SIZE_MAX` are refused because a chain of one data segment needs neither. Every rule of 5.2.6.1 the driver can check is checked at the call — whole sectors, a flush without data and of sector zero, a read or a write with data, nothing past the last sector, no write to a read-only device — because the alternative is a request sent, answered with a status byte, and lost. Two rules of the transport are kept that are easy to miss: the feature windows are read at step 4 of 3.1.1 and not before, since reading them writes a selector and the driver writes nothing before `DRIVER`; and the capacity is read between two reads of the configuration generation, which 2.5.1 requires of any field wider than thirty-two bits, with a bound on the attempts so that a device whose configuration will not stand still is reported rather than spun on. Waiting stays the caller's, as D-52 left it for `virtio-queue`: `reset` writes the zero, `is_reset` answers whether the device has finished, and `initialize` refuses a status that is not zero rather than waiting for one. | decided | 2026-09-12 |
| D-140 | A database engine kept to be read is read by asking it questions, and `norec` is the tool that asks. `research/` holds SQLite because a file system server and the on-disk formats after it are being written here and a mature implementation answers questions no document does; what a reader cannot get from the source is what the program actually does, and a fuzzer is the cheapest way to ask. The technique is NoREC, from Rigger and Su, ESEC/FSE 2020: the same predicate goes once into a `WHERE` clause, which is where an optimizer works, and once onto every row of the same `FROM` clause, where there is nothing to optimize, and the two must count the same rows. It needs no reference implementation, no known-good answer and no model of SQL semantics, which is why it is this technique and not differential testing against a second engine, and it is what makes the tool a few hundred lines rather than a project. The paper is kept in `docs/acm/` under the second case of D-124, the authors' accepted version because the Digital Library answers an automated request with `403`, and the crate cites its sections rather than restating them. The crate is a host tool under `crates/tools/`, kind `Host`, depending on no workspace crate and on nothing outside the workspace: it drives the `sqlite3` shell as a child process over its standard input, one process per case, an in-memory database and `-init /dev/null` so that no machine's `.sqliterc` reaches a run. Four things the generator settles. Section 3.4 of the paper excludes a subquery, a function of the clock or of a random source, and `DISTINCT`, aggregates and window functions, because each is allowed to answer the two forms differently; none is generated, and a test over hundreds of seeds holds that. Section 3.3 counts the optimized query either by its rows or by `COUNT(*)` and alternates between them, which is what this does, the plain row set being the query an optimizer is handed plainly and the count being the cheaper of the two. A `UNIQUE` index is created only where the rows are provably distinct as SQLite compares them — one plain column, no collation, and no value an affinity would convert — because a statement that fails costs the whole case and the conversion rules are the engine's to decide, not this generator's. And what it generates on purpose is what an optimizer has something to do with: indexes, partial indexes, indexes over expressions, `ANALYZE`, and a second table joined by a comma, a `JOIN` or a `LEFT JOIN`, whose clause section 3.2 copies into both queries unchanged. A finding is shrunk before it is written — rows, indexes, joins, `ANALYZE` and the predicate, one engine run per candidate within a budget, every candidate strictly smaller so the search terminates — and what is written is a file the shell reproduces it from, named after the seed the case came from, the seed being the first seed plus the case's number so that one case can be run again alone. The tool is not a step of `check` and finds bugs in software that is not this project's: it is run by hand, `sh tools/xtask.sh norec`, and its exit status says whether anything was found. That it works is not taken on faith. SQLite 3.28.0, the version the paper was evaluated against, answers three of twenty thousand cases two ways, each of them a `LEFT JOIN` whose unmatched row the `WHERE` clause drops and the sum keeps; 3.53.4 answers all twenty thousand consistently. | decided | 2026-09-13 |
| D-141 | SQLite is ported to this repository's Rust, and the port is judged against the C library rather than against a reading of the documentation. Document 16 specifies it: the same file format both ways, the same rows for a statement both engines accept, the same error code for one both refuse, and no claim about plans, file sizes or speed. The reason it belongs here is the one D-53 gave for `fs-fat` one layer down — what a program asks for after a file is a table, and a format implemented twice is a format wrong in two places — and the reason it is possible now is the tooling: `sh tools/sqlite.sh` builds the reference implementation, and `norec` already drives it. The crate is `db-sqlite` under `crates/db/`, a logic crate: no dependency, `forbid(unsafe_code)`, `no_std` outside its tests, and no allocation in the layer that reads, which is what lets a database be read where it lies rather than copied. The documents it is written against are kept in `docs/sqlite/` under the first case of D-124, SQLite's code and documentation being public domain, and every structure names the section it came from. Four things this decides about the testing, because a port is only as good as what it is checked against. Fixtures written by the `sqlite3` shell are the format as it is, and are committed beside the tests that read them, each with the statement that produced it. Differential execution against that shell is what judges semantics, and the harness for it is `norec`'s with a second engine. SQLite's own suite is TCL over a fixture that links the library, so it arrives when there is an engine to link — an adapter crate, which is where a C ABI would live — and until then its `.test` files are read as specifications and the pure-SQL ones run through the differential harness. And the fuzz targets of this repository's own engine cover every parser the port has. Two standards are set higher than the repository's floor. Every test runs across the configuration matrix of 15.6 — three encodings, four page sizes, reserved space, six journal modes, auto-vacuum, four schema formats — rather than under whichever configuration a fixture happened to have. And the coverage goal is the one SQLite holds itself to and `docs/sqlite/testing.html` describes: every branch both ways and MC/DC over every compound condition, measured by the same instrumentation the `coverage` step already uses, with unreachable defensive code treated as a defect rather than as a line to exempt. Step Q1, the format read-only, is done; 15.4 has the nine steps after it. | decided | 2026-09-13 |
| D-142 | The decimal spelling of a double is ported from `sqlite3FpDecode` in `src/util.c` rather than taken from the language's own shortest-round-trip rendering. The two disagree: `1.0/3` is `0.3333333333333333` under the shortest rule and `0.33333333333333332` under SQLite's, and a value that prints differently is a query that answers differently, which D-141 says the port may not do. So the tables `tool/mkfptab.c --round` generated are copied, the 128-bit multiply that extracts eighteen significant digits is written out, and so is the rule that tries a shorter rendering where the shorter one reads back as the same double — the rule that keeps `49.47` from printing as `49.469999999999999`. Every conversion is a fixed number of steps, O(1), and allocates only its digits. The second half of the decision is how it is checked. SQL cannot ask what text a particular double prints as, because SQL has no way to write a double by its bits, so the oracle is a program: `tools/sqlite-oracle.c` links the amalgamation, writes the cases and answers them, and `sh tools/sqlite-fixtures.sh` builds and runs it. Its answers are committed as `fp.corpus` and `fp.golden`, which is rule 8 of 15.3 — the oracle is recorded, not trusted — and the same program is where the next question put to the C library goes. | decided | 2026-09-13 |
| D-143 | An expression is answered by walking its tree, and what each operator does is taken from the opcode of `src/vdbe.c` it compiles into rather than from `docs/sqlite/datatype3.html`. The document says which affinity is applied to which side of a comparison; it does not say that only a value that is nothing but text is made a number, that the attempt to make it an integer is made when storing and not when comparing, or that `'abc' + 1` is `1` because a string with no number in it reads as the integer zero. Those are `applyAffinity`, `applyNumericAffinity` and `computeNumericType`, and a port that reads the prose and writes its own conversions gets a different engine. So `value.rs` is those routines and `sqlite3MemCompare` beside them, and `eval.rs` is the opcodes: integer arithmetic that falls back to doubles on overflow, division and remainder that answer nothing rather than failing, a NaN that is nothing, shifts that turn around for a negative count, and the sign before a literal folded into the number because that is the only way the smallest integer is written. The walk is O(n) in the nodes and is bounded by its own depth, because the tree may come from outside the parser. How it is judged follows D-141: SQL can ask this one directly, so the oracle is the shell's own answers to seven thousand expressions, compared as a class and a value rather than as text. What is not written yet — columns, functions, the pattern operators, and the statements an expression may hold — refuses by name, and the count of what SQLite answers and this does not is a number the test holds down. | decided | 2026-09-13 |
| D-144 | The scalar functions are ported one at a time from `src/func.c`, and the ones that cannot be are refused by name rather than approximated. What the documentation says about them is not enough to write them from: it says `substr` counts characters, not that it counts bytes when the first argument is a blob, that a start before the first character has four rules that move the length as well, or that `substr(x'',1)` answers nothing because an empty blob has no pointer to read from. The same holds for `length` against `octet_length`, for `instr` comparing blobs as blobs and everything else as text, for `replace` answering before it reads its third argument when the second is empty, and for `min` taking the later of two that compare equal while `max` keeps the earlier. So each is the routine it is named after. `LIKE` and `GLOB` are `patternCompare`, with the pattern length limit that comes with it, because that limit is what bounds how deep the comparison recurses — a port that dropped it would need a depth bound of its own, and a depth bound answers differently. Four kinds are left out and refuse: the ones that read a clock or a random source, the ones that answer something about the connection, `printf` with its whole format language, and the mathematical ones, which want a library this repository does not have yet. `zeroblob` is left out for a reason of its own: SQLite builds it lazily and a port that allocates would let one expression ask for a gigabyte. | decided | 2026-09-13 |
| D-145 | The schema is read by parsing the `CREATE` text of `sqlite_schema` rather than by reading the structures a writer would have left behind, because there are none: a SQLite file keeps its schema as the statement that made it, and every column name, declared type, collation and key in the database exists only in that text. So the parser grows the two definitions — `CREATE TABLE` and `CREATE INDEX` — with every constraint the grammar allows, including the ones nothing reads yet, because a definition that is refused is a database that cannot be opened. Two things this decides about how it is judged. What a parser alone decides is the syntax errors, and SQLite says which those are: a refusal the grammar itself made begins `near "X": syntax error`, while a rule one of the grammar's actions applies — a column named twice, `AUTOINCREMENT` on a column that is not an integer key, an unknown table option — says something else. The test holds the parser to the first kind exactly and counts the second, which the schema layer will take over. And `nm` is wider than a name in an expression: `idj ::= ID | INDEXED | JOIN_KW`, so `CREATE TABLE full(x)` is a table and `MATCH FULL` is a foreign key clause, while an alias written without `AS` is still only an identifier or a string — the two readings are kept apart rather than merged. | decided | 2026-09-13 |
| D-146 | The first engine that answers a statement walks the table's tree once and keeps nothing: no plan, no index, no compilation. Rows before plans is the order document 16 sets, and the reason is that a wrong row is a bug while a slow scan is a number. What the walk costs is O(n) in the rows of the table, and O(n log n) where an `ORDER BY` sorts them. Three things this decides. The engine reads one table at a time and refuses everything else by name — a join, an aggregate, a grouping, a compound, `VALUES`, a table whose rows live in the key's own tree, a column computed and not stored, and text that is not UTF-8 — so that what it answers is either right or refused, never near. The names of the answered columns are part of the answer and are compared as closely as the values, because SQLite's rule for them is not obvious and a port that guesses it is a port whose output does not match: an alias wins, a bare column answers under its own name, a rowid answers under the name of the column it is an alias for, and everything else answers under the text it was written as, spaces and quotes included. And every length a file gives is bounded before it is believed: the first thing the fuzzer found when it began opening arbitrary files as databases was a row claiming forty-five thousand million million bytes, which the reader now refuses before it asks for the room. | decided | 2026-09-13 |
| D-147 | Text is held as UTF-8 inside the engine whatever the file keeps it as, and the four places that choice shows through are handled rather than hidden. A database may keep its text in UTF-8, UTF-16 with the little end first, or UTF-16 with the big end first, and converting on the way in is what lets one set of functions serve all three: `length` and `substr` count characters, `like` and `glob` match them, and none of them needs to know. Three functions do know, because they answer the bytes as the file holds them — `hex`, `octet_length` and a cast to a blob — and each is given the encoding rather than guessing. The fourth is the collation. `BINARY` is memcmp over the stored bytes, so on a file that keeps its text in UTF-16 with the little end first the letter `z`, one code unit of `007A`, sorts after a character written as the pair `D800 DC00`, whose first byte is zero. That is not the order the characters are in and not the order UTF-8 would give, so it is a collation of its own — one per byte order — and a database maps `BINARY` onto it when it is opened. The alternative, holding text as the file does and teaching every function three encodings, is what SQLite does and is more code in every function rather than in four places. | decided | 2026-09-13 |
| D-148 | An aggregate is answered by the call it was written as, and the grouping engine is a second pass over the rows the scan already yields. The evaluator gains one hook — a row may answer a call by its node — so that `eval` still knows nothing about groups and two `count(*)` in one statement are two columns rather than one. What this buys: `WHERE` filters rows, `GROUP BY` puts them in groups, each group steps every accumulator once, and `HAVING` filters the groups, each of them the same walk the ungrouped path takes. It costs O(n·g) to find which of `g` groups a row belongs to, which is a list scan rather than a hash, and that is the same trade D-146 made — rows before plans. Two things the C decides that a port cannot guess. `sum` keeps an integer until one overflows and then carries on as a Kahan-Babuska-Neumaier sum, refusing only if no double ever arrives after the overflow, so `sum` and `total` over the same column answer differently and both are right. And a bare column of an aggregate query comes from a definite row: the first row of the group, unless a `min` or a `max` is being accumulated, in which case it is the row the last of them took — which is how `SELECT b, max(a)` answers the `b` beside the greatest `a` rather than an arbitrary one. | decided | 2026-09-13 |
| D-149 | A compound is answered core by core and merged, and which of two rows that compare equal but are not the same bytes survives is part of the answer. SQLite compiles `UNION` and its two relatives as a merge of sorted sides, not as an insert into one index, and the merge drops the left row on a tie — so `SELECT t FROM n UNION SELECT 'a'` over a `NOCASE` column answers `a` where `SELECT 'a' UNION SELECT t FROM n` answers what the table holds. The engine does the same thing the same way: each side sorted by every column and deduplicated keeping its first, then merged keeping the right side's on a tie for `UNION` and the left side's for `INTERSECT` and `EXCEPT`. `UNION ALL` sorts nothing. The collation each column compares under is the first side that writes one, which is why the collation of a column reaches a literal on the other side of the operator. | decided | 2026-09-13 |
| D-150 | A join is the loops nested, one level per table of the `FROM`, and a cursor holds one row of each. What this decides is where the semantics live rather than how fast they are: a `LEFT JOIN` is the level answering its row once with the level below it empty, an `ON` is read at the level it was written on rather than as a `WHERE`, and a `USING` is the comparison it stands for — under the collation of the side written first, so `a JOIN c USING(y)` and `c JOIN a USING(y)` answer differently when one of the two collates without case. A name two tables answer is refused rather than chosen between, `rowid` included, which is what makes `SELECT * FROM t, t` a refusal: a `*` names every column by its table and two tables of one name leave every one of them ambiguous. A `RIGHT` or a `FULL` join adds one pass per such table after the nest: the rows it matched nothing to, with the levels before it empty and the levels after it walked as usual, which puts them after every row the nest answered as SQLite puts them. That pass is what makes a `USING` column of an outer join answer the first side holding something rather than the side written first — the `coalesce` `sqlite3ProcessJoin` writes around it, which this engine applies to every bare reference and to a `*` because the two sides of a `USING` are equal wherever both hold something. What it costs is the product of the tables' rows, with no index and no ordering of the loops, and O(n·m) to find whether a row was matched — the same trade D-146 made, rows before plans. | decided | 2026-09-13 |
| D-151 | A column that is computed and not stored is filled in after the record is read, by passes that repeat until none settles anything. SQLite lets one such column name another in either direction — `CREATE TABLE t(a, b AS (c+1), c AS (a*2))` answers 11 for a=5 — so there is no order over the columns that settles them all, and a pass that refuses a name it cannot answer yet and asks again in the next pass is what gives the dependency order without a graph. It costs at most one pass per computed column, O(k²) evaluations for `k` of them, which is a number a schema fixes and not a file. A column still unsettled after those passes names itself or names a column no table has, and refuses; SQLite refuses both when the table is created, which this engine cannot see because it reads the schema as text. The record is read by the storage place `sqlite3TableColumnToStorage` gives each column, because such a column takes no place in it. | decided | 2026-09-13 |
| D-152 | A table written `WITHOUT ROWID` is walked as the index tree it is, and the mapping from a column to its place in the record is computed once per table when the schema is read. Section 2.4 of the format puts the key's columns first, in the order the key names them, and the rest after them in the order the statement declares them; a column computed and not stored takes no place at all. One table of numbers answers both rules, so nothing downstream asks which kind of table it is reading. The walk of an index tree is not the walk of a table tree: an index tree carries an entry on every page, so the walk answers the entry between two subtrees after the first of them and before the second, which is one frame per level and O(n) steps as before. Such a table answers no `rowid`, which follows from the tree it is in and not from a flag anyone sets. | decided | 2026-09-13 |
| D-153 | A side of a `FROM` is named by the columns it answers and not by the table it is, so a statement written inside the `FROM` and a `WITH` term read the same way a table does. Every side carries a shape: one name, one affinity and one written collation per column, plus whether the side answers `rowid`. A table's shape is read from its definition, a statement's from the columns it answers, which is `sqlite3ColumnsFromExprList` and `sqlite3ExprAffinity` reduced to what a reader needs. A statement inside the `FROM` and a `WITH` term are answered once, before the outer walk begins, because SQLite has no `LATERAL` and neither can read the row the walk stands on; the cost is the rows of the inner statement held in memory, which the pager step replaces with a materialized table. A `WITH` written `RECURSIVE` refuses. | decided | 2026-09-13 |
| D-154 | A statement written inside an expression is answered where it is read and not before, so that an `ON`, a `WHERE`, a `HAVING` and the branch a `CASE` takes each read one and an untaken branch reads none. The cursor the walk stands on carries what it takes to answer one — the database, the tree, the text and the `WITH` terms in scope — and `eval` names the four shapes (`(SELECT ...)` as a value, `EXISTS`, `IN (SELECT ...)`, `IN table`) without knowing how any is answered. A statement that names a column no side of its own `FROM` answers reads it from the row of the statement that encloses it, which is what makes it correlated and costs O(n·m) for `n` outer rows and `m` inner ones; SQLite's own plan for an unflattened correlated subquery costs the same. The option not taken is answering every such statement once before the walk, as the aggregates are: it cannot answer a correlated one and it reads the branch a `CASE` does not take. | decided | 2026-09-13 |
| D-155 | A reader follows the write-ahead log by building its own index of it and never reads the `-shm` file. Section 4.6 leaves the wal-index to the host machine's byte order and says a reader rebuilds it from the log after a crash, so a portable reader has nothing to gain by reading one. `Wal::open` walks the frames once, O(f) for `f` frames, keeping the newest frame of each page in a list sorted by page number, and a lookup is a binary search of O(log p) over the `p` pages the log holds. The walk stops at the first frame whose salts are not the header's or whose checksums are not the running one, because everything after such a frame was written before the last reset. Only frames up to the last commit frame are kept, and that frame says how many pages the database has, so the file's own length is stale once a log holds a commit. The header is read from page one wherever page one is, which is the log for a database whose whole content the log holds. The option not taken is reading the `-shm` file: it is not portable, it is transient, and rebuilding costs one pass the reader makes anyway. | decided | 2026-09-13 |
| D-156 | A hot rollback journal is played back in memory rather than refused, because the rows it restores are the rows SQLite answers for that pair of files and a reader that refuses answers none. `Journal::open` walks the records once, O(r) for `r` records, keeping where each page's content begins in a list sorted by page number, and a lookup is a binary search of O(log p). A page journaled twice keeps its first record, which is the content the transaction started from and is the `pDone` bitvec of `pager_playback_one_page`; this is the opposite of the log's rule, where the newest frame wins, because a journal holds what was and a log holds what is. The first header says how many pages the database had, and a page past that count is passed over, which is the truncation `pager_truncate` does. The walk stops at a record naming page zero, at a record whose checksum is not its own, and at a header whose sector or page size is out of range — each is `readJournalHdr` or `pager_playback_one_page` stopping, because a process that wrote one had crashed before the header was synced. A journal whose first eight bytes are not the magic is not hot at all: SQLite writes zeros there until the records are on disk, so such a file sits beside a database that was never changed. The option not taken is refusing a database with a hot journal: it is safe and it answers nothing, where the format says what the rows are. | decided | 2026-09-13 |
| D-157 | The reader keeps no cache keyed by page number, and a walk holds the one page it stands on instead. A cache exists in SQLite because a page comes from a file; this crate is handed the whole file as bytes, so a lookup keyed by page number would add a search and memory to save a slice computation. What did repeat was the parse: `Rows::next` and `Entries::next` parsed the page again for every cell of it, which is O(n) parses for `n` rows where one per page visited is O(p) for `p` pages. Each walk now holds the page it stands on, which needs no allocation, no interior mutability and no eviction, because a walk stands on one page at a time. Measured over `fixtures/page512.db`, eight hundred scans of four hundred rows each fell from about 30 ms to about 28 ms: five percent, because `Page::parse` is a handful of byte reads and not an allocation. What repeats and this does not fix is the walk itself — a join re-walks its inner side once per outer row — and the plan of Q6 is what fixes that, not a cache. What would change this decision: a database larger than memory, or writing. | decided | 2026-09-13 |
| D-158 | A `WHERE` that holds a side's rowid to a range makes the walk descend to the first row of that range rather than scan to it: O(log n + k) for the `k` rows the range holds where a scan is O(n). Only the terms a top-level `AND` spine holds are read, because a term under an `OR` or a `NOT` need not be true of every row; only the `WHERE` is read and never an `ON`, because an `ON` decides which rows of an outer join match and not which rows the statement answers. A row the range leaves out is a row the same term refuses, and a row of an outer-joined side that the range leaves out answers `NULL` for that column, which the same term refuses as well, so the answer is what a scan answers. A bound that is not a whole number is passed over rather than converted, so `rowid='2'` scans and answers rightly; converting it is `sqlite3BtreeMovetoUnpacked` and is a later step. Measured over `fixtures/page512.db`, two thousand answers of `WHERE rowid=399` fell from 221 ms to 4 ms and of `WHERE rowid>200 AND rowid<=205` from 333 ms to 10 ms; a scan of the whole table is unchanged. The option not taken is planning from the `ON` clauses as well, which is right for an inner join and wrong for every outer one. | decided | 2026-09-13 |
| D-159 | A `WHERE` that holds an indexed column equal to a value is answered out of the index's tree: the descent finds the first entry with that key, O(log n), and each entry names a row the table's tree is then descended to, O(log n) each. An index is taken only where the engine can prove its order is the order the term asks about, and it scans otherwise, because a row wrongly left out is an answer that is wrong where a scan is only slow. What is refused: an index whose first column is held backwards, one whose collation is not the column's, an index over an expression, a partial index, an index over a table that keeps its rows in the key's own tree, a key of `NULL`, and a value a row has to be read to know. The bound has the column's affinity applied to it, because an entry holds what that affinity left of the value. An entry that runs onto an overflow page is read whole before it is compared, and an entry the descent cannot read at all gives the index up rather than refusing. A rowid range is never given up for an index, because a table's own tree answers a row where an index answers only where the row is. Measured over `fixtures/indexed.db`, five thousand answers of `WHERE a=77` fell from 143 ms to 13 ms and of `WHERE b='v77'` from 183 ms to 15 ms. The option not taken is counting how many rows each index would answer, which is what `sqlite3WhereBegin` does and what chooses between two indexes rather than taking the first. | decided | 2026-09-13 |
| D-160 | The math functions this crate answers are the ones whose answers are exact, and the rest refuse. `pi`, `degrees`, `radians`, `ceil`, `ceiling`, `floor`, `trunc`, `mod` and `zeroblob` are a constant, one multiply, a whole number, an exact remainder or a run of zero bytes, so an implementation that is right is the same bits the C library answers and the recorded golden holds it to them. `sqrt`, `exp`, `ln`, `log`, `pow` and the trigonometric set are not required by IEEE 754 to be correctly rounded, so no implementation is bit-identical to the host library the oracle was recorded against, and a golden written from one would hold this crate to that host and not to the format. `ceil`, `floor` and `trunc` are computed out of the bits of the double rather than by converting to an integer, because the crate is `no_std` and has no library to call and because a conversion would refuse the numbers past 2^63 that the bits handle without a special case. The option not taken is answering the transcendentals within a few units in the last place, as `crates/math` does for `pow`: it is right for a kernel and wrong for a port whose test is that its answers are SQLite's. | decided | 2026-09-13 |
| D-161 | The format language is ported whole, including the conversions no statement was expected to reach, and one routine renders a double for `format`, for `quote` and for every real a statement writes down. `sqlite3_str_vappendf` serves the C library's own formatting and the SQL function from one body, and the two differ only in where an argument comes from: read from SQL, `%n` writes nowhere, `%z` is `%s`, `%c` takes the first character of the text of its argument, and `%T` and `%S`, which name a token and a term of a `FROM` that only the parser holds, end the conversion where they are read. A conversion character the table does not name ends it as well, and what was written before it is the answer. A format that ends without a conversion having written answers `NULL` rather than empty text, because `sqlite3StrAccumFinish` answers a pointer to nothing and `sqlite3_result_text` reads that pointer as a `NULL`: `format('%y')` is `NULL`, and `format('%n')`, which writes no byte and still counts as a conversion that wrote, is empty text. Sharing the rendering is what keeps `%g` and the text a real is stored as from parting, because `sqlite3VdbeMemStringify` prints `%!.17g` and `round(X,Y)` prints `%!.*f`, so each is that routine under a different style and a divergence between them is a value that reads back as another. One call is O(n) in the bytes it writes, and two refusals come before any of them: a precision of more than a hundred million digits is that many, which is `SQLITE_FP_PRECISION_LIMIT`, and a field wider than an answer holds refuses before the buffer is taken rather than after it is filled, which is `printfTempBuf` and `sqlite3StrAccumEnlarge`. `unistr(X)` is in the same module, because it reads the escapes `%#q` and `%#Q` write and a reader of one has to be the reader of the other; `quote(X)` of text is `%Q` of it and `unistr_quote(X)` is `%#Q`, which is what `sqlite3QuoteValue` calls. The option not taken is porting only the conversions a statement was expected to reach: every one of the twenty-three is reachable from SQL, `%p`, which writes a value where the C library writes an address, included. | decided | 2026-09-13 |
| D-162 | The storage layer of writing is built before the virtual machine, so Q7 depends on Q3 and Q5 rather than on Q6. Reason 1: a b-tree writer needs the pager and the format and not opcodes, because what it writes is pages; the opcodes that drive it are statements and come with the machine. Reason 2: nearly every test of SQLite's own suite writes a database before it reads one, so the suite of Q9 is out of reach without writing and within reach without the machine. Reason 3: the machine changes how a statement is answered and not what the engine can do, which makes it the one step whose absence costs nothing outside itself. The first piece is the record: `record::write` is `OP_MakeRecord`, which applies the affinity of each column, picks a serial type per value — including `MEM_IntReal`, where an integer in a column of real affinity is stored as an integer while six bytes hold it and as the double it stands for where they do not — and writes the header of types and the body of values. It is O(n) in the values, one pass to size the record and one to write it. What holds it is the bytes the C library wrote: every row of every fixture is read, written back and compared byte for byte, which is 978 rows over eight files, and the fuzz target holds that a record this engine writes reads back as the values it was written from. The option not taken is the plan's order, the machine first: it leaves the engine unable to make a database for another four steps, and the writer would then be written against opcodes that do not exist rather than against the files the C library writes. | decided | 2026-09-13 |
| D-163 | A page is written the way `rebuildPage` writes one: the pointer array names the cells in the order they are given, and the content of each is taken from the end of the usable part downward, so the cell named first lies highest. That is the shape a page filled in key order has, which is every page a balance rewrites and every page a table filled in rowid order holds, so a page this engine builds is the page the C library wrote, byte for byte, where the page carries no freeblock and no fragmented byte. What is not compared is the space between the pointer array and the content: it is unallocated, the format says nothing about it, and SQLite leaves in it whatever the page held before, where this engine leaves noughts. `write_cell` is the cell half of `fillInCell`: the child pointer of an interior page, the length of the payload, the key of a table tree, what of the payload the page holds, and the first overflow page after it. The split between what stays and what runs on is the reader's, because a cell being written back is a cell that was read; a cell made from a row that was never on a page is the b-tree writer's work and needs a pager to allocate the overflow pages from. One page is O(n) in the cells, one pass. What holds the two is the pages the C library wrote: every cell of every tree of eight fixtures is written back and compared against the bytes it lies in, which is 1663 cells, and 56 of those pages are built whole and compared. The fuzz target holds the weaker statement that arbitrary bytes allow: a page built from the cells of another page reads back as those cells, whatever varints the file wrote its lengths as. | decided | 2026-09-13 |
| D-164 | A page counts its own free space on every write rather than carrying the number, and the checks the C library needs because it carries one are not ported. `MemPage.nFree` exists in `src/btree.c` because a page is opened once and changed many times, and every routine that changes it has to keep that number right; `btreeComputeFreeSpace` is what rebuilds it, and `defragmentPage`, `pageFindSlot` and `allocateSpace` each re-check what it already established. This engine counts instead: `Writer::free` is `btreeComputeFreeSpace` and is asked at the start of every insert, which is O(f) for the `f` freeblocks a page holds, where a page holds at most a few. What follows is that a check inside a later step is either the first to look at what it looks at, in which case it is ported and is reached, or it repeats what counting has established, in which case it is dead and is not written: a freeblock list that leaves the page is refused once, by the counting, and `defragmentPage`'s three further tests of the same thing are gone. `dropCell` drops its own check as well, because a cell the reader answered is one whose every byte it found inside the page and what is written back is never longer — a length the file wrote as a wide varint comes back as the shortest varint that holds it. What holds the layer is the pages the C library wrote: every cell of 1333 of them is taken off its page and put back, and the page has to be the bytes it was; the run of four hundred inserts and removes fills a page, fragments it and moves it together again; and a page with one byte of it changed, over every byte and thirteen values, has to refuse rather than write outside itself. The option not taken is carrying `nFree`: it saves a walk of a list that is almost always empty and costs every routine the duty of keeping it right. | decided | 2026-09-13 |
| D-165 | A file this engine writes says it was written by the C library this crate is a port of: `header::LIBRARY_VERSION` is 3053004, the `SQLITE_VERSION_NUMBER` of `research/sqlite`, and it goes in the last four bytes of the header where SQLite writes its own. Reason 1: the field says which library's format the file is in, and the format this crate writes is the one that library documents, so any other number would name a format this crate did not write. Reason 2: a file written by this engine and a file written by that shell can then be compared byte for byte, which is what holds the writer to the reader of every other engine; a number of this crate's own would make every such comparison fail on four bytes that mean nothing to a reader. Every fixture asserts the constant, so a shell of another version changes it here and the assertion says so. The twenty bytes section 1.3 reserves are written as noughts, as SQLite writes them, and a file whose schema has never been written says nought for its encoding where this writes the one a reader made of it, because a reader cannot tell the two apart. The option not taken is a number of this crate's own: it would say the file is in a format no document describes. | decided | 2026-09-13 |
| D-166 | A database being written is the pages it holds in a list, page one first, and a page is added by putting it at the end. `Pages` is what the pager of `src/pager.c` is for a writer that has no file yet: there is no cache, because every page is in memory and a page number is an index; there is no journal, because nothing is overwritten until a transaction is; and a page is added rather than allocated, because the free list of a file is empty until a delete makes one. `tree::insert` is `sqlite3BtreeInsert` less the balance: the descent to the leaf is the one a reader makes, a payload longer than the page holds runs onto overflow pages of `usable - 4` bytes each, chained by their first four bytes, and a leaf that will not hold the cell answers that it will not, where SQLite splits it. One insert is O(log n) for the descent and O(n/p) for the `n` bytes that run onto pages of `p` bytes. What holds all of it is the files the shell wrote: `small.db`, `utf16.db`, `joins.db` and `overflow.db` are built here out of a schema and its rows, and each is the fixture byte for byte — four thousand and ninety-six bytes of the last of them on the leaf and the other fourteen thousand on four overflow pages. The option not taken is a pager that reads and writes one file: it is what Q7's journal step needs and what a database that does not fit in memory needs, and neither is reached before the balance. | decided | 2026-09-13 |
| D-167 | A leaf that will not hold a row is balanced the two ways an append needs and no other way yet: `balance_deeper`, where the root of a tree of one page grows a child under it, and `balance_quick`, where a full right-most leaf grows a sibling beside it with a divider on the parent naming the page that was full and the largest key on it. A row that belongs anywhere but at the end of the right-most page is refused with `Error::Balance` rather than written into a tree this crate cannot then keep right, which is `balance_nonroot` and is the next step. Reason: a table is filled in key order — every `INSERT` of a rowid the engine picks, every index built from a sort, and every `INSERT ... SELECT` of the fixtures — so the append is the shape that makes a tree at all, and it is the shape the C library itself has a routine for. `zeroPage` is ported as it is: the header of the root is emptied and its body is left where it lies, so the bytes between the pointer array and the content of a page that was a leaf are the bytes that leaf held. That is what makes `tall.db` come out byte for byte: four hundred rows over pages of five hundred and twelve bytes, thirteen leaves under one root, and the root still holding the first leaf's rows in the space no cell is in. One insert is O(log n) for the descent, and a balance is O(1) pages: one new page and one divider, where `balance_nonroot` reads three. The option not taken is refusing every tree of more than one page until the general balance is written: it would leave the engine unable to write a table of more than a few rows for another step. | decided | 2026-09-13 |
| D-168 | A row that lands anywhere in a tree is written: `balance_nonroot` reads the page that would not hold the cell and up to one sibling on either side, gathers their cells with the dividers between them, packs the cells over as many pages as they need, evens the two sides out, and writes the parent's dividers again. The balance then runs up one level per turn, because a parent that has no room for a divider holds it the way `insertCell` does and is balanced next, and a root that is full grows a child under it whatever kind it is, so a tree three levels deep is written the same way a tree of two is. What holds it is the files the shell wrote: `shuffled.db` puts four hundred rows in by a key that jumps about, and `deep.db` four thousand, which fills the root, grows the third level and then balances the interior pages of that level against each other; each is the fixture byte for byte, and every one of the four thousand prefixes of `deep.db` was compared the same way while the port was written. Two things the C library does incidentally are ported because the bytes say so: `editPage` moves the pointer array by whole entries and reads as many of them as the page held, which writes stale bytes into the pointers past the last cell, and `copyNodeContent` copies the header, the pointer array and the content area and leaves the space between them as the noughts a page added at the end of the file carries. The space a page takes is counted as a signed number, because the packing hands a cell to the next page before it knows that page holds it and the next page then owes space; counting it unsigned clamps at nought and picks another split. One balance is O(c) in the `c` cells of three pages, and a balance that runs to the root is O(c log n). The option not taken is C's checks for a corrupt `nFree`: a page this crate writes counts its own free space on every write, which D-164 records, so a packing that leaves a page with no cells cannot arise and the check for it is not written. | decided | 2026-09-13 |
| D-169 | A row taken out gives its pages back, and what the file then holds is what the pager wrote and not what the cache holds. `tree::remove` is `sqlite3BtreeDelete` for a table tree, where the descent always ends on a leaf, so the cursor never stands on a divider and the case that moves the previous cell up is the index tree's alone. The overflow chain of the row goes back first and the cell is dropped after it, which is the order the free list comes out in; a leaf left more than two thirds empty is balanced, which joins pages, and `balance_nonroot` writing fewer pages than it read frees the rest. A root left with no cell takes the content of its only child, which is `balance-shallower` and makes the tree one level shorter. The free list is `freePage2` and `allocateBtreePage` without auto-vacuum: a freed page becomes a leaf of the first trunk while that trunk holds fewer than `usable/4 - 8` of them and the new first trunk otherwise, and a page comes back off the leaf nearest the page the caller names, which keeps a tree that grows out of pages lying near each other. Three things the pager does are ported because the bytes say so: a page freed onto a trunk is left out of every commit until it is written again, which is `sqlite3PagerDontWrite`, so the file keeps what that page held when the transaction began while the cache holds what the delete left; a page the free list gives back is read as noughts unless the transaction that asks is the one that freed it, which is `PAGER_GET_NOCONTENT` and `pHasContent`; and `Pages::begin` is where a transaction ends and the next begins, because those two rules are the only places the file and the cache part ways. One delete is O(log n) for the descent and O(f) for the `f` pages of the chain it gives back. What holds it is the files the shell wrote: `deleted.db`, `emptied.db`, `cleared.db`, `unchained.db` and `reused.db`, each built here out of the same statements and each the fixture byte for byte, and every one of the four hundred prefixes of `emptied.db` compared the same way while the port was written. The option not taken is keeping the free list in the header alone and writing every page at every commit: it is simpler and it writes files SQLite does not. | decided | 2026-09-13 |
| D-170 | A commit writes the rollback journal of the transaction, and the journal mode says what is left of it afterwards. `journal::write` is `writeJournalHdr` and `pagerAddPageToRollbackJournal`: a header of one sector holds the magic, the record count, the nonce every checksum begins at, how many pages the database had, the sector size and the page size, and a record of a page number, the page as it was, and a checksum follows for each page the transaction wrote that the database already held. A page the transaction added is in no record, because rolling back shortens the file to the pages it held. `Pages` keeps the order the pages were opened to write in, because that is the order the records lie in, and page one is in every journal whether the transaction wrote it or not: the commit writes the change counter into page one, which is `pager_incr_changecounter`, and a transaction that touched no other part of page one holds it last. The sector is 512 for every file whose device says a write of one sector cannot damage another, which is every file on a build with `SQLITE_POWERSAFE_OVERWRITE` and so the default. `journal::committed` is `zeroJournalHdr` and the four modes around it: `delete`, `memory` and `off` leave no file, `truncate` leaves an empty one, and `persist` writes noughts over the twenty-eight bytes of the header and leaves the records. What holds it is the files the shell left: `journalled.db-journal` is the journal of the delete that makes `emptied.db` and `appended.db-journal` the journal of the insert that makes `shuffled.db`, each written here and each the fixture byte for byte. The nonce comes from SQLite's random source, so the tests read the one the fixture was written with out of its first record and the eight records after it are what say the nonce is right. Writing one journal is O(n) in the bytes of the pages it holds. The option not taken is writing a journal of every page the transaction wrote: a page the file did not hold is not one a rollback can restore, and SQLite shortens the file instead. | decided | 2026-09-13 |
| D-171 | A commit in write-ahead logging mode writes frames rather than pages, and `wal::Log` is `pagerWalFrames` for a caller that has the pages in hand: a header of the magic, the version, the page size, the checkpoint sequence and the two salts, with the checksum of its own first twenty-four bytes, and then a frame of a page number, how many pages the database has where the frame is the last of a transaction, the two salts, and the running checksum. The frames of one transaction are the pages it wrote in the order their numbers run, which is `sqlite3PcacheDirtyList`, and page one is among them where the transaction changed how many pages the database has, because the commit writes that count into page one along with the change counter; a transaction that leaves the count alone holds no page one, which is what a delete that frees no page shows. A header whose schema format is nought says nought for its encoding as well, because SQLite writes the two together when the first table is created, which is what a database `PRAGMA journal_mode=wal` has left holds. What holds all of it is `logging.db` and `logging.db-wal`: three statements, three transactions, thirty-two frames and no checkpoint, written here and the fixture byte for byte. The two salts come from SQLite's random source, so the test reads them out of the header of the fixture and every checksum of every frame is what says they are right. One commit is O(n) in the bytes of the pages it writes. The option not taken is writing the database file at every commit as the rollback journal mode does: a log is what lets a reader read while a writer writes, and section 4 says the file is written only by a checkpoint. | decided | 2026-09-13 |
| D-172 | A statement that changes a database is run from its text: `change::Writer` holds the pages and the header beside them, reads one statement per call, and answers the file those statements made. `CREATE TABLE` takes a page for the tree of the table and puts a row in `sqlite_schema` that names it, with the statement stored as its own text without the space around it and the semicolon that ends it, which is the span the parser read it from. `INSERT` answers its rows through `db::Database` over the file as it stands, which is what makes `INSERT ... SELECT` the same code as `INSERT ... VALUES`: a `VALUES` is a select core in `src/select.c` and it is one here. The columns a statement names may stand in any order and may leave columns out, which take the value nothing; `rowid`, `oid` and `_rowid_` name the key of a table that has no column of that name; the key is the one the statement gives, else the one the column the key is another name for gives, else one past the largest the tree holds, which is `OP_NewRowid`; and the column the key is another name for is stored as nothing, because the key carries it. Each statement is its own transaction, which is what a connection outside `BEGIN` does, so each bumps the change counter. What holds it is the files the shell wrote: `small.db`, `utf16.db`, `joins.db` and `stated.db` are built here by running the statements the shell was given, and each is the fixture byte for byte. Reading one statement is O(n) in its text and running an `INSERT` is O(r log p) for `r` rows over `p` pages. The option not taken is a virtual machine: `sqlite3Insert` compiles to opcodes and this runs the statement directly, which is Q6's work and not Q8's, and the two answer the same file. | decided | 2026-09-13 |
| D-173 | `DELETE` is run from its text the way `INSERT` is: the keys of the rows the `WHERE` keeps are found first and the rows are taken out after, which is what `sqlite3DeleteFrom` does with its `RowSet`, because a tree changes under a walk of it. The `WHERE` is answered against one row at a time through `eval::Row`, which is the trait the expression reader already asks a row for a column through, so a statement that changes rows needs none of the machinery a statement that answers rows needs: no plan, no join, no order. A column may be written with the table's name or the schema's before it, and `rowid`, `oid` and `_rowid_` name the key; a name that belongs to no column of the row is refused rather than read as nothing. `db::Database::rows_of` is the walk it reads: every row of one table, each with its key and the values of its columns, which is O(n) in the rows. A statement that wrote no page leaves the change counter where it stood, because the commit has nothing to write for it, which is what a `WHERE` that keeps no row shows. What holds it is the files the shell wrote: `shuffled.db` is written here by one statement that names the key of every row, and `deleted.db`, `emptied.db` and `cleared.db` are the three deletes over it, each the fixture byte for byte. The option not taken is walking the tree and taking rows out as the walk finds them: the walk would read pages a balance has already written again. | decided | 2026-09-13 |
| D-174 | `UPDATE` is run from its text the way `DELETE` is: the rows the `WHERE` keeps are read first, each with the values its columns are to carry and the key it is to carry, and the tree is written after, because a tree changes under a walk of it. A column the `SET` names is the position it holds in the table, and `rowid`, `oid` and `_rowid_` name the column the key is another name for where the table holds one, so `SET rowid=9` and `SET id=9` write the same place; a name that belongs to no column is refused. The column the key is another name for is read under integer affinity, so `SET id='7'` writes the key 7 and `SET id='x'` is refused, which is the datatype mismatch `sqlite3VdbeExec` raises at `OP_MustBeInt`. `tree::update` is `sqlite3BtreeInsert` where the cursor found the key, and it has three paths: a payload the length the old one was goes where the old bytes lie and the chain the row runs onto stays, which is `btreeOverwriteCell`; a cell the length the old one was, where the old cell held its whole payload, goes where the old cell lies, and the new cell may hold less payload because the four bytes that name the chain make up for it; every other cell is dropped from the leaf and put in again, with a balance where it does not fit. A key the statement writes moves the row, so `tree::remove` takes it out and `insert` puts it back, which is what `sqlite3GenerateRowDelete` and `sqlite3CompleteInsert` do for a statement that writes the key. One row is O(log p) over `p` pages and O(n) in the bytes of its payload, and the statement is O(r) over the rows the `WHERE` keeps. What holds it is the files the shell wrote: `updated.db` writes over rows that keep, shrink and grow their payloads, `overwritten.db` writes over a payload the length it was and over a cell the length it was, `moved.db` moves the key under both of its names and drops an overflow chain, and `keyed.db` writes over every row of a table that holds no column the key is another name for, each the fixture byte for byte. The option not taken is taking every row out and putting it back: a row whose payload is the length it was would give up the chain it runs onto and take the chain again, and a cell that lies where it lies would be written twice. | decided | 2026-09-13 |
| D-175 | `change::Writer` carries the journal mode, so a statement run from its text writes the file and whatever the mode leaves beside it. `Writer::journalling` names the mode, the nonce every checksum begins at and the sector the header takes, and `Writer::journal` answers what the commit left: nothing under `delete`, `memory` and `off`, an empty file under `truncate`, and the records under a header of noughts under `persist`. `Writer::logging` names the two salts and puts the commits into a `wal::Log`: the file stays as `PRAGMA journal_mode=wal` left it, because a checkpoint is what writes a frame back into the file and this crate runs none, and the counter a frame of page one carries is the counter the file carries and one, which is `pager_write_changecounter` reading `dbFileVers` off a file no commit has written. A statement reads its rows out of the pages rather than out of the file the caller sees, which is what lets the log grow while the file stands still. The header this crate keeps beside the pages carries the page count and the free list after every commit, so the record of page one in the next journal holds the header the transaction began with. This closes the journal-mode dimension of 16.11 over the write path: the same three statements under all six modes write `emptied.db` under the five that write the file, and `journalled.db-journal` and `logging.db-wal` are the fixtures byte for byte. One commit is O(n) in the bytes of the pages it wrote, under every mode. The option not taken is a fixture per mode: five files that are `emptied.db` byte for byte would record the mode's name and nothing else. | decided | 2026-09-13 |
| D-176 | Unique-cause modified condition/decision coverage is claimed for the SQLite port from condition coverage plus a check on the shape of its decisions, and not from a report, because the pinned toolchain takes `-Z coverage-options=block`, `branch` and `condition` and refuses `mcdc`. `&&` and `||` evaluate the right operand only where the left one does not settle the answer, so an operand is evaluated only where every operand that could mask it has taken the value that does not mask it, and it decides the outcome wherever it is evaluated; two evaluations of one operand with opposite values are then the pair MC/DC asks for. Condition coverage at 100 percent says every operand was evaluated both ways, so over decisions built only from those two operators it is masking MC/DC, which is the form DO-178C accepts for the objective. A bitwise `&`, `|` or `^` over booleans breaks the argument, because both operands are evaluated whatever the first says and the toolchain records no condition for either, so `cargo xtask mcdc` reads the typed tree of every crate of `COMPLETE` through `-Zunpretty=thir-tree` and reports each one by the span the compiler gave it; reading the source instead would answer nothing, because `&` there is a reference as often as an operator, and the type of an operand is what tells the two apart. Reading one tree is O(n) in its lines. `cargo xtask check` runs `coverage`, `coverage --condition` and `mcdc`, so a decision whose second operand no test settles and a decision written with an operator that hides its operands each fail the check. Unique-cause MC/DC asks beyond that argument only that no decision names one condition twice, because a condition standing in two places cannot be varied on its own, so `cargo xtask mcdc` also reads the source each decision's conditions lie at and reports a decision that names one twice; it passes over a span the compiler wrote from a macro, because a derived `PartialEq` compares each field under one span and would read as one condition repeated. `db-sqlite` holds 409 short-circuit operators, 656 conditions, no bitwise operator over booleans and no decision naming a condition twice, so the crate meets unique-cause MC/DC. The option not taken is waiting for the toolchain to emit the records: a goal does not become measured by waiting. | decided | 2026-09-13 |
| D-177 | A file that vacuums itself keeps pointer maps, and `Pages` writes them: page two is the first map page and every hundred and third page after it over 512-byte pages, which is `ptrmapPageno`, and each map holds five bytes per page after it saying what the page is and which page names it. `Pages::point` is `ptrmapPut` and writes only where the entry changes, so a transaction that moves nothing opens no map page; `Pages::point_cells` is `setChildPtrmaps` and `ptrmapPutOvflPtr` together, and writes an entry for every child of a page and for the first page of every chain its rows begin. The C library writes an entry only where it cannot tell the entry already stands, and writing every one of them leaves the same file, because a map page is opened only where what it holds changes, which is what lets this crate write the maps at the end of a balance rather than inside its packing loops. A page taken at the end of the file that falls where a map lies makes that map page and the caller takes the page after it, which is `allocateBtreePage`. `PRAGMA auto_vacuum=full` has the commit move the pages at the end of the file into the free pages below them and cut the file back, which is `autoVacuumCommit`: `Pages::vacuum_commit` counts the end with `finalDbSize`, moves each page past it with `relocatePage`, writes the pointer on the page that named it and the maps for the pages it names, and drops the free list whole, because every page on it either took a page from the end or lies past the end. `PRAGMA auto_vacuum=incremental` leaves the free list and the file as they stand. `finalDbSize` steps its answer back past a map page it lands on; the count of map pages given up is what puts the answer past the last of them, so no page size, file length or free list reaches that step and D4 has it deleted rather than exempted. Writing one map entry is O(1) and one commit is O(n) in the pages the file gives up. What holds it is six files the shell wrote, each byte for byte: the same four hundred rows under both settings, chains that cross the second map page, the free pages the incremental setting keeps, and the pages the full setting moves down, one of them cutting a file of a hundred and eleven pages back to thirty-five. The option not taken is writing the maps where the C library writes them, inside `balance_nonroot`'s loops: the entry that stands is not written either way, and the loops there carry enough already. | decided | 2026-09-13 |
| D-178 | The matrix of document 16, section 16.11, is put to the write path as a covering array of thirty configurations rather than as the full cross of eight hundred and ten. Every value of every dimension appears in the thirty, and every pair of values from two dimensions appears together at least once, which is what a defect needing two settings at once takes to show; thirty is the fewest rows such an array can have, because the two widest dimensions are five and six values wide, so the array is as small as the property allows. The five dimensions the write path answers are the encoding, the page size, the reserved tail, the journal mode and the auto-vacuum setting; `every_pair_of_the_matrix_is_written_under_some_configuration` counts the hundred and fifty-six pairs and fails where a row is changed to drop one, so the array is checked rather than asserted. Each row holds the same four hundred rows put in by a key that jumps about, and each is the file the shell wrote with the journal or the log beside it, byte for byte; reading the whole array is O(n) in the bytes of the thirty files. `change::Writer` carries the settings as three calls that stand where the pragmas stand — `journalling`, `vacuuming` and `logging` — rather than as one constructor per combination, because the dimensions compose and the constructors would not. What the array found is `allocateBtreePage`: page one is opened to write whenever the file grows, which the journal of a file that keeps pointer maps shows because the map page is opened after the growth and lands behind page one in the records. The option not taken is the full cross: eight hundred and ten files of four hundred rows each, a hundred and thirty megabytes of fixtures, to cover the same pairs the thirty cover. | decided | 2026-09-13 |
| D-179 | A column the record holds no value for answers what it falls back to, which is `sqlite3ExprCodeGetColumnOfTable` reading the default out of the schema rather than answering nothing. Two places carry it: a row written before `ALTER TABLE ADD COLUMN` is short by the columns added after it, and a statement that names no value for a column leaves that column to its default. `schema::Column` keeps the default as the tree of the `CREATE` holds it beside the text `sqlite_schema` keeps, so `db::values_of` evaluates it where the record runs out and `db::Database::defaults` answers the whole row of them for the statement that writes rows. Evaluating one default is O(n) in the nodes of its expression, and the schema is parsed once when the database is opened. The formats the file format document numbers 2 and 3 are what this answers: 2 allows a row to be short and 3 allows the column it is short of to fall back to something other than nothing, so answering the default covers both and no test of the format number is needed. What holds it is three files the schema format dimension needed the oracle to write, because no pragma the shell takes asks for a format below four: `format1.db` under `SQLITE_DBCONFIG_LEGACY_FILE_FORMAT`, `format3.db` with two columns added after its rows, and `format4.db` as the shell writes it, with fourteen cases of `query.corpus` holding the three to the C library; `defaults.db` holds the write path, byte for byte. Format 2 has no fixture, because the pinned library writes 3 wherever the document allows 2. The option not taken is reading the schema format number and answering nothing below 3: the file format document allows a short row only from format 2 and a default only from format 3, so the number says nothing the default does not. | decided | 2026-09-13 |
| D-180 | SQLite's own test files are run against the engine without a TCL interpreter, by `sh tools/xtask.sh sqlite-suite`. A `do_execsql_test` writes its name, its statements and its answer in the file, so a case carrying neither `$` nor `[` in the statements nor in the answer says what it runs and what it should answer without the interpreter running first; 10 428 of the 13 602 cases in `research/sqlite/test` are that shape. One database is kept per file and the cases run against it in the order they are written, because each builds on the ones before it, and a case the engine refuses stops the file, the cases after it refused with it, because the database is then short of what they read. Both sides are compared as the elements of a TCL list rather than as text, so a value holding a space and a value holding nothing are compared as one element each rather than split or dropped. Reading one file is O(n) in its bytes and running its cases is O(r) in the rows they answer. Of the 10 428, 408 pass, 14 answer differently and 10 006 name something the engine refuses; 16.23 lists the fourteen, of which six are defects this crate carries, two are a `UNIQUE` it does not keep, and six are the file or the build rather than the engine. The checkout is not part of this repository, so this is never a step of `cargo xtask check`, which is how `cargo xtask test-ext` treats Test262. The option not taken is building `testfixture` and linking a C ABI over this engine into it: the ABI is the whole of `sqlite3.h` and the suite would then test the ABI as much as the engine, where this tests what SQLite wrote down as the answer. | decided | 2026-09-13 |
| D-181 | Five defects the first run of SQLite's own test files showed are fixed, and each is a rule about which collation or which affinity a comparison uses. `sqlite3ExprCollSeq` reads the collation of an expression off the tree: a `COLLATE` written under an operator, under a call, or under any branch of a `CASE` is the collation the expression compares with, the left operand before the right and the first argument that carries one before the rest. This crate answered the collation only where the `COLLATE` stood on the operand itself, so `'ABC' || ('' COLLATE nocase)` compared with case; `eval::answer` now carries `left.collation.or(right.collation)` out of every binary operator, the first argument's collation out of every call, and the operand's out of every unary operator. A `CASE` is the one place where the answer cannot say it, because the branch a row takes is not the branch the collation is read from, so `eval::written_collation` reads the tree: the operand, then the branches in the order they were written, then the `ELSE`, which is not the order `Arena::under` names them in. `sqlite3ExprAffinity` gives a sign written before an expression no affinity of its own, which is what makes `xt == +xi` compare a text column against a number that takes text affinity from it rather than the other way about; this crate carried the operand's affinity through the sign. `resolveOrderGroupBy` matches an `ORDER BY` term against the answered names with any `COLLATE` taken off it first, so `ORDER BY m COLLATE binary` sorts by the column answered under the name `m`; the same is not true of `likely` and the two beside it, which are taken off only when the statement is compiled and not while its names are resolved, so this crate takes off the `COLLATE` alone. Reading a collation off the tree is O(n) in the nodes under the expression, and every other change is O(1). Seventeen cases of `query.corpus` hold all five to the C library, over `small.db` and a new `affinity.db` that holds the five affinities in one table. The suite went from 408 cases passing to 413. The option not taken is resolving every collation off the tree as the C library does: the answer carries it correctly everywhere but a `CASE`, and a second walk of every expression would cost what the first one already paid. | decided | 2026-09-13 |
| D-182 | `PRAGMA` is how SQLite says what a connection is configured with, so this crate reads one and answers it: `crate::pragma` names the settings the file itself holds, `db::Database::query` answers a pragma that sets nothing out of the header, and `change::Writer::run` applies one that sets something. The page size, the encoding and the auto-vacuum setting are what the first table is written under, so a statement that sets one after a table is there changed nothing once D-202 superseded the refusal; a pragma the file does not hold — the cache size, how a write is synced, where a temporary table lives — is accepted and changes nothing, because refusing it would stop a statement that changes no byte of the file. `PRAGMA journal_mode=X` answers the mode it left the connection in, which is the one setting that answers a row, so `Writer::run` answers the rows a statement makes rather than nothing at all; every statement that writes rows answers none. `journal_mode=wal` turned that mode on only through `Writer::logging`, which D-190 supersedes. `PRAGMA journal_mode` read back answers `wal` for a file whose write version says so and `delete` for every other, because the file holds no other mode. Reading one pragma is O(1) over the header. What holds it is the six files of the auto-vacuum dimension written a second time, with the three calls replaced by the three pragmas and the same fixture answered byte for byte, which is what says the two ways of configuring a file agree. This unblocked 60 of SQLite's own test files, whose first statement is a pragma, and took the suite from 413 cases passing to 425. The option not taken is answering every pragma SQLite has: `PRAGMA integrity_check` walks the whole file and `PRAGMA table_info` reads the schema, and both belong with the statements that read rows rather than with the header. | decided | 2026-09-13 |
| D-183 | `sh tools/xtask.sh sqlite-suite` reads three shapes of SQLite's own test files and not one: a `do_execsql_test`, a `do_test` whose body is one `execsql` and nothing else, and an `execsql` standing outside a case, which is the file setting itself up. The third is what made the other two reachable: ninety files answered `NoTable` for their first case because the table was created by an `execsql` the older files write outside a case, so the engine was refusing nothing and the harness was reading nothing. Reading the three took the corpus from 10 428 cases in 570 files to 15 550 in 881, and the cases passing from 425 to 900. A `do_catchsql_test` is read past whole rather than skipped by its name, so the `execsql` inside it is not taken for setup and its statements do not run twice. Setup is counted only by stopping the file where the engine refuses it, because a case it did not reach is not a case that failed. Reading one file is O(n) in its bytes. The sixty-seven cases that answer differently are grouped in 16.23; the largest, seventeen of them, is one table with a column named `rowid`. The option not taken is running the file under `tclsh` with the commands stubbed: the interpreter would resolve every substitution, and it would also run `db collate` and the other commands that register code in TCL, which no engine of this repository can answer. | decided | 2026-09-13 |
| D-184 | A collation belongs to an expression in two ways, and the two do not weigh the same. `sqlite3BinaryCompareCollSeq` takes the collation of a comparison from a `COLLATE` on the left operand, else a `COLLATE` on the right, else the collation the left operand carries, else the right's; a column carries the collation it was declared with and does not count as a `COLLATE`, which is `EP_Collate`. So `eval::Answer` says whether the collation was written as well as what it is, and `compared_under` weighs the two as the C library does, which is what makes `a < 'ccc' COLLATE binary` compare under binary where `a` was declared `COLLATE nocase`. A call carries two collations for the same reason: the one it compares under is the first argument that carries any, which is what `sqlite3ExprCodeTarget` gives a function that needs one, and the one it answers with is the first argument a `COLLATE` was written on, because `sqlite3ExprCollSeq` reaches an argument only along a path a `COLLATE` marked. An `ORDER BY` that names a column of the answer sorts under the collation that column was declared with, which this crate compared under binary; `db::Key::Place` now carries it, and a `COLLATE` written on the term beats it, the outer one where two are written. A collation no name of this crate answers is refused where it is written rather than where it is used, which is one place narrower than the C library, where `SELECT 'a' COLLATE nosuch` answers `a` because nothing asks that expression for its collation; the narrower rule is recorded here rather than answered, because a sort that silently falls back to binary is a wrong answer and a refusal is not. Choosing a collation is O(1) and reading one off an `ORDER BY` term is O(1). Eleven cases of `query.corpus` hold all of it to the C library over `wide16.db`, whose one table declares a column `COLLATE NOCASE`. SQLite's own files go from 27 cases answering differently to 16. | decided | 2026-09-13 |
| D-185 | The seven fuzz targets over `db-sqlite` are run as a campaign and not only as a regression replay, and what a campaign is answers what the regression replay does not: the replay reads the inputs a test cites and says nothing about inputs no one has written down. Five minutes of each target on one core is 264 million executions over the seven, which found one crash: `sqlite_expr` held that a column's place in a primary key is at most the count of columns, and `CREATE TABLE t(a INT,b,PRIMARY KEY(a, a, b))` gives `b` the place 3 in a table of two columns, which is what `PRAGMA table_info` answers for that statement as well. The invariant was the target's and not the crate's, so the target now holds what is true — no two columns share a place — and the case is a named entry of the regression corpus. The differential fuzzer of D-140 is the other half: 3 000 generated cases put to this engine and to the pinned shell agreed on 2 998 and were refused by the shell on 2, with no finding. What a run of a target finds is written into the corpus directory under a hexadecimal name and is not tracked, because another run finds it again; the ignore rule matched any name of thirty-two characters rather than thirty-two hexadecimal digits, which hid a named entry of the image corpus, and the rule now spells the digits out. One campaign is O(n) in the executions it runs, and each execution is O(n) in the bytes of its input. The option not taken is tracking what a campaign finds: 355 megabytes over seven directories, which the regression replay of every check would then read. | decided | 2026-09-14 |
| D-186 | `CREATE INDEX` writes the tree it names, and an `INSERT` keeps it. The b-tree writer carried both kinds of page already — the balance of D-169 names the table case `leaf_data` and the index case is the other one — so what was missing is the walk an index is searched by, the two places the divider of an index differs from a table's, and the order the entries are written in. `tree::insert_entry` takes the values the entry holds beside the bytes of it, because an index tree is ordered by what its entries say and not by a number, and the key of the row stands last, which is what makes the order total. `tree::place_entry` compares the key against every cell of a page and follows the child of the first that stands at or above it, reading an entry that runs onto a chain whole before comparing, because a column of the key may be the part that ran on. A divider between two index leaves is the entry itself, which no leaf keeps a copy of, so the parent's copy gains the four bytes of a child pointer and a divider read out of the parent loses them, which is the `leafCorrection` of `balance_nonroot`. `CREATE INDEX` sorts every entry before it writes any, which is the sorter of `sqlite3VdbeSorterInit`, and asks the balance to fill the left page and leave the right one to the entries still to come, which is `BTREE_BULKLOAD`; an `INSERT` into an index that is already there asks for neither, and the two write different files, which is what nine fixtures hold byte for byte. Building an index over `r` rows is O(r log r) comparisons to sort and O(r) writes to fill; keeping one costs O(log n) per row. Taking an entry out of an index is D-187. The option not taken is filling an index by inserting each entry as it is read: the file would then differ from the one SQLite writes wherever a page splits, and the sort costs one pass over rows already in hand. | decided | 2026-09-14 |
| D-187 | An entry comes out of an index, so a `DELETE` and an `UPDATE` over a table an index is over are written rather than refused. `tree::remove_entry` is `sqlite3BtreeDelete` over an index cursor: the descent stops where a cell equals the key, which an index tree holds on an interior page as well as on a leaf; an entry on a leaf is dropped and the leaf is balanced; an entry on an interior page is replaced by the entry before it, which lies at the end of the right-most leaf under the page the removed entry named, and the leaf is balanced first, the interior page after and only where the balance of the leaf stopped below it. The page above then holds one cell it has no room for while the leaf under it is balanced, which is the `pParent->nOverflow` of `balance_nonroot`: `tree::divider_at` reads the dividers of such a parent by their place among all of them, one place earlier on the page for each that lies after the cell the parent has no room for, and the balance takes that cell as a divider and drops nothing for it. `sqlite3GenerateRowIndexDelete` writes the entries out before the row, because the entries are found through the row, and an `UPDATE` writes each entry again under the term the row is given. An index over the column the rowid is another name for holds the key twice, because that column answers the key, so an entry is built from the values the row answers and not from the record, which stores nothing for that column. Taking one entry out costs O(log n) pages for the descent and O(log n) balances above it. Seven fixtures hold what the pinned shell wrote, of which `index-tall.db` is nine hundred rows whose text runs from four bytes to fifty-seven, so its index is three levels deep and a third of its entries come out of it. The option not taken is marking an entry dead and leaving it on the page: every walk would then weigh entries the table no longer holds, and the pages would never join. | decided | 2026-09-14 |
| D-188 | `fillInCell` writes nought over the four-byte pointer of every overflow page it takes, and `tree::spilled_as` writes it over the page a chain ends on. A page the free list gives back keeps the bytes it held where this transaction is the one that freed it, which is `PAGER_GET_NOCONTENT`, so a chain ending on such a page named as its next page whatever those four bytes held; the reader then asked for a page the file does not hold, or read a page of the tree as part of an entry. Five hundred entries of varying length in one index tree is what shows it, because a tree that size frees pages as its balances join them. Writing the pointer costs O(1) per chain. `every_entry_taken_out_of_an_index_leaves_the_ones_beside_it` puts five hundred entries in a tree under eleven shapes of length, page size and removal order, takes every one of them out again and reads the rest back as it goes, which is what found the defect and what holds it. The option not taken is zeroing every page the free list gives back: the pager keeps the bytes of a page this transaction freed, and a chain needs four of them written and not the page. | decided | 2026-09-14 |
| D-189 | Five defects the third run of SQLite's own test files showed are fixed, and four are the engine. A connection answers a pragma out of what it holds and not out of the file, because `sqlite3Pragma` reads the schema in memory and a file with no table writes nought for its encoding, so `PRAGMA encoding=utf16le; PRAGMA encoding` answered `UTF-8`; `change::Writer::pragma` answers a read out of the header the connection carries, with the count of pages and the free list taken from the pages rather than the header, which a commit has not written yet. `x IN (list)` compares under the affinity of `x` alone, which is `comparisonAffinity` where the right side is a list and not an expression; this crate gave each member the left side's affinity as well, and two text affinities together are `sqlite3CompareAffinity`'s blob, so `SELECT x FROM t1 WHERE x IN (1)` over a `TEXT` column answered nothing. A `USING` column names the leftmost side of the join that holds it, because the join drops the copy the right side carries: `t1 LEFT JOIN t2 USING(a) LEFT JOIN t3 USING(a)` compares `t3.a` against `t1.a` and not against the `t2.a` the outer join left null. `CAST(blob AS text)` reads the bytes under the encoding the file keeps, which is the mirror of `CAST(text AS blob)`, so `crate::value::decoded` stands beside `crate::value::stored` and both are named where the cast is. The fifth is the harness: a file that writes one case in each arm of a conditional names it once per arm, and the interpreter runs one arm, so `suite::cases` keeps the first case of a name and drops the rest; `db eval` outside a case is a file setting itself up, as `execsql` is, and reading it reached four more files. `db null` and `db nullvalue` say what a `NULL` prints as, which two more files write their answers under. SQLite's own files go from 642 cases passing and 17 answering differently to 654 and 9. The option not taken is running the conditional: `ifcapable` and `working_64bit_int` are answered by the build the interpreter was linked against, which no engine of this repository holds. | decided | 2026-09-14 |
| D-190 | `PRAGMA journal_mode` belongs to the connection and not to the first table, so a statement sets it whenever `sqlite3PragmaJournalMode` is asked and not only before a table is there; the page size, the encoding and the vacuuming stay as they were, because the first table was written under them. `PRAGMA journal_mode=wal` turns write-ahead logging on rather than being refused: the two salts a log carries come from SQLite's random source there and are nought here, because a salt tells one generation of a log from another and nothing else reads it, and `change::Writer::logging` is still what a caller that names its own salts uses. A connection already logging answers `wal` and writes no second log; one that is logging refuses every other mode, because a file leaves that mode through a checkpoint, which this crate does not write. A reader of such a connection follows the log beside the file, which is `db::Database::open_with_log`, so the harness reads through it. `suite::cases` reads past the block of an `ifcapable !X` where `X` is a capability this engine has, which is write-ahead logging and UTF-16; every other condition is read inline, because both arms name one case and the first of a name is the one kept. `nmnum ::= plus_num | nm | ON | DELETE | DEFAULT` is what a pragma is set to, so the parser reads those three words where a name is asked for and `PRAGMA journal_mode=delete` is a statement rather than a parse error. SQLite's own files go from 654 cases passing to 678. The option not taken is refusing the pragma and keeping `Writer::logging` the only way in: thirty-two files name the pragma as their first statement, and a connection that cannot answer it stops there. | decided | 2026-09-14 |
| D-191 | `DROP TABLE` and `DROP INDEX` take a table or an index away, which is `sqlite3CodeDropTable`: the rows of `sqlite_schema` that name it go first, because the trees are found through them, and every page of every tree they named goes on the free list after. `tree::destroy` is `sqlite3BtreeDropTable` for a file that does not vacuum itself, and `clear_page` is `clearDatabasePage`: the child of a cell is freed before the chain the cell runs onto, the page under the right pointer after the cells, and the page itself last, so the free list holds the pages in the order the C library put them there. The roots are destroyed largest first, which is what `destroyTable` does so that a file that vacuums itself moves each root once. A table takes its indexes with it, because `sqlite_schema` names the table of an index in `tbl_name`; an index takes the one row of its own name. A name the database does not hold is refused unless the statement wrote `IF EXISTS`. Dropping one tree is O(n) in its pages. Six fixtures hold what the pinned shell wrote: a table of two rows beside one that stays, four hundred rows whose tree has a page above its leaves, two rows that run onto chains, a table an index is over, the index alone, and the table whose root is the last of the file. `PRAGMA count_changes` comes with it, because three of SQLite's own files set it: a connection that counts answers one row of one column for every statement that changes rows, which is how many it changed, and `sqlite3GetBoolean` is what reads the value it is set to. SQLite's own files go from 678 cases passing to 721. The option not taken is leaving the pages where they are and only taking the schema rows out: the file would then hold pages no tree reaches, which is what `PRAGMA integrity_check` calls a leak. | decided | 2026-09-14 |
| D-192 | `suite::cases` reads past the block of an `else`, because the interpreter runs one arm of a conditional and the arm written before it is the one that runs. This is the rule the repeated case name of D-189 was a special case of: `where7.test` writes `where7-1.1.1` in one arm and `where7-1.1.1-noopt` in the other, with two answers for one statement, so the two names hid what the two arms were. An `ifcapable !X` whose block is read past is the one place the `else` carries the arm that runs, so the word is read there and its block is not. Reading one file stays O(n) in its bytes. The corpus goes from 15 379 cases to 15 364 and the cases answering differently from 10 to 9. The option not taken is running the condition: `permutation`, `ifcapable` and `working_64bit_int` are answered by the build the interpreter was linked against, which no engine of this repository holds. | decided | 2026-09-14 |
| D-193 | A transaction spans statements, so `BEGIN`, `COMMIT` and `ROLLBACK` bound one and a connection outside them commits every statement of its own, which is what a connection does. `change::Writer` holds the header the `BEGIN` read, and that header is what the rollback journal of the `COMMIT` records, so the journal holds each page as the transaction found it and not as the last statement found it. `tree::Pages` already kept what a rollback needs — `before` holds what every opened page held when the transaction began and `origin` how many pages the file had — so `Pages::rollback` puts each opened page back, cuts the file to the pages it had, and puts the free list back where it was, which is what playing the journal back does. A `BEGIN` inside a transaction is `Error::Nested` and a `COMMIT` or a `ROLLBACK` outside one is `Error::NoTransaction`, which is what `sqlite3BeginTransaction` and `sqlite3EndTransaction` refuse. `BEGIN DEFERRED`, `IMMEDIATE` and `EXCLUSIVE` are read and answered the same way, because one writer of one file takes the same locks whenever it takes them; `ROLLBACK TO` names a savepoint, which is a statement of its own and is refused rather than read as a rollback of the whole. A transaction costs O(1) beyond the statements in it, and a rollback O(n) in the pages they opened. Three fixtures hold it: `tx-one.db` is the file the same statements write on their own but for the change counter, which counts the transaction once; `tx-grown.db` frees pages and takes one of them back inside the transaction; `tx-back.db` is what a `ROLLBACK` leaves, which is `tx-kept.db` byte for byte. This unblocked twenty-nine of SQLite's own files and took the cases passing from 721 to 763. The option not taken is treating `BEGIN` and `COMMIT` as words that change nothing: a `ROLLBACK` would then keep what the transaction wrote, which is a wrong answer where a refusal is not. | decided | 2026-09-14 |
| D-194 | Setup a file writes as a quoted string rather than a braced block carries a substitution the interpreter fills in, so `suite::cases` stops the file there. `select5.test` is what shows it: the file writes thirty-one rows from a TCL loop, and a harness that reads the `COMMIT` after the loop but not the loop itself runs every case of the file against an empty table. Before D-193 the file stopped at its `BEGIN` and the cases counted as refused; after it the same cases counted as wrong, which is the harness reporting a defect the engine does not have. A file whose setup this harness cannot write is one whose cases it cannot score, which is the rule a case it cannot read already followed. Reading one file stays O(n) in its bytes. The cases answering differently stay at nine and the cases passing at 763. The option not taken is filling the substitution in: `$i` is a TCL variable a loop of the file's own sets, so writing the rows would mean running the file. | decided | 2026-09-14 |
| D-195 | A view is a named statement, so `CREATE VIEW` writes a row of `sqlite_schema` whose type is `view` and whose root page is nought, because a view holds no row of its own, and a name in a `FROM` that is neither a `WITH` term nor a table is answered by the statement the view names, which is what `sqlite3SelectExpand` puts in its place. The statement is answered where the view is named and not when the view is written, so a view over a table reads what the table holds now; the rows stand where a table's rows stand, which is what makes a view join, group and sort like one. `CREATE VIEW v(a,b)` answers its columns under the names the definition wrote and under the statement's own where it wrote none. A view that names itself, directly or through another, is stopped by the count of views the statement is already inside, which is `SQLITE_MAX_VIEW_DEPTH`; a view whose statement this crate cannot read is passed over, and a statement that names it then answers no table, which is the rule an index this crate cannot walk already followed. `DROP VIEW` takes the row away and frees no page, because a view names no tree, and a view is not a table: `DROP TABLE` over one is refused and so is `DROP VIEW` over a table. Answering a view costs what its statement costs, once per place it is named. Three fixtures hold what the pinned shell wrote. SQLite's own files go from 763 cases passing to 789. The option not taken is writing the rows of a view into a tree when it is made: the rows would then be the ones the table held then, which is a wrong answer where reading the statement again is not. | decided | 2026-09-14 |
| D-196 | `ALTER TABLE ... ADD COLUMN` writes the column into the statement of the table and writes no row again, which is `sqlite3AlterFinishAddColumn`: the text between the start of the statement and `addColOffset`, then `, `, then the column as it was written, then the rest. `addColOffset` is the comma the table constraints begin after, and the bracket that closes the columns where the table has none, so a column added to `CREATE TABLE t(n,s,PRIMARY KEY(n))` stands in front of the `PRIMARY KEY` and not behind it; the parser records it where `sqlite3EndTable` records it, so the schema layer carries it and no statement is read twice. A row the table already holds gains nothing, so the column answers what it falls back to, which is the schema format of D-178 and the machinery that format already needed. The column is refused where `sqlite3AlterFinishAddColumn` refuses it: `PRIMARY KEY` and `UNIQUE`, because the index over it would have to hold rows the table already has, and one that may not be nothing and falls back to nothing, because those rows hold nothing for it. Adding a column is O(n) in the bytes of the statement. Three fixtures hold what the pinned shell wrote, of which `alter-typed.db` is the one with a constraint after its columns. SQLite's own files go from 789 cases passing to 797. The option not taken is writing every row again with the column in it: SQLite writes one row of `sqlite_schema` and no row of the table, and a file that differs there is not the file the shell wrote. | decided | 2026-09-14 |
| D-197 | A name a statement writes in quotes is matched with its quotes off, which is `sqlite3Dequote` before `lookupName`: `"a"`, `[a]` and `` `a` `` each reach the column `a`, and a quote doubled inside a name is one quote of the name, so `"""cb"""` is the column `"cb"`. The schema layer already dequoted every name it stored, so the gap was on the reading side alone: the column of an expression, the table and the schema in front of it, the table and the alias of a `FROM`, the names of a `USING`, the name and the columns of a `WITH` term, the alias a result column answers under, the table of a `t.*`, the name an `ORDER BY` and a `GROUP BY` term matches, the collation a `COLLATE` names, the function a call names, and the type a `CAST` names. Dequoting one name costs O(n) in its bytes. `true` and `false` come with it, because `ON true` is what stops SQLite's own join files: a name no table answers to, written without quotes and without a table in front of it, is one where it is `true` and nought where it is `false`, which is `sqlite3ExprIdToTrueFalse`; a column of that name answers instead, and `EP_Quoted` is why the same name in quotes is a column and never a number. Both together unblocked a chained `RIGHT JOIN`, whose make-up walk kept its matches in a list of its own: a row the join at an earlier level answered then counted as matched by nothing, so the join after it answered a row twice. The walk now marks into the list the whole statement keeps, which is the row set `sqlite3WhereRightJoinLoop` keeps. SQLite's own files go from 797 cases passing to 868, and the cases answering differently stay at nine. The option not taken is dequoting in the parser: a `Span` points into the statement and holds no bytes of its own, so a parser that dequoted would have to allocate for every name, including the ones no statement looks up. | decided | 2026-09-14 |
| D-198 | A `WITH` term that reads itself is answered row by row, which is `generateWithRecursiveQuery`: the cores of the term in front of the one that reads it answer the rows the walk starts from, each row the walk takes off the front is the whole of the term while the cores after it are answered, and the rows those answer stand behind it, so the walk ends where the rows do. `UNION` in front of a recursive core answers a row once, against every row the term has answered, and `UNION ALL` answers each. A term whose first core reads it is refused, because the walk would start from nothing, and so is a recursive core under `EXCEPT` or `INTERSECT`, which is what `multiSelect` refuses. A term of `n` rows costs what its recursive cores cost, `n` times. SQLite hands one row on as it answers it, so a term that does not stop is stopped by the `LIMIT` of the statement that reads it; this crate answers the rows of a term into memory before the statement that reads them, which D2 of document 16 records, so a term that answers more than `RECURSION_ROWS` rows is refused with `Error::Recursion` instead. That count is 100 000, which is ten times the largest recursive term of SQLite's own files. SQLite's own files go from 868 cases passing to 873. The option not taken is answering the term as an iterator the statement pulls from: the rows of every `FROM` would then be an iterator, which is the shape D2 decided against. | decided | 2026-09-14 |
| D-199 | `random` and `randomblob` answer bytes a seed draws, because this crate has no operating system under it to seed `sqlite3_randomness` from: `random::Source` is `splitmix64` over a counter, `Writer::randomness` and `Database::seeded` say where it starts, and a connection told no seed draws from nought, so a statement over a given seed answers the same bytes every time and a fixture can hold them. Every statement of a connection that writes draws one word of the connection's own source for the database it opens, so two statements of one connection answer `randomblob` differently. `randomblob(N)` answers one byte where `N` is less than one, which is what `randomBlob` does, and a count above `SQLITE_MAX_LENGTH` is refused. An expression answered against no connection — the expression layer on its own, and a generated column, which SQLite refuses by name — has no source and is refused with `Error::NoRandom`. Drawing `n` bytes costs O(n). This unblocked fifteen of SQLite's own files, whose rows are built from `randomblob`, and took the cases passing from 877 to 888. The option not taken is answering nought bytes: `randomblob` would then be `zeroblob` under another name, and a test that tells the two apart would pass for the wrong reason. | decided | 2026-09-14 |
| D-200 | The schema tree grows past one page: `tree::deepen` is `balance_deeper` over a root that begins a hundred bytes in, which `copyNodeContent` handles by copying the content area of the root where it lies — a cell of page one stands above the hundredth byte already — and then as many bytes from the root's own header as the root's cell pointer array *ends at*, which is the array's offset and not its length, so the child is handed a hundred bytes of what lies after the array as well. `sqlite3StartTable` writes a record of five noughts into `sqlite_schema` and `sqlite3EndTable` writes over it, so a `CREATE TABLE` and a `CREATE VIEW` write the row twice and the page keeps the bytes of the blank record where the row no longer stands; `sqlite3CreateIndex` writes its row once. Both together are what makes the file the shell wrote: `schema-deep.db` is eight tables at a page size of 512, whose schema tree is a root over four leaves, and it is byte for byte what the pinned shell wrote. Deepening a tree costs O(1) pages. SQLite's own files go from 888 cases passing to 893. The option not taken is refusing to grow the schema tree, which is what this crate did: a database of more than a page of schema was then one it could not write. | decided | 2026-09-14 |
| D-201 | `RECURSIVE` says nothing about a `WITH` term: a term that reads its own name reads itself whether the word was written or not, which is what `sqlite3WithPush` decides by the name alone, so a term that names itself under a `WITH` with no `RECURSIVE` is answered row by row as D-198 says and a name the term shares with a table of the schema is the term and not the table. The parser reads the word and keeps nothing, so `ast::Select` no longer carries it. This unblocked seven of SQLite's own files, whose terms read themselves and write no `RECURSIVE`, and took the cases passing from 893 to 900. The option not taken is holding a term to the word: SQLite's own files write a term that reads itself without it, so a crate that refused those would refuse what the C library answers. | decided | 2026-09-14 |
| D-202 | A pragma the file does not hold is answered out of what the connection was told, which is `crate::pragma::HELD`: a table of the name, what a connection told nothing answers, how the value is written, and whether setting it answers a row. `mmap_size`, `locking_mode`, `secure_delete`, `max_page_count`, `journal_size_limit`, `wal_autocheckpoint`, `threads`, `analysis_limit` and `busy_timeout` answer the value they were set to; `query_only`, `read_uncommitted`, `automatic_index`, `defer_foreign_keys`, `foreign_keys`, `recursive_triggers`, `reverse_unordered_selects`, `trusted_schema`, `synchronous`, `temp_store`, `cache_size` and `default_cache_size` answer nothing when set and what they hold when read. `mmap_size` stands at nought whatever a statement sets it to, because this crate maps no file, and `data_version` stands at one, because a write by another connection is what raises it and this crate has no other connection. A database being read answers what a connection told nothing answers, because a database read here was told nothing. The page size, the encoding and the auto-vacuum setting written after the first table change nothing rather than being refused, which is what `sqlite3Pragma` does for the first two and what leaves the third to `VACUUM`. Answering one pragma is O(1) over the table. SQLite's own files go from 900 cases passing to 914. The option not taken is accepting those pragmas and answering no row: a case that reads one back would then be scored wrong rather than refused, which is a worse answer than a refusal. | decided | 2026-09-14 |
| D-203 | `CREATE TABLE name AS <select>` writes a table whose columns are the ones the statement answers: `sqlite3ColumnsFromExprList` names them and `sqlite3SubqueryColumnTypes` gives each the type its affinity is written as, so the statement of the table is built rather than taken from the text, which is `createTableStmt`. A name another column already carries gains `:1`, `:2` and so on; `true` and `false` are not names a column may carry, so a column of either is `column` and its place; a name is written in quotes where it begins with a digit, is a keyword, carries a byte other than a letter, a digit or an underscore, or is empty; and the statement is written one column per line where the names together take fifty bytes or more. The rows are the ones the statement answered, written under the affinities the columns were given. A name that collides more than four times is answered `:5` where SQLite answers a random number, which is the one place the file differs. Answering the statement costs what the statement costs and writing `n` rows costs O(n log n). Eight fixtures hold what the pinned shell wrote, of which `as-typed.db` reaches every affinity, `as-quoted.db` every reason a name is quoted, `as-wider.db` the layout of one column per line, and `as-vacuum.db` a file that vacuums itself. The naming brought a defect with it: a name no table answered to was answered under the name of the key, so `SELECT true FROM t` answered a column called `rowid`; only one of the rowid's three names is answered that way now, and every other name is answered under the name that was written. SQLite's own files go from 914 cases passing to 921. The option not taken is keeping the text of the statement as written: SQLite writes the statement it built, and a file that holds another text is not the file the shell wrote. | decided | 2026-09-14 |
| D-204 | `CREATE TRIGGER` writes one row of `sqlite_schema` whose type is `trigger` and whose root page is nought, because a trigger holds no row of its own, and the text it holds is the words `CREATE TRIGGER` and then the text from the name to the `END`, which is what `sqlite3FinishTrigger` writes, so `TEMPORARY` and `IF NOT EXISTS` are not in what the file holds. A statement that changes rows reads the triggers of the table once and runs them per row, which is `sqlite3CodeRowTrigger`: the `BEFORE` ones run before the row is written and the `AFTER` ones after, each set in the order `sqlite3FinishTrigger` left them, which is the one made last first. The body reads the row as `new` and `old`: a `DELETE` carries `old` alone, an `INSERT` carries `new` alone and an `UPDATE` carries both, and a name with a schema in front of it names neither, because they are cursors and not tables. `UPDATE OF` runs the trigger only where the statement writes one of the columns it names. `RAISE(IGNORE)` passes the row over and every other `RAISE` refuses the statement, which then leaves the file as it found it, because a statement outside a transaction rolls its pages back where it refuses what it was given. A trigger that reaches itself runs once, which is the `P5` of `OP_Program` where `PRAGMA recursive_triggers` is off. SQLite runs a body as a program of its own machine and bounds it at `SQLITE_MAX_TRIGGER_DEPTH`, a thousand; this crate runs the body where the statement runs, so the count is the frames the stack holds and `TRIGGER_DEPTH` is thirty-two, the bound a view that names itself already carried. A trigger written `INSTEAD OF` is refused, because only a view carries one and this crate writes no row of a view. Running a trigger costs what its body costs, once per row. A `DROP` takes away the row of its own kind, which is what `sqlite3DropIndex` and `sqlite3DropTriggerPtr` name, and a `DROP TABLE` takes the triggers on the table with it. Six fixtures hold what the pinned shell wrote. SQLite's own files go from 921 cases passing to 964, and the cases answering differently go from nine to sixteen: seven of `insert3.test` read a `UNIQUE` this crate does not keep, which is the gap `conflict3.test` and `tkt2832.test` already name. The option not taken is running the body as a program of a machine: this crate answers rows by walking them, which D2 of document 16 decided, and a machine for triggers alone would be a second engine. | decided | 2026-09-14 |
| D-205 | A `UNIQUE` or a `PRIMARY KEY` on a table gives the table an index of its own: `sqlite3CreateIndex` builds one per constraint while the `CREATE TABLE` is parsed, so the row of the index stands between the blank record `sqlite3StartTable` writes and the row `sqlite3EndTable` writes over it. The name is `sqlite_autoindex_<table>_<n>`, `n` counting the constraints from one in the order they are declared, and the row holds no statement, so the columns and their collations are read back from the constraint the name points at rather than parsed. An `INTEGER PRIMARY KEY` gains no index, because the key of the row is the constraint, and a `UNIQUE` over that same column gains one. A constraint over the columns of one already there gains no second index, and the index there takes the clause where it carries none, which is the loop `sqlite3CreateIndex` runs over the indexes of the table; the order of a column is left out of that comparison. The schema cookie is raised once per statement and not once per index. `sqlite3GenerateConstraintChecks` holds every row to every unique index before the row is written: `ABORT` refuses the statement and leaves the file as the statement found it, `FAIL` stops the statement and keeps the rows already written, `IGNORE` passes the row over, and `REPLACE` takes the row that is there out with its entries and runs the `DELETE` triggers on it, which is `sqlite3GenerateRowDelete` under `OE_Replace`. The clause is the one the statement names, and the one the index carries where the statement names none. A key one of whose columns is nothing constrains no row, which is what makes a `UNIQUE` hold over values and not over rows. `sqlite3CompleteInsertion` writes the entry of every index before the row itself, so a row whose entry runs onto a chain takes the pages of the entry first; writing the row first left `key-wide.db` a page order the shell never wrote. Holding one row to one index costs O(log n) in the rows of the table, and a statement over `n` rows costs O(n log n) per index. Eight fixtures `key-*.db` hold what the pinned shell wrote, of which `key-text.db` carries a collation, `key-wide.db` an entry and a row that both run onto chains, and `key-vacuum.db` a file that vacuums itself. Two defects of the port fell out of them: `tree::balance_nonroot` collapsed a root into its child without the guard `balance_nonroot` holds the root header to, so a child whose content reached below the hundredth byte was written over the database header; and `Pages::point_cells` read an index leaf as a table leaf, so an index of a file that vacuums itself could not be pointed at. SQLite's own files go from 964 cases passing to 968 and the cases answering differently from sixteen to seven, because `conflict3.test`, `tkt2832.test` and the seven of `insert3.test` read the constraint this crate now keeps. The option not taken is keeping the constraint in the table and writing no index: SQLite writes the row of the index into `sqlite_schema`, and a file without that row is not the file the shell wrote. | decided | 2026-09-14 |
| D-206 | A text of comments, whitespace and semicolons alone holds no statement: `crate::parse::blank` says so, `Database::query` answers no row for one, and `Writer::run` writes no byte and raises no counter of the header, which is what `sqlite3_prepare_v2` answering no statement makes `sqlite3_exec` do. The suite reads the first word of a statement with the comments taken out, so a `SELECT` written under a comment reaches the connection that reads and a `CREATE TRIGGER` written under one keeps its body whole up to the `END`. Reading the text costs O(n) in its bytes. SQLite's own files go from 968 cases passing to 973. The option not taken is refusing a text that holds no statement, which stopped four files at a comment. | decided | 2026-09-14 |
| D-207 | `ANALYZE` counts what the tables and their indexes hold into `sqlite_stat1(tbl,idx,stat)`, which the statement makes where the database holds none. A run over the whole database counts the tables with the one made last first, which is the order `sqliteHashFirst` answers, and `analyzeOneTable` counts no table whose name the word `sqlite_` begins, which is also why a name that begins with that word names a table whatever the database holds under it; a run that names a table counts that table, and one that names an index counts the table of that index and writes the row of that index alone. The rows a run before this one wrote are taken out first, which `openStatTable` does for every row of a whole database and for the rows of the table of one table. The row of an index holds the number of rows and then, per prefix of the index columns, the number of rows a lookup of that prefix answers, which `statGet` writes as `(rows + distinct - 1) / distinct`; the indexes are written with the one made last first, which is the order `pTab->pIndex` carries. A table that carries no index at all carries one row that names no index and holds the number of rows, and a table with no row carries none. The counts change no answer this crate gives, because a statement here is answered by walking the rows and not by a plan chosen from the counts; what they are for is the files SQLite's own tests read back. Ordering the entries of one index costs O(n log n) and counting them O(n k) in the `k` columns. Ten fixtures `stat-*.db` hold what the pinned shell wrote, of which `stat-again.db` is a second run over a table a first one counted and `stat-index.db` a run held to one index. SQLite's own files go from 973 cases passing to 2145, of which 1168 are `joinD.test`, whose first statements the word `ANALYZE` ended. The option not taken is `sqlite_stat4`, which holds samples of the keys and which `ANALYZE` writes only where the build asks for it. | decided | 2026-09-14 |
| D-208 | A term of a `WHERE` is read on the level of the walk that answers it, which is `sqlite3WhereSplit`: the top-level `AND` spine is split, and each term is placed on the last side it names a column of, so a term is read once per row of that level and not once per row of the product. The term is read again where the statement is answered, so the placement is a cost and not an answer. A term this walk does not answer from the sides alone stays where the statement is answered: a function, which may answer differently each time it is read, a statement of its own, an `EXISTS`, an `IN` over a statement or over a table, a bound value, a name no side answers, and a name two sides answer. A `RIGHT` or a `FULL` join keeps the rows of its own side the walk matched nothing to, and a row is marked matched where the levels under it are read, so a term the levels above such a join answer is read on the level of that join instead, where every row it would mark is marked already. A side whose `ON` compares one of its columns against a column of a side already read is read by an index over that column, which is the equality case of `sqlite3WhereLoopAddBtree`, with the key read once per row of that side rather than once per statement; the `ON` is read again for every row the descent answers, so a plan that answers more rows than the key names still answers the rows of the statement. An index held in another order, or under another collation than its column compares under, names no key; a key of nothing reaches no entry, so the side is read out of its own tree; and a `RIGHT` or a `FULL` side is read out of its own tree, because the two walks of such a join tell the rows they matched by where the rows stand. A level reads `t` terms at what the `t` terms cost, and a descent costs O(log n) against a scan of O(n). The first hundred cases of `joinD.test` go from 120 seconds to 48, and SQLite's own files answer the same 2145 cases in sixteen minutes rather than thirty. The option not taken is answering a term only where it is placed: a term of a function read twice answers twice, and reading the whole `WHERE` once more is what makes every placement safe. | decided | 2026-09-15 |
| D-209 | `REINDEX` writes the entries of an index again out of the rows they belong to, which is `sqlite3RefillIndex`: the tree is cleared to an empty leaf page, which is `sqlite3BtreeClearTable`, and the entries are then written in key order through the same sorter a `CREATE INDEX` writes through. `REINDEX` alone writes every index of the schema again; a name is read as a collation first, and then every index one of whose columns is held in that collation is written again, as a table second, and as an index last, which is the order `sqlite3Reindex` reads it in, and a name none of the three carries is a refusal. The indexes are written with the one made last first, which is the order `reindexDatabases` reads them in. Clearing a tree of `n` pages costs O(n) and writing `m` entries again costs O(m log m). Five fixtures `re-*.db` hold what the pinned shell wrote, of which `re-collate.db` writes again the indexes of one collation and `re-key.db` the index a `PRIMARY KEY` carries. An index whose entries run onto a chain is not among them: the pages the free list gives back are reused in another order than the C library reuses them, so the tail of one overflow page holds what this crate's free list left rather than what the C library's left, in bytes no row reads. SQLite's own files go from 2145 cases passing to 2157, of which six are files whose first statement is `ANALYZE sqlite_master`. The option not taken is writing the entries again without clearing the tree first, which leaves the pages of the old tree on no list at all. | decided | 2026-09-15 |
| D-210 | `PRAGMA integrity_check` answers one row per problem the file holds and one row of the word `ok` where it holds none, which is what `sqlite3Pragma` under `PragTyp_INTEGRITY_CHECK` answers, and `PRAGMA quick_check` is the same walk without holding an index to the rows it is over. The walk marks every page it reaches — from a tree the schema names, from the free list, from a pointer map, and page one, which is the root of the schema's own tree — and a page it reaches a second time answers `2nd reference to page <n>` and is not walked again, so a file whose pages name each other in a ring is walked once round it and no further; a page no walk reached answers `Page <n>: never used`. Every row is held to the columns that may not be nothing, which answers `NULL value in <table>.<column>`, and every index to the rows it is over, which answers `row <key> missing from index <name>`, `wrong # of entries in index <name>` and `non-unique entry in index <name>`. One run answers a hundred problems and no more, which is what `PRAGMA integrity_check(N)` takes at its default. Walking the file costs O(p) in its pages and holding a table of `n` rows to an index costs O(n log n), because the entries are put in order once and each row is then looked up in them. The checks the C library makes that this one does not are the ones about the bytes of a page: the free space of a page, the order of the keys on it, the depth of its children, and the length of an overflow list; a file this crate cannot read whole refuses the pragma rather than answering a problem in it. SQLite's own files go from 2157 cases passing to 2182. The option not taken is answering `ok` wherever the file can be read: the tests that ask for the check ask it of a file they have just written, and a check that answers `ok` whatever it reads holds nothing to anything. | decided | 2026-09-15 |
| D-211 | A `CREATE` of a name the schema already holds is refused, which `sqlite3StartTable` and `sqlite3CreateIndex` do, unless the statement writes `IF NOT EXISTS`, which writes nothing instead. A table, an index and a view are one namespace, and a trigger is named in its own, so a trigger and an index may share a name and a table and an index may not. This is what `PRAGMA integrity_check` found: a second `CREATE TABLE` of one name wrote a second row of `sqlite_schema` and a second tree, and the pages of the first were then named by nothing, which the check answered as `Page <n>: never used`. Looking one name up costs O(n) in the rows of the schema. | decided | 2026-09-15 |
| D-212 | The harness writes a real with fifteen significant digits, which is what `tester.tcl` sets `tcl_precision` to and therefore what the interpreter wrote into the answers the files hold. This crate renders a real with seventeen, which is what the C library renders one with and what `db-sqlite` answers; the difference is the interpreter between the library and the file, so it belongs in the harness and not in the engine. `fpconv1.test` is the one file that asks the library to render instead, through `sqlite3_db_config db FP_DIGITS`, and its two cases at seventeen digits are the two this harness answers differently. SQLite's own files go from 2182 cases passing to 2184 and from eight answering differently to six. The option not taken is rendering with seventeen digits everywhere, which scored every real of every file against digits the file never held. | decided | 2026-09-15 |
| D-213 | A step the harness cannot run stops the file only where the text it stands for may have changed the database. A `do_test` whose body runs more than one `execsql`, a `do_catchsql_test`, and a statement a substitution stands in were all read as stopping the file, because the cases after such a step read what it wrote; a step that only reads writes nothing, so the cases after it read what the file wrote and the file runs on with the step itself counted refused. What says a step may have written is a word that names a statement that writes, a command that opens or closes a connection, and a substitution, which stands for a command this harness cannot read. A step wrongly read as reading alone leaves the cases after it scored wrong rather than refused, which shows as a failure and not as a pass. SQLite's own files go from 2184 cases passing to 2315, and the five cases that then answered differently were three of `selectA.test` and two of `with5.test`, which D-214 and D-215 record. Reading one step costs O(n) in its bytes. The option not taken is running the steps: a `do_test` body is TCL, and running it is an interpreter. | decided | 2026-09-15 |
| D-214 | The `ORDER BY` of a compound compares under what its term was written with, which `multiSelectOrderBy` reads off the term and not off the column the term counts to. A `COLLATE` on a term of the `ORDER BY` of a `UNION` was read and then dropped, so the rows were ordered under the collation of the column instead; three cases of `selectA.test` read the order that leaves. Sorting costs what it cost before. | decided | 2026-09-15 |
| D-215 | The rows of a recursive term wait in a queue, and the `ORDER BY` of the term says which of them is taken next while the `LIMIT` says how many are taken at all, which is `generateWithRecursiveQuery`. A term that wrote no `ORDER BY` takes its rows in the order they were written, so the walk steps through the queue; a term that wrote one reads the queue for the smallest row each turn, which costs O(n) a turn and O(n²) over the term. An `OFFSET` passes rows over without holding the walk back, because a row passed over is still read by the cores that read the term. Two cases of `with5.test` read the four smallest rows of a closure. The option not taken is ordering and bounding the rows once the walk has ended, which answers the same rows for a term that ends on its own and never ends for one the `LIMIT` is what ends. | decided | 2026-09-15 |
| D-216 | `sqlite_schema` is a table a statement may read: `sqlite3InitOne` builds it in memory out of the five columns every row of it is written with, so this crate holds one of its own, at page one and under the statement `CREATE TABLE sqlite_master(type text,name text,tbl_name text,rootpage int,sql text)`. It answers to `sqlite_master`, `sqlite_schema`, `sqlite_temp_master` and `sqlite_temp_schema`, which the lookup folds into the one name it is held under. It is not one of the tables `Database::tables` answers, because it is read out of the schema rather than written in it, so `ANALYZE` counts it into nothing and the integrity check reaches page one once. `sqlite3SchemaMayNotBeModified` keeps an `INSERT`, an `UPDATE`, a `DELETE` and a `DROP` off it. Reading it costs what reading any table costs. SQLite's own files go from 2322 cases passing to 2330. The option not taken is answering it out of the rows of the schema without a table, which is a second way to read a table and a second place for every clause to work. | decided | 2026-09-15 |
| D-217 | A row of `sqlite_schema` holds the words `CREATE` and the kind and then the text from the name to the end, which is what `sqlite3EndTable` and `sqlite3CreateIndex` write: `TEMP` and `IF NOT EXISTS` stand before the name, so no row holds either, and `UNIQUE` stands after `CREATE`, so the words are `CREATE UNIQUE INDEX`. The text was kept as the statement wrote it, so a `CREATE TEMP TABLE` held a statement no file the C library writes holds. Building one costs O(n) in the bytes of the statement. | decided | 2026-09-15 |
| D-218 | A key that counts up is counted in `sqlite_sequence(name,seq)`, which `sqlite3StartTable` makes with the first table that carries `AUTOINCREMENT` and which `autoIncrementEnd` writes the largest key that table ever held into. `autoIncBegin` reads that count, so the next key is past the largest the table ever held and not past the largest it holds: a key given back by a `DELETE` is not given out again. A key the statement names that is smaller than the count leaves it, and `sqlite3CodeDropTable` takes the row away with the table. Reading the count costs O(n) in the rows of `sqlite_sequence` and writing it O(log n). SQLite's own files go from 2330 cases passing to 2333. | decided | 2026-09-15 |
| D-219 | A window function is answered over the rows a statement has already filtered and grouped, once per row, and the walk over an expression looks the answer up rather than working it out, which is how an aggregate is already answered. The parser reads `FILTER (WHERE x)` and `OVER` after the closing bracket of a call and `WINDOW name AS (...)` after a `HAVING`; each of the three words is a name anywhere else, which is what `analyzeWindowKeyword`, `analyzeOverKeyword` and `analyzeFilterKeyword` of `tokenize.c` decide by the words around them. A window names its partition, its order and its frame; `OVER name` is the named window whole, frame and all, which `sqlite3WindowUpdate` copies, while `OVER (name ...)` builds on it and `sqlite3WindowChain` refuses one that overrides the named window's partition, order or frame. A frame is counted in `ROWS`, in `RANGE` or in `GROUPS`, and `sqlite3WindowAlloc` refuses one that ends before it begins. `RANGE` counts the offset onto the term of one row and compares it against the term of the other, leaves a term that is text or a blob alone, which frames such a term's peers and nothing else, and answers a comparison against a null by rule where the term's nulls sort after its values, which is `windowCodeRangeTest` whole. The eleven built-in window functions and every aggregate answer over a window; `DISTINCT` is refused, and a `FILTER` is taken for an aggregate and refused for the rest. A statement that writes no `ORDER BY` of its own answers its rows in the order the windows' terms sort them, because `sqlite3WindowRewrite` answers them out of a sorter over the first window's partition and order. Sorting a partition costs O(n log n), finding the frame of a row costs O(log n) by halving and O(n) for a term whose nulls sort after its values, and stepping an aggregate over a moving frame costs O(n·m) over a partition where the C library is O(n). 13 667 statements over one table, every function over every frame over six windows, answer the same rows in both engines, and the refusals are the same too. The seventeen `window*.test` files go from no case passing to 1906. The option not taken is rewriting a window into a nested statement the way `sqlite3WindowRewrite` does, which is the same answer through a second kind of plan. | decided | 2026-09-15 |
| D-220 | A bracketed command a step the harness cannot run holds leaves the step reading alone where the command is one of the few that read — `db eval`, `execsql`, `catchsql`, and the list and string commands — and stops the file where it is anything else. A variable no longer stops a file on its own: the text a variable stands for was written by a step of the same file, and that step, which holds the text, is what stops it. `do_execsql_test NAME { SQL }` written with no answer after it expects no row, which is what the interpreter reads an argument it was not given as, and an answer of one word needs no braces around it, so what stands on the line the case ends on is that word. D-213 records why a step that reads alone does not stop the file, and D-222 what this got wrong. SQLite's own files go from 2333 cases passing to 4715, this and D-219 together, and the cases that then answer differently go from six to 53, none of them a window: `whereD.test` and `limit.test` read fourteen each, `select4.test` seven, and `select5.test` and `affinity3.test` four each. Reading one step costs O(n) in its bytes. | decided | 2026-09-15 |
| D-221 | A column of a compound converts before a comparison what every core of it leaves it converting, which is `sqlite3SubqueryColumnTypes`: the affinity is the first core's, or the first core after it that has one, and a core beyond that one which answers a class the affinity would convert takes the affinity away. Which classes a core's column may answer is `sqlite3ExprDataType` read off the expression and not off a row: a literal answers its own class, a call or a bound value answers any of them, and a column answers what its affinity keeps. So a view over `SELECT id FROM t(id INT) UNION SELECT id FROM u(id TEXT)` converts nothing, and a join against a `TEXT` column compares the integer one against text as the classes sort rather than as numbers, which is the four cases of `affinity3.test`. A table made from such a statement is written with no type for that column, so the table the C library writes and the table this crate writes are the same bytes. Reading one column costs O(n) in the cores of the compound. | decided | 2026-09-15 |
| D-222 | A step the harness cannot run says for itself whether it may have written, and the text read past it only adds to what it says. `execsql "..."` written as a quoted string is such a step: the harness reads no text past the command word, so the statements the string holds are unknown and the step stops the file, which D-220 had let the empty text lower to a step that writes nothing. `limit.test`, `select4.test` and `select5.test` each write their rows from a TCL loop of that shape and then read them, so each ran its cases against a table of no rows: SQLite's own files go from 4715 cases passing to 4611 and from 53 answering differently to 27. | decided | 2026-09-15 |
| D-223 | SQLite's own test files are run by the `tclsh` of the machine, which reads `tools/suite/tester.tcl` — this repository's own — and reaches the engine over a socket on the loopback address. A file of the suite drives the whole of TCL: a `proc` a `foreach` calls with a variable an `expr` counted, which is 2927 `set`, 1030 `proc`, 650 `foreach` and 484 `if` commands across the files, and a harness that reads nine of those commands counts every case behind one of them refused. The suite's own tester is not read: it is 2626 lines calling eighty commands `testfixture` links in, which are the C library's internals, and a file reads the tester by `source $testdir/tester.tcl` where `$testdir` is `[file dirname $argv0]`, so a runner that names itself `argv0` hands every file the tester of this repository. The line is a socket because a file writes to standard output and a request written to the same stream is read as that text. The harness holds one writer per path a connection opened, so `db` and `db2` over `test.db` read one database. Each file runs in a process of its own, ended after sixty seconds, because a statement the engine answers slowly cannot be stopped from inside; the cases the file ran before it was ended are counted, which is what each of them is written out for as it is scored. A command that needs the C library's internals raises rather than answering nothing, so a case that reads its answer is refused and not scored against a value this harness made up. SQLite's own files go from 17 724 cases the harness knew about to 61 784 of 636 files, and from 4616 passing to 41 970, with 6517 answering differently and 13 297 refused. `docs/17-the-suite-on-the-machine.md` holds the steps. The option not taken is a TCL interpreter written here, which is the words, the substitutions, `expr`, the control commands, the list and string commands and `format` — a language and not a feature, and every one of them a way to score a case wrong. | decided | 2026-09-15 |
| D-224 | A foreign key holds the rows of both tables it names, and every check runs when the statement runs. The row that points is checked as `I.1` of `src/fkey.c` checks it: an `INSERT` or an `UPDATE` whose key names no row of the table pointed at is refused with `FOREIGN KEY constraint failed`, and a key any column of which is null is held by nothing. The row pointed at is checked as `D.2` checks it: a `DELETE` or an `UPDATE` of it carries the action the key names over the rows that point at it, and writing those rows runs the keys that point at the table they belong to, so a cascade reaches two tables away. `CASCADE`, `SET NULL` and `SET DEFAULT` write those rows; `RESTRICT`, `NO ACTION` and a key that names neither refuse. The columns pointed at are the primary key of the table pointed at or a unique index over exactly those columns, which is `sqlite3FkLocateIndex`, and anything else is `foreign key mismatch`, raised when the statement runs rather than when the table is created, so a table may name a table no `CREATE TABLE` has written yet. A key that names no columns points at the primary key in the order that primary key was written, not the order of the table. `PRAGMA foreign_keys` turns the two checks on, `PRAGMA foreign_key_list` answers the keys of a table newest first with `to` null where the key named no columns, and `PRAGMA foreign_key_check` answers one row per row that points at no row whatever `PRAGMA foreign_keys` says. `DEFERRABLE INITIALLY DEFERRED` is read from the text and answered by `foreign_key_list`, and the check it asks for at the end of the transaction is open. A refusal now carries the text the C library writes for it where the engine names one — the two above, `UNIQUE constraint failed` and the three transaction messages — which the harness answers a `catchsql` with; `docs/17-the-suite-on-the-machine.md` step T6 holds the names those messages carry. Finding the row a key points at costs O(n) in the rows of the table pointed at, and the check on the row pointed at costs O(n*m) in the rows of both tables, because neither reads an index. SQLite's own files: `e_fkey.test` goes from 162 cases passing to 570 of 641, `fkey2.test` to 821 of 937, and `without_rowid3.test` from 81 to 640 of 907, and the suite as a whole from 41 970 cases passing to 44 451 of 63 280. | decided | 2026-09-15 |
| D-225 | A row of values is compared against a row of values, which is `sqlite3ExprCodeVectorCompare`: an equality holds where every pair holds and is unknown where a null stands in a pair no other pair settles, and an order is settled by the first pair that is not equal, or by the last pair where every pair before it is equal. `=`, `<>`, `<`, `<=`, `>`, `>=`, `IS`, `IS NOT`, `BETWEEN` and `IN` take a row; every other operator is a misuse, and so is a row compared against one value, a row compared against a row of another width, and a row written where one value belongs. A statement answering as many columns as the row is wide stands for a row, so `(a, b) = (SELECT x, y FROM t)` compares against the first row that statement answers, or against nulls where it answers none. The misuse is refused before the first row of a table is read, which `crate::eval::rows_placed` reads off the arena in O(n) in its nodes: a row of a `VALUES` and a row under one of those ten operators stands where it may, and every other row is `row value misused`. A view is read when a statement names it, so a row misused in the body of a view refuses there. A statement used as one value carries the affinity and the collation of the column it answers, which is `sqlite3ExprAffinity` of a `TK_SELECT`, so `a = (SELECT x FROM u)` over a text column and a column of no type converts neither side; this crate compared under no affinity before, which is the eleven cases of `rowvalue9.test`. Comparing two rows of `n` values costs O(n), and looking a row up among `m` rows costs O(n*m). SQLite's own files: `rowvalue2.test` goes from 4 cases passing to 2061, `rowvalue.test` from 11 to 155, `rowvalue9.test` from 32 to 91, `rowvalue3.test` from 7 to 34, `rowvalue4.test` from 2 to 11 and `rowvalueA.test` from 1 to 11. The option not taken is reading an index for a row compared against a row, which answers the same rows in fewer steps and is a plan and not an answer, so D2 of `docs/16-sqlite-in-rust.md` leaves it for later. | decided | 2026-09-15 |
| D-226 | The column a `USING` names stands for the side that filled it, which is the first of the sides read before this one that holds the name and answers something other than nothing. A `RIGHT JOIN` keeps a row of the side on the right with every side on its left empty, so the column of a side on the left is nothing and the name stands for the side that kept the row; a join written after it compares against that value, which `sqlite3ExprCodeVectorCompare` reads off the coalesced column. This crate read the leftmost side alone, so `t1 RIGHT JOIN t4 USING(a) JOIN t5 USING(a)` compared t5 against the empty t1 and answered no row for every row the `RIGHT JOIN` kept. The value a statement answers for such a column was already the coalesce, which is why a join of two tables answered what the C library answers and a join of three did not. Reading one row of one side costs O(n) in the sides read before it. SQLite's own files: `joinB.test` goes from 128 cases passing and 320 answering differently to 448 and none, and `join9.test` from 33 to 66. | decided | 2026-09-15 |
| D-227 | Tables written inside brackets in a `FROM` stand for a statement that answers every column of them, which is `seltablist ::= stl_prefix LP seltablist RP` of `parse.y`: the brackets hold the joins between those tables together, and the join written after the brackets is against what that statement answers. So `t1 JOIN (t2 LEFT JOIN t3 USING(a)) USING(a)` reads `t2 LEFT JOIN t3` first and joins `t1` against its rows, where this crate refused the statement before because it read the bracket as a statement and found none. The option not taken is flattening the tables into the list around them, which `parse.y` does for a bracket holding one table alone; a bracket holding a join carries an order of its own, and a statement is what keeps it. A column written with the name of a table inside the brackets does not reach it, which `SF_NestedFrom` keeps reachable in the C library: `SELECT * FROM t5 JOIN ((t4 JOIN (t5 JOIN t6)) t7)` is `ambiguous column name` there and one row here, which is the one case of `join.test` this costs. SQLite's own files: `joinB.test` goes from 448 cases passing to 512 of 512, `join9.test` from 66 to 90, and `joinI.test` and `join2.test` one case each. | decided | 2026-09-15 |
| D-228 | A statement standing for the tables inside brackets in a `FROM` answers every column of every one of them, each under the name of the table it came from, which is `SF_NestedFrom`: `t2.a` reaches the table `t2` inside the brackets, the name the brackets carry reaches the same columns, and a table inside them that carries a name of its own answers under that name alone. The column a `USING` or a `NATURAL` matched is answered as well and marked as one a `*` leaves out, so `SELECT *` answers it once and `t3.a` still reaches the copy of the side that hides it. Each column of such a statement is answered as its own table holds it, and a bare name over it answers the first of the columns of that name that is not nothing, which is the column a `RIGHT` or a `FULL` join inside the brackets filled; `crate::db::project` filled every column before the brackets held a join, which answered `t2.a` as the row of the side that filled it rather than as nothing. Reading one column of such a statement costs O(n) in the columns of the tables inside the brackets. SQLite's own files: `joinC.test` goes from no case running to all 256 passing, and `joinB.test`, `joinD.test`, `joinE.test` and `joinF.test` stay at every case passing. | decided | 2026-09-15 |
| D-229 | A refusal carries the name it is about, so the engine writes the message the C library writes: `no such table: NAME`, `no such column: NAME`, `no such function: NAME`, `wrong number of arguments to function NAME()` and `no such collation sequence: NAME`. `crate::eval::Error` and `crate::db::Error` hold the name as the statement wrote it, dequoted, with the schema in front of it where one was written, and `Error::message` of each writes the text; a refusal the walk raises for a side it put there itself carries no name, because no statement named it. The 1224 `do_catchsql_test` cases of SQLite's own files compare the message, which `docs/17-the-suite-on-the-machine.md` step T6 holds: `select1.test` goes from 97 cases passing to 103, `misc1.test` from 63 to 64 and `index.test` from 66 to 69. Carrying one name costs one allocation per refusal, which only a refused statement pays. The option not taken is a message built where the statement is answered, which needs the name to reach that far anyway. | decided | 2026-09-15 |
| D-230 | A row is held to the columns that refuse nothing, to every `CHECK` of the table and to the key it is written under, which is `sqlite3GenerateConstraintChecks` in that order, before the keys of the indexes are held. A column written `NOT NULL` refuses a row that holds nothing there with `NOT NULL constraint failed: table.column`; a `CHECK` that answers false refuses the row with `CHECK constraint failed:` and the name the `CONSTRAINT` gave it, or the text of the expression as the statement wrote it, a `CHECK` that answers nothing holding; and a key the table already holds refuses the row with `UNIQUE constraint failed:` and the columns the key is over. This crate wrote such rows: neither `NOT NULL` nor `CHECK` was read at all, and a row written under the key of a row the table holds was written over it where the column the key is another name for was written. What a refused row does is what the statement says, and what the column's own constraint says where the statement says nothing: `IGNORE` passes the row over, `REPLACE` writes what a column that refuses nothing falls back to and refuses where it falls back to nothing, and `FAIL` stops the statement where it stands and keeps the rows it wrote, which `crate::change::Writer` now marks by a flag of its own rather than by a refusal of its own, so the message of the constraint is written for `FAIL` as well. Holding one row costs O(c) in the columns and the checks of the table, and reading the key costs O(log n) in the rows. SQLite's own files: `conflict.test` goes from 65 cases passing to 95, `conflict2.test` from 7 to 21, `intpkey.test` from 47 to 56 and `update.test` from 105 to 113. | decided | 2026-09-15 |
| D-231 | `PRAGMA ignore_check_constraints` leaves every `CHECK` of a table unread, which is `SQLITE_IgnoreChecks` of `sqlite3GenerateConstraintChecks`, and `PRAGMA writable_schema` is read and answered rather than refused, so a file that sets it runs on. A name written in double quotes that no table answers is refused as `no such column: "NAME" - should this be a string literal in single-quotes?`, which is the question `resolveExprStep` asks, and a name written with a table in front of it is refused with both names, their quotes taken off. The tester answers `sqlite3_db_config` with the value it was given and `verify_ex_errcode` with nothing, because this engine holds no extended error code and a case scored against one made up here would read as a defect of the engine. SQLite's own files: `check.test` goes from no case running to 41 passing and `notnull.test` from 3 to 69. | decided | 2026-09-15 |
| D-232 | The date and time functions are `src/date.c` ported whole: `date`, `time`, `datetime`, `julianday`, `unixepoch`, `strftime` and `timediff`. A moment is held as the julian day number times 86 400 000 and as the year, month, day, hour, minute and second it was written as, and every function reads its first argument as a moment, applies the modifiers after it in the order they were written, and writes the moment out in its own shape. The modifiers are the twelve of the C library: `NNN days` and the five words beside it, `start of day`, `start of month`, `start of year`, `weekday N`, `unixepoch`, `julianday`, `auto`, `ceiling`, `floor` and `subsec`, and the two shapes `(+|-)HH:MM:SS.FFF` and `(+|-)YYYY-MM-DD HH:MM`. `now`, `localtime` and `utc` ask for a clock this crate is given none of, so a call that names one of them answers nothing, which is 2 cases of `date.test` and the whole of `date4.test`, which compares against the C library's own `strftime`. A month that runs past the days it has rolls forward, and `floor` rolls it back, which is `computeFloor`; the year is written with the sign counting toward its four digits, which is `%04d` of C. Reading one moment costs O(m) in the modifiers, and each of them costs O(1). SQLite's own files: `date.test` goes from no case running to 1217 of 1219 passing, `date5.test` from none to 874, `date3.test` from none to 126, and the suite loses 986 refusals of `no such function`. | decided | 2026-09-15 |
| D-233 | `crate::change::Writer::opened` answers a connection over a database that is already written: the header of the image says the page size, the reserved tail, the encoding, the free list and the largest root page, and `crate::tree::Pages::opened` splits the image into the pages the header counts. This crate wrote every database from one page up, so a file it wrote could be read and never written again. A header that counts no pages, which is what a library older than 3.7.0 wrote, is read as the pages the image carries; an image that carries fewer pages than the header counts is refused. The settings a connection is told rather than reads, from the journal mode to the seed of `randomblob`, are the ones a new connection begins with. Opening an image costs O(n) in its pages. The suite's tester copies a database with it, which is what `db_save` and `db_restore` of SQLite's own tester ask for, and carries the twenty-odd procedures of that tester that need no library of its own, from `execsql2` to `permutation`, so that a file which calls one runs on rather than stopping. `sqlite-suite --why` counts the statements the engine refused by their first words beside the cases, which is what says which shape of statement to build next. | decided | 2026-09-15 |
| D-234 | A table written `WITHOUT ROWID` keeps its rows in the tree of its `PRIMARY KEY`, which section 2.4 of the format writes as an index tree whose entry is the whole row: the columns of the key first, in the order the key names them, and the rest after them in the order the statement declares them. This crate read such a table and refused every statement that wrote one. `CREATE TABLE` now makes the root as a tree of index pages and writes no `sqlite_autoindex` row for the key, which the tree itself holds, while the key still counts toward the number the next index of the table's own carries. `INSERT`, `UPDATE` and `DELETE` place, replace and take out the entry the columns of the key find, which costs O(log n) in the rows of the table, and the entry of every index over such a table ends with the columns of that key rather than with a rowid. A statement that names `rowid` on such a table is refused as a column no table has, which `crate::change::Held` says by holding no key. `PRAGMA integrity_check` holds the rows of such a table to its indexes the same way. The file this crate writes for a table with two key columns and an index beside it is the file the shell writes for it, byte for byte. Two rules of the C library were read again while this was built: `sqlite3GenerateConstraintChecks` writes a row out under `OE_Replace` with the triggers of a `DELETE` only where `SQLITE_RecTriggers` is on, and a trigger that says the row stays leaves the key where it was, so what the statement writes shares a key with it and is refused; and `sqlite3Pragma` names a row of `integrity_check` by the count of rows the walk has read, which is register 7 of the routine it writes, and not by the key of the row. SQLite's own files: `without_rowid1.test` goes from 3 cases passing to 57, `without_rowid3.test` from 640 to 681, `without_rowid6.test` from none to 8 and `without_rowid7.test` from 2 to 4. | decided | 2026-09-15 |
| D-235 | The JSON functions are `src/json.c` ported whole: the twenty-six scalar names from `json` to `jsonb_array_insert`, the four aggregates `json_group_array`, `json_group_object`, `jsonb_group_array` and `jsonb_group_object`, and the operators `->` and `->>`. A document is held in the binary form the header comment of `src/json.c` states, whose element carries its type in the low four bits of its first byte and the length of its payload in the high four, a width up to eleven being the length itself and one past that the count of bytes that hold the length, so a document read out of text and a document read out of a `jsonb` blob are the same bytes and one walk serves both. Text is read as RFC 8259 states it and as JSON5 adds to it: a label with no quotes, a string in single quotes, a hexadecimal number, `Infinity` and `NaN`, a comment, a trailing comma. The reader marks a value it read under one of the additions, which `json_valid` answers 1 for under flag 2 and 0 for under flag 1. A call answers whether what it answered is JSON of its own, which the C library marks with `JSON_SUBTYPE` and this crate carries beside the value as `crate::eval::Answer::json`, so `json_array(json('[1]'))` writes `[[1]]` where `json_array('[1]')` writes `["[1]"]`. `crate::func::Function::Json` names which of the family a call is and whether the name began `jsonb`, so the table of names holds one row per name and the call holds one arm. Reading a document costs O(n) in its bytes, one path step costs O(n) in the elements of the value it steps into, and an edit writes the document again at O(n). Three rules of the C library were read again while this was built: `jsonLookupStep` refuses a pair whose value ends past the object that holds it; `jsonMergePatch` refuses a patch that nests deeper than a thousand with `JSON nested too deep`, where a path that steps deeper says `JSON path too deep`; and `jsonUnescapeOneChar` refuses `\0` followed by a digit, which JSON5 asks for and which the C library did not enforce before 3.50.0. `json_each` and `json_tree` need a virtual table and are open. SQLite's own files: `json101.test` goes from no case passing to 42, `json102.test` from none to 292 of 298, `json103.test` from none to 14, `json104.test` from 1 to 33, `json105.test` from none to 52, `json109.test` from none to 17, `json501.test` from none to 183 of 184, `json502.test` from none to 5, and `jsonb01.test` from none to 39, and the suite goes from 54 797 cases passing to 55 581. | decided | 2026-09-15 |
| D-236 | `SAVEPOINT`, `RELEASE` and `ROLLBACK TO` bound a part of a transaction under a name, which is `sqlite3Savepoint`: a `SAVEPOINT` outside a transaction opens one and the release of that savepoint commits it, a `RELEASE` of any other keeps what the savepoint wrote, and a `ROLLBACK TO` puts the file back to what the savepoint stands over and leaves that savepoint open. A `COMMIT` and a `ROLLBACK` take every savepoint with them. A name is read without regard to case and with its quotes taken off, the innermost savepoint of that name is the one a statement names, and a name no open savepoint holds is refused as `no such savepoint: NAME`. `crate::change::Writer` keeps a savepoint as the pages and the header as they stood, so opening one costs O(n) in the pages of the file and rolling back to one costs the same. A statement of a transaction is a unit of its own as well: `crate::tree::Pages` keeps every page the statement opens as the statement found it, which is the statement journal `sqlite3VdbeOpenStatement` opens, and a statement that refuses puts those pages back, so `INSERT INTO t VALUES(2),(1)` into a table that already holds 1 leaves the transaction where it stood rather than keeping the row it wrote first; a statement that stops where it stands keeps what it wrote, which is `OE_Fail`. Undoing a statement costs O(n) in the pages it opened. The file this crate writes for a savepoint block, and the file it leaves where a statement of a transaction refused, are the files the shell writes for them, byte for byte. SQLite's own files: `savepoint.test` goes from 7 cases passing to 28, `savepoint6.test` from 20 to 117, `savepoint5.test` from 1 to 3, `savepoint7.test` from none to 3, and the suite goes from 55 581 cases passing to 56 154. | decided | 2026-09-15 |
| D-237 | `ALTER TABLE ... RENAME TO` writes every row of `sqlite_schema` that names the table again under the new name, which is `sqlite3AlterRenameTable`: the table's own row carries the new name in `name`, `tbl_name` and its text; an index and a trigger over the table carry it in `tbl_name` and in their text; a view and a trigger that only name the table carry it in their text; and an index SQLite made for a key of the table is renamed from `sqlite_autoindex_<old>_N` to `sqlite_autoindex_<new>_N`. The row of `sqlite_sequence` that counts the key up is written again under the new name; the rows of `sqlite_stat1` are left as they are, which is what the C library leaves them as. The tree of the table stays where it is, so a rename writes the schema and nothing else, at O(n) in the rows of the schema and O(m) in the text of each. `crate::rename::places` marks where a statement names the table from the tree the parser built and not from the text, so a column, a string and a `WITH` term that read like the table are left alone, and the new name is written in double quotes with every quote inside it doubled, which is `%w` of the C library. The statement is refused where the schema holds no such table (`no such table: NAME`), where the name is a view (`view NAME may not be altered`), where it begins `sqlite_` (`table NAME may not be altered`, the schema's own table under the name `sqlite_master`), and where the new name is one the schema already holds (`there is already another table or index with this name: NAME`). One rule of the C library was read again while this was built: the `sqlite_sequence` table SQLite makes with the first table that counts its keys up raises the schema cookie once and not twice, so a file this crate writes for such a table now carries the cookie the shell writes. `RENAME COLUMN` and `DROP COLUMN` are open. SQLite's own files: `altertrig.test` goes from no case passing to 11, `altertab3.test` from 43 to 54, `altertab2.test` from 19 to 27, `altertab.test` from 5 to 8, `alter.test` from 4 to 8, `alterdropcol.test` from 21 to 25 and `hook.test` from 38 to 41. | decided | 2026-09-16 |
| D-238 | `INSERT ... ON CONFLICT` is built, which is `sqlite3UpsertNew` and `sqlite3UpsertDoUpdate`: a clause names the columns of a key of the table or names none at all, and a row that shares that key reaches it rather than the resolution of the statement. `DO NOTHING` passes the row over, and `DO UPDATE SET` writes the row the conflict found, reading its columns under the name of the table and the columns of the row that was not written under the name `excluded`, each of which answers `rowid` as the key of its own row. A statement may write one clause per key, and the clause a conflict reaches is the one over the key the row shares, so a conflict on another key is refused as it would be with no clause at all. The write of a clause runs under `OE_Abort` whatever the statement wrote, so a constraint refuses it rather than passing the row over, and the triggers of an `UPDATE` run over it. A clause that names columns no key is over is refused as `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint` before the statement writes a row. Reading the row a conflict found costs O(n) in the rows of the table and writing it costs O(log n) in them. One rule of the C library was read again while this was built: the key of a table is a whole number and nothing else, so a value the affinity of that column leaves as text, as a real with a fraction or as a blob is refused as `datatype mismatch`, which `INSERT`, `UPDATE` and a clause that writes the key all answer now. A table that keeps its rows in the key's own tree is not written this way yet. SQLite's own files: `upsert5.test` goes from 6 cases passing to 135, `upsert4.test` from 9 to 33, `upsert1.test` from none to 11, `upsert2.test` from none to 6, `upsert3.test` from 1 to 5, `intpkey.test` from 56 to 71 and `altercol.test` from 75 to 90. | decided | 2026-09-16 |
| D-239 | A statement that writes reads the statements written inside its expressions, which is `sqlite3ExprCodeSubselect` there: `crate::change::Held` carries the database as the statement found it, so `EXISTS`, `IN (SELECT ...)`, `IN table` and a statement that stands for a value are answered under an `INSERT`, an `UPDATE` and a `DELETE` as they are under a `SELECT`. The rows such a statement reads are the rows the file held when the statement began, because the writer reads the rows it will write before it writes the first of them. Answering one costs what the statement costs, once per row the outer statement holds. A row a statement wrote over under `OE_Replace` is gone before the walk reaches it, which is ticket #2832: the walk now passes over a row the file no longer holds, and, for a table that keeps its rows in the key's own tree, over an entry that is no longer the row that was read, so the entries of the indexes count what the table holds. The C library walks that tree itself and reaches a row again where the write moved it past the walk, which this crate does not; the file holds what it says either way. One rule was read again with it: the key of such a table refuses nothing, so a row whose key a statement written inside the `SET` answers nothing for is refused as `NOT NULL constraint failed`. SQLite's own files: `sidedelete.test` goes from 105 cases passing to 401 with none failing, `in2.test` from 1313 to 1398, `in.test` from 92 to 98, `subquery.test` from 47 to 52, `rowid.test` from 131 to 135, and the suite goes from 56 338 cases passing to 56 736. | decided | 2026-09-16 |
| D-240 | `ALTER TABLE ... DROP COLUMN` takes the column out of the text that made the table and out of every row of it, which is `sqlite3AlterDropColumn`: the text of the column goes with the comma beside it, and each row is written again without the value that stood there, at O(n) in the rows and O(log n) per row. The statement is refused where the table holds no such column (`no such column: \"NAME\"`), where the column carries a `PRIMARY KEY` or a `UNIQUE` of its own (`cannot drop PRIMARY KEY column: \"NAME\"`), and where it is the one column the table has (`cannot drop column \"NAME\": no other columns exist`); a key written as a constraint of the table rather than of the column is not one of these, which is `COLFLAG_UNIQUE` marking the column alone. Every statement of the schema that names the table is read again once the column is gone, which is `renameTestSchema`: one that still names the column refuses the whole statement as `error in KIND NAME after drop column: no such column: NAME`, with the column as that statement wrote it, so a trigger that reads `new.a` names `new.a`. The entries of an index over a column that stayed are the values they were, so no index is written again. `crate::change::alterable` is the one place both `ALTER TABLE` statements hold the table they alter to. SQLite's own files: `alterdropcol.test` goes from 25 cases passing to 55 and `alterdropcol2.test` from 1 to 3. | decided | 2026-09-16 |
| D-241 | `RETURNING` answers one row per row a statement wrote, which is `sqlite3AddReturning`: an `INSERT`, an `UPDATE`, a `DELETE` and the `DO UPDATE` of an `ON CONFLICT` each answer the row as it stands after the write, and a `DELETE` answers the row as it was. `*` answers the columns of the table and an expression beside it is read against that row, so `rowid` answers the key. A statement that wrote no row answers none. `crate::change::Writer` holds the rows while the statement runs and answers them in place of the count `PRAGMA count_changes` gives, at O(c) per row in the columns the clause names. `INSERT INTO t DEFAULT VALUES` writes one row of what every column falls back to and reads no statement of its own, which `sqlite3Insert` writes as a row of defaults. SQLite's own files: `returning1.test` goes from 9 cases passing to 33, `literal.test` from 44 to 66, `insert3.test` from 13 to 16 and `alter4.test` from 20 to 23. | decided | 2026-09-16 |
| D-242 | A statement that writes is refused with the words the C library writes. `sqlite3Insert` names the table and the column where a statement names a column the table does not hold (`table t has no column named zz`), counts the values against the columns the statement named (`3 values for 2 columns`), and counts them against the columns of the table where the statement named none (`table t has 2 columns but 3 values were supplied`); an `UPDATE` that sets a column the table does not hold is refused `no such column: zz`. `sqlite3StartTable` and `sqlite3CreateIndex` split a name the schema already holds in two: a name the same kind holds is `KIND NAME already exists` and a name the other kind holds is `there is already a/an KIND named NAME`, where a table and a view are one kind to a `CREATE INDEX`. `sqlite3CheckObjectName` refuses a name that begins `sqlite_` as `object name reserved for internal use: NAME`, which `PRAGMA writable_schema=ON` lifts and which `crate::change::Writer::own_table` lifts for the two tables this crate writes for itself, `sqlite_sequence` and `sqlite_stat1`. `writable_schema` moves from the pragmas the crate ignores to the ones the connection keeps, and every pragma written as a truth value now reads `sqlite3GetBoolean(zRight, 0)`, so a word that names no truth value turns the flag off rather than refusing the statement, which `PragTyp_FLAG` does and which `PRAGMA writable_schema=RESET` needs. Each refusal is read out of the schema the statement stands against, at O(n) in its rows. SQLite's own files: `table.test` goes from 49 cases passing to 58, `index.test` from 78 to 85, `insert.test` from 24 to 29, `update.test` from 120 to 122 and `select1.test` from 135 to 137. | decided | 2026-09-16 |
| D-243 | An index holds a place over an expression and holds entries for fewer rows than the table has, which is `sqlite3CreateIndex` filling `aColExpr` and `pPartIdxWhere`. `crate::schema::Of` says what one place holds: a column of the table by the place it takes in it, or an expression as the tree of the `CREATE INDEX` holds it. A term written as text names a column, which is `sqlite3StringToId` turning `TK_STRING` into `TK_ID`; a term that names a column under a table or a schema is refused `the \".\" operator prohibited in index expressions`, which `sqlite3ResolveSelfReference` refuses under `NC_IdxExpr` and leaves alone in the `WHERE` of a partial index. A place over an expression is compared under the collation the term writes or under `BINARY`, and its value is read against the row at O(1) per node of the expression. A partial index holds an entry only for a row its `WHERE` answers true for, which is `sqlite3ExprIfFalse` over `pPartIdxWhere`, so a `UNIQUE` over it holds only those rows to the key and `ANALYZE` counts the entries the index holds rather than the rows the table has. `sqlite3UniqueConstraint` names such an index `index 'NAME'` in place of the columns. A statement is planned against neither, because both answer fewer entries than their places say and a walk of the table answers every row; `crate::schema::Index::first_keyed` is the one place that says so. The statement that made an index is kept beside it only where the index holds an expression or a `WHERE`, so a statement that writes rows into a table of plain indexes copies no tree. SQLite's own files: `indexA.test` goes from 15 cases passing to 141, `indexexpr2.test` from 65 to 106, `indexexpr1.test` from 21 to 57, `upsert4.test` from 33 to 61, `alterdropcol2.test` from 3 to 15 and `expridx1.test` from 18 to 21. | decided | 2026-09-16 |
| D-244 | An `ON CONFLICT` clause names a key by its columns and their collations, which is `sqlite3UpsertAnalyzeTarget`: the clause names as many terms as the key has columns, and every column of the key is one the clause names under the collation the key holds it in or under none, because `sqlite3ExprCompare` answers one rather than two for a `COLLATE` that one side writes and the other does not. The terms are parsed as the columns of an index are, so a term carries its own collation and its own order, which the parser dropped before. A clause that names one column twice and another not at all names no key, and neither does a clause whose term writes a collation the key does not hold the column in; both are refused `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`. An index that holds a place over an expression, and one that holds entries for fewer rows than the table has, are named by no clause, because matching either needs the trees compared rather than the names; the C library compares them with `sqlite3ExprCompare`, so a clause that names such an index is refused here and accepted there. Holding a clause to a key of `k` columns costs O(k^2). SQLite's own files: `upsert4.test` goes from 61 cases passing to 71 and `upsert1.test` from 13 to 18. | decided | 2026-09-16 |
| D-245 | `ALTER TABLE ... RENAME COLUMN` writes every statement of the schema that names the column again under the new name, which is `sqlite3AlterRenameColumn`: the rows of the table are not written again, because a row holds no name. `crate::rename::column_places` marks the places from the tree the parser built, which is `renameColumnFunc` walking the tree a statement was parsed into: the column's own name, the keys the table carries, every `CHECK`, every `DEFAULT`, every computed column, the columns of a `REFERENCES` that points at the table, the terms and the `WHERE` of an index over the table, the `UPDATE OF` and the `WHEN` of a trigger on it, and the statements of that trigger's body. `crate::rename::Stands` says what a name written under nothing reads and what `new` and `old` read, so a statement over another table names none of this table's columns: a name written under nothing reads the table the statement writes, or the tables its `FROM` names, and `new` and `old` read the table the trigger stands on. The new name is written as the statement wrote it, which is `sqlite3AlterRenameColumn` reading `bQuote` off its first byte, so a name written without quotes stays without them. The statement is read again before it is written, which is `renameTestSchema` under `after rename`: a rename that leaves the table with two columns of one name is refused `error in table NAME after rename: duplicate column name: NAME` and the schema stands as it was. Rewriting one statement costs O(n) in the nodes of its tree, and the whole rename costs O(m) in the rows of the schema. A name inside a statement written inside another, such as the `SELECT` of an `UPDATE` of a trigger's body, is left alone where the outer statement writes another table; the C library renames it, because it resolves each statement against its own `FROM`. SQLite's own files: `altercol.test` goes from 91 cases passing to 159, `altertab3.test` from 60 to 70, `altertab2.test` from 27 to 35 and `altertrig.test` from 11 to 16. | decided | 2026-09-16 |
| D-246 | A row is held to the keys of a table in the order the `ON CONFLICT` clauses name them, which is `sqlite3GenerateConstraintChecks` building `IndexListTerm`: the keys the clauses name come first in the order the clauses name them, the key of the table stands in the place of the clause that names it and after the named keys where no clause names it, and the indexes no clause names come last in the order the schema holds them. A clause that names a key an earlier clause named is one no row reaches, which `bUsed` marks, and a clause that names no column ends the run, which is `pUpsertTarget==0` breaking the loop. `crate::change::Writer::checked_order` answers that order once per statement rather than once per row, and refuses `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint` where a clause names no key, which is what `upsert_keys` refused before. Ordering `n` clauses over `k` indexes costs O(n k), and holding one row to the keys costs O(k log m) in the rows of the trees. A table that keeps its rows in the key's own tree reaches no clause yet. SQLite's own files: `upsert5.test` goes from 135 cases passing to 168 with 37 answering differently down to 4, `upsert4.test` from 71 to 75 and `upsert1.test` from 18 to 22. | decided | 2026-09-16 |
| D-247 | The four `ALTER TABLE` statements that write a constraint read the tokens of the statement that made the table rather than the tree it was parsed into, which is `alterDropConstraintFunc` and `alterAddConstraintFunc`: `crate::constraint` walks the tokens with a bracketed run answered whole, so the `(a != b)` of a `CHECK` holds its comma. `DROP CONSTRAINT` cuts the run from the word `CONSTRAINT` to the end of the constraint, `ALTER COLUMN ... DROP NOT NULL` cuts the `NOT NULL` of the column at that place, `ALTER COLUMN ... SET NOT NULL` writes the words the statement wrote at the end of that column, and `ADD [CONSTRAINT name] CHECK (...)` writes them before the bracket that closes the columns, with a comma before them. The text is written as the statement wrote it, so `NOT   NULL ON CONFLICT fail` keeps its spacing and its case. Only a `CHECK` and a `NOT NULL` may be dropped, so a name over another kind is refused `constraint may not be dropped: NAME` and a name the statement does not hold `no such constraint: NAME`; a `DROP NOT NULL` of a column that holds none writes the statement as it stands, which SQLite takes from postgres. A `SET NOT NULL` over a column a row holds nothing in, and an `ADD CHECK` a row does not hold to, are both refused `constraint failed`, and a name the statement already holds `constraint NAME already exists`. Reading the tokens costs O(n) in the bytes of the statement, and holding the rows to a clause costs O(m) in them. SQLite's own files: `altercons.test` goes from 56 cases passing to 155 and `altercons2.test` from 6 to 9. | decided | 2026-09-16 |
| D-248 | A foreign key is read against the columns of the parent and not against its own: the affinity and the collation of the parent's column are what a child value is compared under, which is `R-04240-13860`, so a child column written under `BINARY` that points at a parent column written under `NOCASE` is compared under `NOCASE` in both directions. `crate::change::Points` carries that pair per column of the key, read once per statement. `sqlite3FkLocateIndex` runs where the statement is read and not where a row is written, so a key that names no key of the parent is refused whatever the row holds, which is what lets `INSERT INTO t2 VALUES(NULL)` reach the refusal; the refusal names both tables, `foreign key mismatch - \"CHILD\" referencing \"PARENT\"`. An index of the parent's own says the columns are a key where it is unique, holds those columns and holds each in the collation that column compares under, so `CREATE UNIQUE INDEX t1i ON t1(x)` is a key and `CREATE UNIQUE INDEX t1i ON t1(x COLLATE nocase)` over a `BINARY` column is not. `PRAGMA foreign_keys` set while a transaction is open changes nothing, which is `PragTyp_FLAG` taking `SQLITE_ForeignKeys` out of the mask where `autoCommit` is nought; every other pragma the connection keeps is set there. Reading one key costs O(n) in the rows of the parent. A key that points at a table the schema does not hold is refused `no such table: main.NAME`, which names the schema the table would stand in. SQLite's own files: `e_fkey.test` goes from 571 cases passing to 598, `fkey2.test` from 830 to 851 and `fkey3.test` from 13 to 16. | decided | 2026-09-16 |
| D-249 | `timediff` counts the years and the months by walking the second moment to the first rather than by subtracting the two, which is `timediffFunc`: the year of the second moment is set to the year of the first and its julian day read again, then its month is set to the month of the first, then it is walked a month at a time until it stands on the near side of the first, forward where the first moment is the earlier and back where it is the later. What is left is added to the julian day of the first day of the year nought, `1486995408 * 100000`, and read back as a date, so the day of the month it lands on, less one, is the count of days and the time of day is the rest. Reason: a month is not a fixed run of days, so subtracting the months and then the days answers a day count that belongs to no month; the walk is what `%04d-%02d-%02d` of `timediffFunc` writes against. Walking the months costs O(n) in them, which is at most twelve. SQLite's own files: `timediff1.test` goes from 982 cases passing to 1520, with none answering differently. | decided | 2026-09-16 |
| D-250 | A foreign key names a row by its key and not by a rowid, so a table that keeps its rows in the key's own tree is held to a key like any other: `crate::db::Database::held_rows_of` answers the key of each row, which is one whole number for a table with a rowid and the columns of the `PRIMARY KEY` for a table without one, and the rows a key points at, the rows that point, and the row one action writes are all read under that key. `Writer::taken_away` and `Writer::rewrite` take the entry out of the key's own tree and put it back where the table keeps its rows there, and write the row by its rowid where it does not. Reason: `sqlite3FkLocateIndex` and the routines around it read a row through the cursor of the table, which is the key's own tree for such a table, so a rowid is not what names a row. Reading one key costs O(n) in the rows of the table it points at. SQLite's own files: `without_rowid3.test` goes from 706 cases passing to 828. | decided | 2026-09-16 |
| D-251 | A `CREATE` and a `DROP` name in a refusal what the statement wrote. `sqlite3StartTable` writes the name with its quotes, which is `%T` of the token, so `CREATE TABLE \"t\"(y)` over a table named `t` is refused `table \"t\" already exists`; a name the other kind holds is written with its quotes off, which is `%s` of the name. `sqlite3DropTable`, `sqlite3DropIndex`, `sqlite3DropView` and `sqlite3DropTrigger` each name what the statement said it takes away, so a `DROP TRIGGER` of a name the schema does not hold is refused `no such trigger: NAME`. A `CREATE TRIGGER` and a `CREATE INDEX` over a table the schema does not hold name that table under the schema it would stand in, `no such table: main.NAME`, and a temporary trigger names no schema. Only a view carries an `INSTEAD OF` trigger and only a table carries a `BEFORE` or an `AFTER`, so the other four pairings are refused `cannot create WORD trigger on KIND: NAME`, and a table whose name begins `sqlite_` carries no trigger at all, `cannot create trigger on system table`. `CREATE TEMP TRIGGER` is read as a temporary trigger, which the parser took for a name before. SQLite's own files: `trigger1.test` goes from 22 cases passing to 34, `returning1.test` from 33 to 39 and `schema4.test` from 12 to 15. | decided | 2026-09-16 |
| D-252 | A statement the parser refuses is named by the token the parser stopped at: `near \"FROM\": syntax error`, which is `sqlite3ErrorMsg` of `parse.y`, and `incomplete input` where the tokens ran out before the grammar was met. `crate::db::Error::near` reads the token out of the text at the span the parse error carries, so the message costs O(1) over the parse. The readings of `crate::change::Writer::ran` are tried in turn and `crate::parse::furthest` keeps the one whose span stands latest, because a statement that is no definition fails at its first token while the reading that fits it fails where the grammar stopped. Reason: 1224 `do_catchsql_test` cases compare the message, and the debug form of a parse error matches none of them. SQLite's own files: `select1.test` goes from 46 cases answering differently to 40, `e_select.test` from 13 to 7, `tkt3935.test` from 7 to 4, `main.test` from 43 to 40 and `wherelimit.test` from 15 to 13, with 18 files losing 35 between them. | decided | 2026-09-16 |
| D-253 | An `INSERT`, an `UPDATE` or a `DELETE` over a view runs the `INSTEAD OF` triggers of the view and writes no row of the view, which is `sqlite3Insert`, `sqlite3Update` and `sqlite3DeleteFrom` reaching `sqlite3ViewIsEditable`: a view that carries no `INSTEAD OF` trigger of that event refuses the statement `cannot modify NAME because it is a view`. `crate::db::Database::viewing` answers the view as a table of the columns its statement answers, each converting nothing and compared byte by byte, and the rows the statement answers now; an `INSERT` puts one row in `new`, a `DELETE` puts each row the `WHERE` keeps in `old`, and an `UPDATE` puts the row in `old` and the row the `SET` clauses make of it in `new`. A view has no key, so a value written for `rowid` goes nowhere. `sqlite3_changes` counts nought, because the statement wrote no row. Reason: the statement of the view is answered where the view is written, so one statement costs what that statement costs plus what the triggers cost per row. SQLite's own files: `trigger4.test` goes from 1 case passing to 21, `triggerA.test` from 6 to 17, `trigger2.test` from 82 to 93, `without_rowid4.test` from 78 to 88 and `tkt3298.test` from 0 to 6. | decided | 2026-09-16 |
| D-254 | A connection carries three counters that `changes()`, `total_changes()` and `last_insert_rowid()` answer, which is `sqlite3_changes`, `sqlite3_total_changes` and `sqlite3_last_insert_rowid`. `crate::func::Counted` holds the three; `crate::change::Writer::counts` answers them and `crate::db::Database::counting` tells a reader built over the writer's file what they stand at, because the counters belong to a connection and the pages to a file. Every statement that changes rows sets `changes()` to its own count and adds to the total, the statements of a trigger's body among them, and the statement that fired the trigger sets `changes()` again when it ends. A statement refused under `OE_Abort` leaves `changes()` at nought and one refused under `OE_Fail` at the rows it wrote before it stopped. `crate::change::Writer::fire` keeps `last_insert_rowid()` over a trigger's body, which is what `OP_Program` does, and a foreign key action counts toward the total alone. Reason: 1224 `do_catchsql_test` cases and the `db changes`, `db total_changes` and `db last_insert_rowid` methods of the tester read the three, and a connection that answered nought for all of them stopped every case behind one of them. Reading a counter costs O(1). SQLite's own files: `e_changes.test` goes from 12 cases passing to 57, `lastinsert.test` from 0 to 32, `laststmtchanges.test` from 3 to 25, `e_totalchanges.test` from 2 to 24 and `changes.test` from 24 to 50. | decided | 2026-09-16 |
| D-255 | A cell takes four bytes of a page at the least, which is `cellSizePtr`: a cell of fewer bytes would leave behind too few to be a freeblock, so `crate::page::cell_room` answers four for one of one, two or three bytes and every routine that takes or gives back the space of a cell reads it — `Writer::insert`, `Writer::remove`, `free_array`, `insert_array`, `rebuild`, `build`, and the packing of `crate::tree::cost_of`. Reason: `crate::page::Writer::release` is `freeSpace`, which is written against a block of four bytes or more, so a three-byte cell given back wrote a freeblock that leaves the page and the file no longer counted up: `DELETE FROM t1 WHERE x=1` over `CREATE TABLE t1(x INTEGER PRIMARY KEY) WITHOUT ROWID` holding 1 and 2 was refused `the free space of a page does not add up to the page`. The page this crate writes for those statements is now the page the shell writes, byte for byte. Answering the room one cell takes costs O(1). SQLite's own files: `changes.test` goes from 50 cases passing to 56 with none answering differently. | decided | 2026-09-16 |
| D-256 | An `INSERT ... ON CONFLICT` over a table that keeps its rows in the key's own tree reaches the same clauses an `INSERT` over a table with a rowid reaches, with the key of the table standing where the rowid stood: `Writer::key_column` answers the columns of the `PRIMARY KEY`, each in its own collation, so `sqlite3UpsertAnalyzeTarget` matches a clause that names them; `Writer::conflicted_keyed` walks the order `checked_order` built, reading the tree of the table for the key of the table and an index for each other key; and `Writer::upserted_keyed` reads the row the conflict found by its key, writes the row the `DO UPDATE` computes, and writes the entry under the new key where the clause wrote the key itself. The clause writes under `OE_Abort`, which is the `UPDATE` `sqlite3UpsertDoUpdate` builds, so a key the written row shares with another refuses the statement whatever the key's own clause says. Reason: the engine refused every such statement `Unsupported`, which stopped 155 cases of `upsert5.test` alone. Reading the row the conflict found costs O(n) in the rows of the table and writing it costs O(log n) in them and in the entries of each index. SQLite's own files: `upsert5.test` goes from 168 cases passing to 246, `upsert4.test` from 75 to 97, `upsert2.test` from 6 to 12 and `upsert1.test` from 23 to 27. | decided | 2026-09-16 |
| D-257 | A `STRICT` table names in a refusal the column whose type it cannot read and the value a column cannot hold. `sqlite3EndTable` refuses a column of no type `missing datatype for TABLE.COLUMN` and one of a type that is not `INT`, `INTEGER`, `REAL`, `TEXT`, `BLOB` or `ANY` `unknown datatype for TABLE.COLUMN: \"AS WRITTEN\"`, which `crate::schema::Error` now carries the names for. `OP_TypeCheck` applies the affinity of the column and then holds the value to the type the column declares, so a column of `INT` holds the text `'3'` as the number 3 and refuses `'xyz'` `cannot store TEXT value in INT column TABLE.COLUMN`; a column of `REAL` holds a whole number as a real, which is `MEM_IntReal`, and a column of `ANY` holds whatever it is given. `crate::change::stored_types` runs before the constraints of the table, which is where `sqlite3Insert` writes the opcode. An `ALTER TABLE ... ADD COLUMN` reads the schema again and names itself where the schema can no longer be read, `error in table TABLE after add column: MESSAGE`, which is `corruptSchema` of `src/prepare.c`. Reason: the engine answered the debug form of the refusal, which no case of the suite compares, and wrote a value of any type into a `STRICT` table. Holding one row costs O(n) in its columns. SQLite's own files: `strict1.test` goes from 6 cases passing to 28, with 41 answering differently falling to 19; the nineteen left are generated columns, which this crate computes where they are read and not where the row is written. | decided | 2026-09-16 |
| D-258 | A statement that writes a row computes the columns of a `GENERATED ALWAYS AS` before it holds the row to the types and the constraints of the table, which is `sqlite3ComputeGeneratedColumns` running ahead of `OP_TypeCheck`: `crate::db::Database::compute_row` fills every computed column of the row, and `crate::change::Writer::constrained` calls it. A column computed once is written down; a column computed where it is read takes no place in the record, which is `sqlite3TableColumnToStorage`, so every record the writer builds is now laid out by `crate::change::ordered` and `crate::change::ordered_affinities` rather than by the columns of the table. A column whose expression the row cannot answer keeps the value it was given, because the read of such a column refuses on its own. Reason: a `STORED` column answered nothing, and a `VIRTUAL` column held a place in the record, so every column written after one answered the value of the column before it. Computing `k` columns costs O(k²) evaluations, because one expression may name another. SQLite's own files: `strict1.test` goes from 28 cases passing to 47 with none answering differently, and `alterdropcol.test` from 57 to 69. | decided | 2026-09-16 |
| D-259 | `median(Y)`, `percentile(Y,P)`, `percentile_cont(Y,P)` and `percentile_disc(Y,P)` are aggregates of this crate, which is `ext/misc/percentile.c` built in: the fraction is read out of every row and is the same for every row of a group to a thousandth, which is `percentSameValue`; a value that is nothing is passed over, one that is neither nothing nor a number refuses, and an infinity refuses. The answer is the value the fraction names among the values the group held in order, between the two it falls between for three of them and on the one it names for `percentile_disc`. `f(...) WITHIN GROUP (ORDER BY Y)` is `f(Y,...)`, which is `sqlite3ExprAddFunctionOrderBy` putting the one term of the clause in front of the arguments, so the parser rewrites the call and the aggregate needs to know nothing about the clause; a `DISTINCT` on such a call is refused by name. An aggregate called with a number of arguments it does not take now answers `wrong number of arguments to function NAME()` rather than `no such function`, because the scalars hold no name of it. Reason: `percentile.test` is 210 cases and the engine answered five of them. Sorting the values of a group costs O(n log n) in them. SQLite's own files: `percentile.test` goes from 5 cases passing to 209, with 44 answering differently falling to none. | decided | 2026-09-16 |
| D-260 | A number written with digit separators is read as the number the separators are taken out of, and the separators say nothing about which kind of number it is: `crate::parse::Parser::separated` is `sqlite3DequoteNumber`, so a `.` or an `e` left behind makes the number a real, a hex literal is a whole number whatever its digits are, and a separator anywhere but between two digits makes the token one the tokenizer read as no token at all. Such a token is refused `unrecognized token: \"TEXT\"` wherever the parser meets it, which is the `TK_ILLEGAL` of `sqlite3RunParser`, so `SELECT 123a456` is refused under its own text. An `ORDER BY` or a `GROUP BY` term that counts to a column the answer does not have names its own place, `2nd ORDER BY term out of range - should be between 1 and 3`, which is `resolveOutOfRangeError` writing `%r`. The suite harness hands a statement the bytes after its last one that carries meaning, because `SELECT 1 /* ` is a comment that runs to the end and `SELECT 1 /*` is a slash and a star. Reason: the parser read every separated number as a whole one, so `1.1_1` answered 1. Reading one token costs O(n) in its bytes. SQLite's own files: `literal.test` goes from 66 cases passing to 96, `tokenize.test` from 1 to 14, `tkt2822.test` from 15 to 29, `blob.test` from 3 to 13 and `main.test` from 51 to 61. | decided | 2026-09-16 |
| D-261 | An `UPDATE` reads the tables of a `FROM` beside its own: the parser holds the clause as the statement `SELECT * FROM ...` answers it, which is what `sqlite3Update` builds it into, so `crate::db::Database::joined` answers its columns and its rows through the machinery a `SELECT` already uses. Every row of the table is written against the row of the clause the `WHERE` holds for, and against the last of them where the clause holds more than one, which is the ephemeral table `sqlite3Update` writes the rows into; a view runs its `INSTEAD OF` triggers once per row of the join, duplicates included. `crate::change::Aside` answers the columns of the clause and hands a name it does not hold to the row a trigger's body stands on. A `WITH` before such a statement names the tables of the clause, and the table the statement changes named again in the clause is refused `target object/alias may not appear in FROM clause: NAME`, a table under another schema being another object. Reason: the engine refused the whole shape `near \"FROM\": syntax error`. Answering the clause costs what its statement costs, and one row of the table costs O(m) in the rows the clause answered. `UPDATE` and `DELETE` with an `ORDER BY` and a `LIMIT` are what the suite reads as `update_delete_limit`, which this engine does not have and the harness now says so for. SQLite's own files: `upfrom3.test` goes from 7 cases passing to 38, `upfrom2.test` from 17 to 44, `upfrom1.test` from 11 to 17 and `altertrig.test` from 16 to 33. | decided | 2026-09-16 |
| D-262 | A foreign key written `DEFERRABLE INITIALLY DEFERRED`, and every key while `PRAGMA defer_foreign_keys` is on, is held where the transaction ends rather than where the row is written: `crate::change::Writer` carries the count `sqlite3VdbeCheckFk` reads, which a row that points at no row raises and a row that stops pointing at nothing lowers, and a `COMMIT` that reads it as other than nought is refused `FOREIGN KEY constraint failed` and leaves the transaction open. A statement outside a transaction commits at its end, so such a key is held by the statement and the rows it wrote go with it; releasing the savepoint that opened the transaction writes it and is held the same way, and a `ROLLBACK TO` puts the count back where the savepoint found it. A row that appears is the parent the rows waiting for it were counted against, which is `fkScanChildren` with a delta of one fewer, read only while the count is other than nought. `ON DELETE RESTRICT` is held where the row is written whatever the key says, which is what `sqlite3FkCheck` leaves it as, and `NO ACTION` counts the rows that lose their parent. Reason: the engine refused every such statement where the row was written, so a transaction could not write two rows that point at each other. Counting one row costs O(m) in the rows of each table that points at the one written. SQLite's own files: `fkey2.test` goes from 851 cases passing to 911 with 82 answering differently falling to 24, and `without_rowid3.test` from 828 to 888 with 76 falling to 18. | decided | 2026-09-16 |
| D-263 | A window is refused in the words the C library refuses it with, which `window1.test` compares in 41 of its cases: `sqlite3WindowRewrite` names the function a window function written where no window was worked out is `misuse of window function NAME()`, `sqlite3WindowAlloc` names a scalar written under an `OVER` `NAME() may not be used as a window function` and a name called with a number of arguments it does not take `wrong number of arguments to function NAME()`, and `sqlite3WindowChain` names the window a window that writes again what the one it builds on wrote overrides, reading the partition, the order and the frame in that order. `crate::db::refused_over` tells the three apart by which table holds the name. A frame counted in rows or in groups takes a whole number, which is `OP_MustBeInt` of `sqlite3WindowCodeStep`, and one counted in the values of its term takes any, which is `windowCodeRangeTest`, so the two are refused `non-negative integer` and `non-negative number`. Reason: the engine answered the debug form of each, which no case compares. Naming a refusal costs O(1). SQLite's own files: `window1.test` goes from 105 cases passing to 137, with 41 answering differently falling to 9. | decided | 2026-09-16 |
| D-264 | An aggregate written where no group has been made is refused in the words the C library refuses it with: `resolveExprStep` answers for the number of arguments before it answers for the misuse, so `min(*)`, which a `*` leaves with no arguments, is `wrong number of arguments to function min()`, and an aggregate the arity fits is `misuse of aggregate function min()` in a `WHERE`, inside another aggregate, in a `FILTER`, and in an `INSERT`, an `UPDATE` or a `DELETE`. `sqlite3ResolveOrderGroupBy` names a `GROUP BY` term `aggregate functions are not allowed in the GROUP BY clause`, `sqlite3ExprCodeTarget` names an `ORDER BY` term of a statement that groups nothing `misuse of aggregate: min()`, and `lookupName` names an alias standing for an aggregate `misuse of aliased aggregate m`. `crate::db::Gathering` carries the answered columns and the sides of the `FROM` through the walk, so the alias is told from a column in O(1) per name. A scalar reads a `DISTINCT` and drops it, which is what `sqlite3FindFunction` does with it. Reason: the engine answered the debug form of each, which no case compares. SQLite's own files: `select1.test` goes from 148 cases passing to 157, with 35 answering differently falling to 26. | decided | 2026-09-16 |
| D-265 | A name more than one side of a `FROM` answers to is refused `ambiguous column name: a` and named as it was written, which `lookupName` refuses once it counts a second match. `crate::db::Cursor::answering` reads every side once and answers nothing, one side, or more than one, which is O(sides) per name, and `crate::eval::Row::ambiguous` asks it only where `column` answered nothing, so a row that reads a column pays for one walk and not for two. A name one `USING` or one `NATURAL` matched is answered by the side before it and counts once. Reason: the engine answered `no such column: a`, which reads as a name no table holds. SQLite's own files: `select1.test` goes from 157 cases passing to 160 and `resolver01.test` from 20 to 24. | decided | 2026-09-16 |
| D-266 | A statement names the columns it answers by `sqlite3GenerateColumnNames`, which reads `PRAGMA short_column_names` and `PRAGMA full_column_names`: an `AS` name is the name, a column reference answers under the column's own name where either pragma is on, with the name of its table in front of it where `full_column_names` is, and every other expression answers under the text it was written as. A `*` answers under the name the statement calls the side, which `sqlite3SelectExpand` writes in front of each column where `full_column_names` is on and `short_column_names` off, and a statement inside the `FROM` with no alias is named after its own place. `crate::db::Column` carries the name a statement above it reaches the column by beside the name it is answered under, because `sqlite3ColumnsFromExprList` and `sqlite3GenerateColumnNames` name the same column differently: the first makes the names of a view and of a statement inside a `FROM` unique, `a`, `a:1`, `a:2`, in O(columns²) per statement. The two pragmas belong to a connection, so `crate::pragma::Kept` carries them per connection for a caller that holds one writer per file. Reason: the engine named every column by the column alone, which three of the four modes do not. SQLite's own files: `colname.test` goes from 38 cases passing to 61 and `select1.test` from 160 to 176. | decided | 2026-09-16 |
| D-267 | A compound is refused in the words the C library refuses it with: `sqlite3SelectWrongNumTermsError` names the word that joins the two cores that answer different numbers of columns, or the rows themselves where the cores are `VALUES`; `parserDoubleLinkSelect` names the word after an `ORDER BY` or a `LIMIT` written on a core other than the last, which the parser reads without taking it because the loop above it has already taken every word that joins two cores; and `resolveCompoundOrderBy` says which term of the `ORDER BY` names no column. A term is read against each core from the left and counts to the column of the first core that answers one of that name, which costs O(cores · terms), where the engine read the first core alone. Reason: the engine answered the debug form of each and refused a term that names a column of a core to the right. SQLite's own files: `select4.test` goes from 90 cases passing to 121 with none failing, and `select1.test` from 176 to 181. | decided | 2026-09-16 |
| D-268 | An index entry holds the value the row holds and not the one the statement wrote: `sqlite3TableAffinity` converts the row before both the row and its entries are written, so `crate::change::entry_of` applies the affinity of the column each place is over, which is O(1) per place and idempotent, so an entry built from a row already read is the same entry. A `CREATE INDEX` is held to what `sqlite3CreateIndex` holds it to before the row of the schema is written: the table is there, it is no view, and its name is not one SQLite keeps for itself; a `DROP INDEX` is refused for an index a `UNIQUE` or a `PRIMARY KEY` made, which carries no statement of its own. A constraint that says `ON CONFLICT ROLLBACK` undoes the transaction and ends it, which `crate::change::Refusing` carries beside `OE_Fail` and `OE_Abort`. Reason: the engine wrote entries a `PRAGMA integrity_check` then called missing, indexed a table SQLite keeps for itself, and answered `OE_Rollback` as `OE_Abort`. SQLite's own files: 2953 cases answering differently fall to 2884, `index.test` goes from 86 passing to 108, `conflict.test` from 124 to 141, `conflict2.test` from 116 to 133 and `types2.test` from 386 to 398 with none failing. | decided | 2026-09-17 |
| D-269 | A foreign key is located where the statement is read and not where a row is written, which is `sqlite3FkLocateIndex`: `crate::change::Writer::located` reads the keys of the table the statement changes and the keys that point at it, so a key that points at a table that is not there or at columns that are no key of it is refused although the statement reaches no row. It costs O(keys) per statement, against O(keys) per row before. The columns a key points at are read against the columns of the index in whatever order they were written, which is what the C library matches, and `crate::schema::pointing` refuses a key of one width pointing at another and a key over a column the table does not hold where the `CREATE TABLE` is read, whatever `PRAGMA foreign_keys` says. Reason: a statement that reached no row wrote nothing and refused nothing, and a key pointing at a `UNIQUE` the other way round was a mismatch. SQLite's own files: `e_fkey.test` goes from 598 cases passing to 875 and runs 940 cases where it stopped at 641, and 2884 cases answering differently fall to 2863. | decided | 2026-09-17 |
| D-270 | An application defines functions on a connection, which `crate::func::Defined` carries as a name, a number of arguments and a function pointer, and which `crate::eval::Row::defined` reads before the functions the crate holds, as `sqlite3FindFunction` does. A pointer and not a closure, because a closure makes the connection carry a lifetime and no longer derive `Debug`; the callers this serves hold their functions in a `static`. `PRAGMA max_page_count` holds the file to a count of pages, which `crate::tree::Pages` carries and `allocateBtreePage` refuses to grow past, so a statement that would grow the file past it is refused `database or disk is full` and leaves the file as it found it. Reason: SQLite's own files call the functions `testfixture` defines, and `tkt2686.test` writes rows until the file fills. SQLite's own files: `tkt2686.test` goes from 816 cases passing with 409 answering differently to 2242 with none, `memdb.test` from 27 to 73, and 2863 cases answering differently fall to 2448. | decided | 2026-09-17 |
| D-271 | The journal mode belongs to the connection and not to the file: `crate::change::Writer::journalled` answers the word the mode is written as, `crate::db::Database::journalling` is told it, and the header of the file says only whether the file is in write-ahead logging. `sqlite3PragmaJournalMode` leaves the mode as it is where the connection has a transaction open, so the pragma answers the mode it did not change. How many columns a statement an `IN` looks in answers is counted where the statement is read, which `sqlite3SubselectError` does: `crate::db::width_of` counts the result columns of the first core in O(columns) and says nothing where a `*` stands among them, because that one is as wide as the tables it is over and the count reads no table. Reason: the engine answered `delete` for every mode it was set to, and let a statement of two columns stand under an `IN` that reaches no row. SQLite's own files: `jrnlmode3.test` goes from 88 cases passing with 36 answering differently to 124 with none, and `in.test` from 100 to 106. | decided | 2026-09-17 |
| D-285 | The tester matches the method of a connection by any beginning of its name that names exactly one method, which is what TCL does for every command. SQLite's own files write `db func` in 214 places against 80 for `db function`, and abbreviate others the same way. The tester read the names whole, so a file that wrote a short one stopped at its first use and every case after it was refused. SQLite's own files go from 70 567 cases passing to 74 313, and twenty-three files that answered no case at all now run: `func.test` answers 2189 where it answered 9, `e_expr.test` 374, `wal3.test` 245, `rollback2.test` 135, `rowvalue.test` 246 where it answered 155, and `join.test` 158 where it answered 132. | decided | 2026-09-18 |
| D-284 | A `WITH` term reads the terms beside it, whichever was written first: `crate::db::Database::terms` answers the terms that read no other first and the ones that read them after, turn by turn, passing over a term whose refusal names a term of the same `WITH` that has no answer yet. A turn that answers none of what is left holds only terms that read each other, which is refused `circular reference: X` naming the term the statement itself reads, and left unanswered where the statement reads none of them, the C library answering a term where it is read and never resolving one no statement reaches; two terms of one `WITH` under one name are refused `duplicate WITH table name: X`, which `sqlite3WithAdd` does. The terms were answered in the order they were written before, so a term that read one written after it was refused `no such table`. Answering n terms costs n turns at worst, so O(n²) statements against O(n) before, which is n under ten in every file of SQLite's own suite. The option not taken is answering a term where it is read, which is what the C library does and which needs a cache the read path may write into. SQLite's own files: `with2.test` goes from 18 cases passing to 25 and `with1.test` from 8 to 11. A term that reads itself inside a subquery is still answered rather than refused. | decided | 2026-09-18 |
| D-283 | Three refusals the C library gives that this engine gave another answer for. 1. `likelihood(X,Y)` takes a real written as one for `Y`, between nought and one: `sqlite3ExprFunction` under `SQLITE_FUNC_UNLIKELY` reads the argument where the statement is read, so a whole number, a text and a sum are each refused. 2. `#1` is the register of a routine the C library writes and no statement carries one, so the token is read and then refused as a syntax error, where the engine read it as a variable and refused it as unsupported. 3. A column added to a table that points at a row of another falls back to nothing, which `sqlite3AlterFinishAddColumn` holds because the rows the table already holds gain no value of their own and would point at no row. Each reads what the statement already carries, so each costs O(n) over its nodes or its tokens. SQLite's own files: `func3.test` goes from 7 cases passing to 10, `whereG.test` from 38 to 41, `expr.test` from 314 to 315, and `alter4.test`, `altercons.test`, `altertab2.test` and `colname.test` each by one. | decided | 2026-09-17 |
| D-282 | A `CREATE TRIGGER` whose text carries a variable — `?`, `?1`, `:name`, `@name` or `$name` — is refused `trigger cannot use variables`, which `sqlite3CreateTrigger` does, a trigger running with no statement of its own to bind against. The check reads the tokens of the text the schema holds, which is every place a variable may stand: the `WHEN`, the body, a statement inside it, a `GROUP BY`, a `LIMIT`, an `ORDER BY` and a window. A write of the body that names a schema is refused too, which `sqlite3TriggerInsertStep` and the two beside it do by taking the name of a table alone. Reading the tokens costs O(n) in the text. The option not taken is walking the arena for `Node::Variable`, which reaches the nodes the parser kept and not a variable in a clause the parser holds as a span. SQLite's own files: `triggerE.test` goes from 1 case passing to 19, `window1.test` from 137 to 140 and `e_insert.test` from 25 to 27. | decided | 2026-09-17 |
| D-281 | The parser reads up to three words before `JOIN`, each a name of its own, and refuses a combination no join is written with: a word no join carries, and `INNER` beside `OUTER`, which is the table of `sqlite3JoinType`. The words were read as the join words alone before, so `LEFT BOGUS JOIN` stopped at `BOGUS` with a syntax error rather than naming the combination. An `ON` of an outer join that names a table read after it is refused as well, which the walk of `select.c` does after `sqlite3ProcessJoin` marks the terms with the side the join is on; an inner join carries no such mark, its `ON` being read as a `WHERE`. Checking one `ON` costs O(n) over its nodes. Reason: SQLite's own files hold 6 cases of the first and 14 of the second. SQLite's own files: `join.test` goes from 126 cases passing to 132, `joinI.test` from 23 to 27, `join2.test` from 30 to 32 and `joinH.test` from 15 to 16. | decided | 2026-09-17 |
| D-280 | The tester writes a real a statement stands a variable for with `%.17g`, which is every digit the bits of the double carry. `tcl_precision` is 15, which SQLite's own tester sets, so the string form of a double is fifteen digits; `testfixture` hands the C library the double itself through `sqlite3_bind_double` and never that string, so a harness that writes the string into the statement hands the engine another number. Reason: `atof1.test` compares a real written as `%.32e` against the same real stood for by a variable, and 476 of its cases refused because the two were not the same double. SQLite's own files go from 63 641 cases passing to 70 257: `atof1.test` from 614 to 7280 with none refused. | decided | 2026-09-17 |
| D-279 | `crate::change::Writer` keeps the bytes of the file beside the schema cookie they were taken under, and `constrained` reads the schema out of them rather than building the file again. A constraint reads the schema and no row — a `CHECK` reaches no table and a generated column reaches none either — so bytes taken under one cookie answer for every row a statement writes under it; a statement that changes the schema moves the cookie and the bytes are taken again. Building them per row costs O(n) in the pages of the file, which made a statement that writes n rows cost O(n²): an `INSERT` of a hundred thousand rows of two hundred bytes each reached the sixty-second deadline, and now takes four seconds. The cost is one further copy of the file, against the three the pages already hold for the journal. The option not taken is handing the open database down from each of the six callers, which carries a borrow of the bytes through every path that writes a row. SQLite's own files: `temptable2.test` goes from 1 case passing to 53, `tkt2686.test` from 2182 to 2269, `joinD.test` from 74 to 111, and `index4.test`, `ovfl.test` and `securedel2.test` run at all where the deadline ended them. | decided | 2026-09-17 |
| D-278 | `crate::tree::balance` counts the room in the parent before it chooses `balance_quick`, and leaves the leaf to the balance proper where the parent has none. The routine writes a divider naming the page that was full into the parent, and refused the whole statement with `Error::Balance` where the parent would not hold it; the C library lets the parent overflow there and balances it in the next turn of its loop. A tree over 512-byte pages reached that refusal after nine hundred rows, and the schema of a database over such pages was held to the one page the header is on. Counting the room reads one page, which is O(1) against the O(log n) the balance already costs. The option not taken is letting the parent overflow, which is a second list of spilled cells to carry up the stack. Reason: the goal asks for the engine to be exercised under a variety of page sizes. SQLite's own files under `utf8-512-delete` go from 60 186 cases passing to 61 073: `in2.test` 1 to 1329, `changes.test` 52 to 66 and `seekscan1.test` 1 to 6. Four files that refused the balance early now do the work and reach the sixty-second deadline instead. | decided | 2026-09-17 |
| D-277 | A value in the write path carries its text in UTF-8, and `crate::record::write_in` writes it into the encoding the file names, which is the one place the conversion happens. Reasons: 1. `sqlite3ApplyAffinity` reads text as a number, and the bytes of UTF-16 are not the digits of one, so `'03'` into an `INTEGER` column was stored as text under UTF-16 where the C library stores 3. 2. A value that reached a record through two paths was written into that encoding twice, so a trigger carrying `new.a` into another table wrote the bytes of the bytes. 3. A row read out of the file arrives in UTF-8, so a path that writes it again needs no conversion of its own. The trees keep the invariant at their own edge: `crate::tree::Ordering` carries the collation of each column and the encoding, and `insert_entry`, `remove_entry` and `entry_tail_at` write the key into that encoding, because an index is ordered by the bytes the file holds. `crate::check` writes the entry a row would make into that encoding before it holds it against the entries the index carries. The option not taken is reading every stored value into UTF-8 as it is compared, which orders an index differently from the C library, whose `BINARY` compares the bytes the file holds. SQLite's own files under `utf16le-4096-delete` go from 59 909 cases passing to 61 214, against 61 750 under UTF-8, and `types2.test`, `affinity2.test`, `bigrow.test`, `boundary4.test` and `where5.test` answer exactly what they answer under UTF-8. | decided | 2026-09-17 |
| D-276 | A `DO UPDATE` writes the row the conflict found and the values the clause sets in the encoding the file names: the row is read out of the file into UTF-8 and written back, and a value the clause computes is text of a statement, which never is. Both paths of the clause — a table with a rowid and one without — wrote UTF-8 into a UTF-16 file, so the text read back as nothing. Reason: SQLite's own `upsert5.test` writes text through every `ON CONFLICT` target. SQLite's own files under `utf16le-4096-delete`: `upsert5.test` goes from 50 cases passing to 248, which is what it answers under UTF-8. | decided | 2026-09-17 |
| D-275 | The schema and the index trees hold their text in the encoding the file names, so a name a statement carries is read into UTF-8 before it is compared against a row of `sqlite_schema`, and a row a `CREATE INDEX` or a `REINDEX` reads out of the file is written back into that encoding before it becomes an entry. A `DROP` of a table, an index, a view or a trigger compared the bytes of the schema against the name the statement carried and found nothing under UTF-16; an index filled from rows already read held UTF-8 text, which a descent under UTF-16 never matched. `sh tools/xtask.sh sqlite-suite --configuration <name>` runs every file under one of nine configurations — the three encodings, page sizes 512, 1024, 4096 and 65536, and the journal modes `persist`, `truncate` and `wal` — which is the part of `permutations.test` this harness opens a connection under. The option not taken is reading every index entry into UTF-8 as it is compared, which costs a conversion per entry of a descent and orders the tree differently from the C library, whose `BINARY` compares the bytes the file holds. Reason: the goal asks for the engine to be exercised under a variety of run-time configurations. SQLite's own files: `select1.test` answers 181 cases the same under all nine configurations, where UTF-16 answered 44. | decided | 2026-09-17 |
| D-274 | A function an application defines takes any number of arguments where `crate::func::Defined::count` is nothing, which is `nArg` of `sqlite3_create_function` at -1, and is given the name it was called under, so one function answers for every name an application defined. A `COLLATE` written on a whole number of an `ORDER BY` or a `GROUP BY` is taken off before the number is read, which `sqlite3ExprSkipCollateAndLikely` does inside `resolveOrderGroupBy`, so `ORDER BY 1 COLLATE numeric` counts to the first answered column and sorts it under `numeric`; the term was read as a constant before, which left the rows in the order they were written. The harness reaches a function the tester defined over the same line a collation is reached over, and refuses a proc that runs a statement of its own, because the answer to the call is read from that line. The option not taken is a second line per connection, which costs a socket per connection and an interpreter that reads two. Reason: 55 of SQLite's own files define a function in TCL. SQLite's own files: `collate1.test` goes from 37 cases passing to 57 with none answering differently, `selectC.test` from 19 to 23 and `trigger6.test` from 0 to 6. | decided | 2026-09-17 |
| D-273 | An application defines collations on a connection, which `crate::value::Collating` carries as a name and a comparison function pointer, and which the whole read path is told as `&'static [Collating]`: `crate::change::Writer::collates` takes them, `crate::db::Database::open_collating` and `open_log_collating` read a schema against them, and `crate::value::Collation::Defined` names one inside a comparison. A name a statement carries is looked up where the statement is read, which `crate::schema::collations` does by walking every `Node::Collate` of the arena in O(n) over its nodes, so a statement over an empty table is refused too. The comparison is a bare function pointer and carries nothing, because `Collation` is `Copy` and sits inside every column of the schema; the harness reaches the tester's proc through a thread-local line. The option not taken is a boxed closure, which costs an allocation per column and takes `Copy` off `Collation`. Reason: SQLite's own files define a collation in TCL and then read every ordering path through it. SQLite's own files: `collate1.test` goes from 18 cases passing to 37, `collate2.test` from 9 to 118, `collate3.test` from 3 to 34, `collate4.test` from 19 to 67, `collate5.test` from 9 to 36 and `collate9.test` from 1 to 18. | decided | 2026-09-17 |
| D-272 | `crate::token::complete` answers whether a text ends a statement, which is `sqlite3_complete`: the state machine of eight states and eight tokens, read from the table that file carries, so a `CREATE TRIGGER` ends only after the `END` of its body. It reads every byte once, which is O(n) in the text. `NULLS FIRST` and `NULLS LAST` are refused where an index takes its terms, which `sqlite3HasExplicitNulls` does, so the parser reads such a list through `indexed_list` and the four places that take one — a `CREATE INDEX`, a `PRIMARY KEY`, a `UNIQUE` and an `ON CONFLICT` target — share it. Reason: SQLite's own tester asks a connection whether a text is complete before it runs it, and the harness answered that every text was. SQLite's own files: `main.test` goes from 61 cases passing to 87 and `nulls1.test` from 81 to 94. | decided | 2026-09-17 |
| D-286 | A table whose largest key is 9223372036854775807 gives the next row a key drawn at random, which is `OP_NewRowid` past `MAX_ROWID`: `crate::change::Writer::free_key` takes a word from the random source, holds it to 62 bits, adds one, and answers that key where the tree holds no row under it, up to a hundred draws, after which the write is refused `database or disk is full`. A table whose key counts up draws nothing and is refused at that key, which is `pMem->u.i==MAX_ROWID` of the same routine. `Writer::keying` no longer works the key out: `Writer::given_key` reads the key the statement carries, and the caller draws where the statement carries none. One draw reads the tree, so a draw costs O(log n) in the rows and a write at the largest key costs O(k log n) in the draws it takes. SQLite's own `rowid.test` goes from 138 cases passing to 239. The option not taken is one past the largest key, which names the row already there and refuses `UNIQUE constraint failed`. | decided | 2026-09-18 |
| D-287 | `save_prng_state` and `restore_prng_state` reach the random source over the line: `crate::random::Source::held` answers what the next draw follows from, `Writer::randomness_held` hands it out, and `Writer::randomness` draws from it again. SQLite draws for the whole process from one source, so the state one writer answers is handed to every writer the file holds. Reading the state and setting it each cost O(1). This is what lets the hundred cases of `rowid-12.3` each reach a key the draw before them took. The option not taken is a seed of its own per restore, which draws words the statements before it did not. | decided | 2026-09-18 |
| D-288 | The `sqlite3_mprintf_*` commands of `src/test1.c` reach `crate::format::format`: the harness's `mprintf` request carries the format and one value per argument, each under the C type the command hands the format — a 32-bit `int`, a 64-bit one, a double, the sixteen hexadecimal digits of a double, a string, or a null pointer. `%x`, `%X`, `%o` and `%u` read an `unsigned int`, so the harness reads a negative `int` argument of those conversions as the same 32 bits without the sign, and `%c` reads an `int`, so the harness reads that argument as the character the number names; which argument a conversion reads is found by scanning the format and counting the argument a width or a precision of `*` reads. Scanning costs O(n) in the bytes of the format. SQLite's own `printf.test` goes from no case running to 1382 passing and none answering differently. The option not taken is handing every argument as the SQL function would read it, which answers 64 bits where the C command hands 32 and the digits of a number where it hands a character. | decided | 2026-09-18 |
| D-289 | `btree_varint_test` reaches `crate::bytes::varint_again`, which writes a value as a varint, reads it back, and answers the value with how many bytes the write took and how many the read took; the harness's `varint` request runs that over the count of values the command names and refuses naming the first value that did not come back. `crate::bytes` is public for it, and nothing else of the module is. One value costs O(1), so a request costs O(n) in the count. SQLite's own `varint.test` goes from no case running to 160 passing. The option not taken is a roundtrip written in the harness, which would hold the harness's own codec against the file rather than the engine's. | decided | 2026-09-18 |
| D-290 | The limits of `src/sqliteLimit.h` are variables of the tester, each this engine's own limit where the engine holds one: `SQLITE_MAX_EXPR_DEPTH` is 200, which `crate::parse::MAX_DEPTH` is, `SQLITE_MAX_LENGTH` is 1000000000, which `crate::func::MAX_LENGTH` is, and the rest are the defaults of the header. `sqlite_pending_byte` is 0x40000000, which `test2.c` links to `sqlite3PendingByte`. A file reads one of these to skip a case its build cannot reach, so a file that reads one stopped there before. The option not taken is the defaults of the header throughout, which would tell a file this engine reads an expression 1000 deep. | decided | 2026-09-18 |
| D-291 | A file that has 5000 cases in a row refused for the same reason is ended there, because a loop whose end a command this harness has none of decides runs without bound: `bitvec.test` counts up until `sqlite3BitvecBuiltinTest` answers that no allocation failed, and refused 544 644 cases before the deadline ended it. The count is per reason and is reset by a case that passed or answered differently, so a file that refuses 1868 cases for one reason and runs on, which `where7.test` does, is untouched. The option not taken is the deadline alone, which spends sixty seconds of the run on a file that scores nothing. | decided | 2026-09-18 |
| D-292 | `PRAGMA wal_checkpoint` writes the pages the log holds into the database file, which is `sqlite3_wal_checkpoint_v2`: `crate::change::Writer::checkpoint` answers three columns — nought, because one connection writes here and no other holds the log back, and twice how many frames the log holds, which `crate::wal::Log::frames` counts. `TRUNCATE` and `RESTART` begin the log again at once, which `Log::restart` does by raising the count of checkpoints and the first salt so that no frame of the log before it reads as one of this log, and `TRUNCATE` answers two noughts because the log it leaves holds no frame; every other mode leaves the frames where they are and the commit after the checkpoint begins the log again, which is `walRestartLog`. A file that is not logging answers minus one twice. The file the checkpoint writes carries the change counter the frames carried, which is `pager_write_changecounter`. Writing the file costs O(n) in its pages. SQLite's own `walslow.test` goes from 912 cases passing to 3573. The option not taken is refusing the pragma, which is what this crate did and which left 51 files of the suite stopped at their first checkpoint. | decided | 2026-09-18 |
| D-293 | The harness answers how many bytes it holds under a name, which the `size` request asks for: the database itself, the log beside it, or the journal beside it, and nothing where it holds no such file. The tester puts its own `file` in front of TCL's, so `file size` and `file exists` over one of those names answer out of the harness and every other name reaches TCL's own command; 481 `file size` commands in 102 files read a database the machine holds no file of. `tools/suite/wal_common.tcl` is then the suite's own file: `wal_frame_count` is arithmetic over that size and `wal_cksum` the checksum of section 4.2, and only the commands that write the log's header through the C library's virtual file system raise. Reading a size writes the database out, so it costs O(n) in its pages. SQLite's own `wal.test` goes from no case running to 180 passing and `wal3.test` from 245 to 296. The option not taken is writing every file to the machine's disk, which would hold the harness to the machine's own file system for a database it already holds in memory. | decided | 2026-09-18 |
| D-294 | `do_multiclient_test` runs the round of `lock_common.tcl` where all three connections stand in this interpreter, and not the round where two of them stand in processes of their own: this harness holds one writer per file and as many connections over it as a file opens, so `code2` and `code3` reach `uplevel #0` and `db2` and `db3` are connections over the same path. The round the suite numbers 1 needs `launch_testfixture`, which starts another `testfixture`. SQLite's own `wal5.test` goes from no case running to 3 passing, and the files that read `do_multiclient_test` no longer end at their first use of it. The option not taken is the empty body this harness had, which counted every case of such a file as one that did not run. | decided | 2026-09-18 |
| D-295 | A command of `testfixture` that names something this engine holds none of answers rather than raises, where the answer changes nothing a case reads: `test_set_config_pagecache` sizes a page cache this engine has none of, `sqlite3_simulate_device` names the sector size and the properties of the device under the file, and `sqlite3_test_control_pending_byte` moves the byte-range a lock takes. `sqlite3_libversion`, `sqlite3_libversion_number` and `sqlite3_sourceid` answer the version this engine writes into every file it makes, and `sqlite3_wal_checkpoint_v2` is `PRAGMA wal_checkpoint` of the connection it names. A file that read one of these at its top level ended there before. The option not taken is raising, which counts every case after the command as one that did not run rather than as one that answered differently. | decided | 2026-09-18 |
| D-296 | A file that stops is counted by the first line of what it stopped at, cut to 72 characters, and not by the first two words of it: the two words name the kind of the message and the line names the command, which is what says what to build next. The option not taken is the first two words, which gathered 180 files under `stopped: THIS HARNESS` and named none of the commands they wanted. | decided | 2026-09-18 |
| D-297 | `hexio_read` and `hexio_write` of `test_hexio.c` reach the bytes the harness holds, which the `read` and `write` requests carry as the capital hexadecimal digits `sqlite3TestBinToHex` writes: `read` answers the bytes of a database, a log or a journal from an offset, and `write` puts the bytes those digits name back and reads the database again from what the write left, so a write that leaves a file this engine cannot read is refused. `hexio_get_int`, `hexio_render_int16` and `hexio_render_int32` are arithmetic and stand in the tester. Reading costs O(n) in the bytes it answers and writing O(n) in the bytes of the file. SQLite's own `corruptF.test` goes from 4 cases passing to 266 and `alter2.test` from no case running to 12. The option not taken is holding the raw bytes beside the writer, which would let a write leave a file no connection can open; a file this engine cannot read is one the harness cannot answer a statement over. | decided | 2026-09-18 |
| D-298 | The compile options a file reads as `$::sqlite_options(name)` are an array of the tester, each nought where `ifcapable` answers that this engine has none of it and one otherwise, and the variables `SQLITE_DEFAULT_FILE_FORMAT`, `AUTOVACUUM`, `TEMP_STORE` and `bitmask_size` carry 4, 0, 1 and 64. Under D-295, `sqlite3_test_control`, `sqlite3_soft_heap_limit`, `sqlite3_hard_heap_limit`, `sqlite3_shutdown`, `sqlite3_initialize`, `sqlite3_db_config_lookaside`, `optimization_control`, `extra_schema_checks`, `test_restore_config_pagecache`, `unregister_devsim` and `translate_selftest` answer rather than raise. SQLite's own `expr.test` goes from 315 cases passing to 613, `date.test` from 1217 to 1371 and `without_rowid3.test` from 891 to 1088. The option not taken is raising for each, which ended 17 files at their first use of one. | decided | 2026-09-18 |
| D-299 | `launch_testfixture` names no channel and `testfixture` runs the script it was given in this interpreter, because the harness holds one writer per file and a connection another `testfixture` would open over that file is a connection over the same writer here; the form that takes a callback answers nothing, because nothing runs later. `::cmdlinearg` carries the defaults of `tester.tcl` lines 378 to 391, which this harness takes off no command line. `sqlite3_connection_pointer` answers the name of the connection, because every command that takes that pointer is a stand-in here. Under D-295, `load_static_extension`, `sqlite3_soft_heap_limit64`, `sqlite3_config_uri`, `sqlite3_register_cksumvfs` and `sqlite3_multiplex_initialize` answer rather than raise, and the functions a module would carry stay missing. Of 757 files 20 more answer a case, `pragma.test` going from none to 48 and `avtrans.test` from 23 to 58. The option not taken is another `tclsh` per connection, which would hold each its own writer and no two of them the same file. | decided | 2026-09-18 |
| D-300 | A row a foreign key action writes is held to the constraints of the table it writes and carries the chain of keys past that table: `crate::change::Writer::rewrite` runs `constrained` over the values and then `orphaned` over the row, which is `sqlite3FkActions` writing the row through a trigger of its own, so `UPDATE ab SET a=5` over `ab`, `cd` and `ef` of `fkey2-3.1.3` reaches the `CHECK (e!=5)` of `ef` and is refused. The key the action wrote points at the row that changed, which is written after the action runs, so that key alone is held by construction and not read again. The chain is bounded by `TRIGGER_DEPTH`, which `Writer::deepened` counts against by pushing a name carrying a byte no identifier holds, so a chain of 40 tables each pointing at the one before it is refused rather than reaching the end of the stack. One step of the chain reads the schema, so a chain of k steps costs O(k n) in the pages of the file. SQLite's own `fkey2.test` goes from 1020 cases passing to 1026 and `without_rowid3.test` from 1088 to 1094. The option not taken is holding the row to its own keys as well, which reads the parent row before the write that makes it and refuses a cascade the C library carries. | decided | 2026-09-18 |
| D-301 | `sqlite3_table_column_metadata` answers out of the schema the harness holds: the type the column was declared with, the collation it compares under, whether it may be nothing, whether it stands in the primary key, and whether that key counts up. `rowid`, `oid` and `_rowid_` name the key of a table that holds its rows under one, unless a column of the table carries that name, and a table that holds its rows in the key's own tree has no such column. A connection that opens again writes `NULL` as the empty string, which `sqlite3` of the suite's own tester leaves it at, so a file that set another rendering before reopening does not carry it over. SQLite's own `colmeta.test` goes from 2 cases passing to 48 and `joinH.test` from 45 to 58. The three cases left in `colmeta.test` read the collation under the name the schema spelled it with, and this crate holds the collation and not the spelling. | decided | 2026-09-18 |
| D-302 | A file of the suite is ended after three minutes and not after one: `rowvalue2.test` answers 3834 cases in about two minutes and was cut at some 2900 under the shorter deadline, and `in2.test`, `joinD.test`, `savepoint6.test` and `tkt2686.test` were cut at a different case each run, so a total of the suite was exact to about a tenth of a percent and a comparison of two runs had to allow for it. Ten files answer more cases under the longer deadline and the suite goes from 81 726 cases passing to 83 637, at 47 minutes for one run over two jobs rather than 13. The option not taken is no deadline at all, which lets a file that loops without end stop the run. | decided | 2026-09-18 |
| D-303 | `now` names the moment the caller tells the connection, which `crate::change::Writer::clocking` and `crate::db::Database::clocked` take as the seconds since 1970 and `crate::date::julian_of` reads as the julian day number times 86 400 000: `setDateTimeToCurrent` reads the clock of the operating system, which this crate has none of. A call that names no moment reads the same clock, so `datetime()` is `datetime('now')`, and `CURRENT_TIME`, `CURRENT_DATE` and `CURRENT_TIMESTAMP` are `time`, `date` and `datetime` of it, which is `currentTimeFunc`. A connection told no clock answers nothing for `now` and refuses the three literals. `crate::func::Given` carries the clock beside the random source and the counters, because a function reads all three and `call` took seven arguments already. The harness sets the clock from `sqlite_current_time`, which a write trace of the tester carries over the line, and nought there is the clock of the machine and so no clock at all. Reading the clock costs O(1). SQLite's own `date.test` goes from 1371 cases passing to 1395 and `e_expr.test` answers 374 with no case differing. `localtime` is still refused, because the rules of a time zone are what it needs and this crate is given none. The option not taken is a clock the crate reads itself, which needs an operating system under it. | decided | 2026-09-18 |
| D-304 | A connection that closes leaves the file it read no transaction: the harness runs a `ROLLBACK` over the writer of that path, because `sqlite3_close` rolls back the transaction the connection had open and this harness holds one transaction per file. A case that ends inside a transaction — which the command it names raising does — left that transaction open before, and every `BEGIN` after it was refused `cannot start a transaction within a transaction`: `tkt-9d68c883.test` refused 198 cases for that and now refuses 100 for the command it names, and `temptable2.test` answers 241 cases where it answered 167. The option not taken is a writer per connection, which would hold each its own file and no two of them the same rows. | decided | 2026-09-18 |
| D-305 | `VACUUM` makes the database again from nothing, which is `sqlite3RunVacuum`: `crate::change::Writer::vacuum` reads the rows of the schema in the order the schema tree holds them, makes every table and index of them in a writer of its own, writes every row under the key it had, and then makes the views and the triggers, so no trigger fires over a row the vacuum wrote and no constraint is read again. The file it leaves holds the rows in the order of their keys and no free page. A table the engine writes for itself is there already where a table of the schema made it, which `sqlite_sequence` is for a key that counts up, so a statement of the schema that names one is passed over. `PRAGMA page_size` written after the first table is kept as `Writer::wanted_page` and is the size the vacuum writes the file under, which is the only thing that can write it again under another size. A `VACUUM` inside a transaction is refused `cannot VACUUM from within a transaction`, a schema this connection does not hold `unknown database X`, and `VACUUM INTO` is refused because it writes a file this crate hands no caller. It costs O(n) in the rows and O(k log n) in the entries of k indexes. SQLite's own vacuum files answer 146 cases where they answered none, `ifcapable vacuum` having skipped every one of them. The option not taken is moving the pages of the file in place, which is `incrVacuumStep` and which the pointer maps decide, where writing the file again reads the schema alone. | decided | 2026-09-18 |
| D-306 | A pragma no version of the library holds answers no row rather than refusing, which is `sqlite3Pragma` for a name it does not know: `crate::pragma::Setting::Ignored` now answers nothing when read as well as when set, and `default_synchronous` is one of those names. This amends the scope of D-202, which chose refusal for the pragmas a connection holds a value for: a name the C library itself does not know is not one of those, and `cksum` of the suite reads `default_synchronous` on every call. A name this crate holds nothing for at all is still refused. SQLite's own `vacuum.test` goes from no case running to 35 passing. | decided | 2026-09-18 |
| D-307 | The suite's own harness answers `md5` and `md5file` with the digest of RFC 1321, which `crates/tools/xtask/src/md5.rs` writes against `docs/rfc/rfc1321.txt`: section 3 gives the five steps, section 3.4 the table of 64 sine constants and the shift amounts, and section A.5 the seven digests the module's own test holds it against. `cksum` of the tester writes one state of a database as a text and answers its length and its digest, which is how the suite holds the state before a `VACUUM` against the state after it. The digest is a checksum of what the engine answered and no part of the engine. 36 files of the suite read `md5` or `cksum`. The option not taken is a digest of this project's own, which `func.test` would refuse: it holds `md5sum` of the engine against `md5` of the tester and both must be the same function. | decided | 2026-09-18 |
| D-308 | An application defines an aggregate on a connection, which `crate::func::Grouped` carries as a name, a count of arguments and a function over the rows of a group: `sqlite3_create_function` with a step and a final keeps state per group, and this crate holds the arguments of every row the group stepped and reads them at once, so the function keeps no state of its own. `crate::agg::Aggregate::Defined` carries the name and the function, `agg::lookup_in` and `agg::named_in` read the connection's list beside the table of the library's own, and `window::lookup_in` lets an `OVER` follow one. `Writer::groups` and `Database::grouping` take the list, and `crate::eval::Row::grouped` carries it to where a call is read. A group of no row answers the function over no row at all, `DISTINCT` puts the rows through one column before they are stepped, and a name the connection was not told of is no function at all. Holding the rows costs O(n) in them, which is what a function that reads them at once needs. The harness answers `md5sum` of `src/test_md5.c` this way, which 30 files of the suite call. This is what D-270 left open. The option not taken is a step and a final with state between them, which needs a place per group for the application to write into and answers the same for every aggregate the suite calls. | decided | 2026-09-18 |
| D-309 | `PRAGMA case_sensitive_like` sets whether `LIKE` tells the twenty-six letters apart, which `sqlite3RegisterLikeFunctions` of `research/sqlite/src/pragma.c:1658` does by registering the function again: the connection holds a truth value, `crate::func::Given::sensitive` carries it to `crate::func::pattern`, and the `fold` field of the pattern is its negation. `PragFlg_NoColumns` stands against the name in `research/sqlite/pragma.h:200`, so the pragma answers no row set or read, which is why `crate::pragma::Setting::CaseSensitiveLike` is its own setting and not a row of `HELD`. A word that names no truth value turns it off, which is `sqlite3GetBoolean(zRight, 0)`. `GLOB` tells the letters apart whatever the pragma says. `crate::eval::Row::sensitive` reads false for a caller that opens a database and says nothing, so a statement costs what it did. SQLite's own `like.test` goes from 44 cases passing to 48. The option not taken is holding two `Function` values, one folding and one not, which would leave the `like` function of one name answering under two codes. | decided | 2026-09-18 |
| D-310 | `PRAGMA default_cache_size` writes the word at offset 48 of the header and the cache size of the connection together, which is `OP_SetCookie` with `BTREE_DEFAULT_CACHE_SIZE` and `pDb->pSchema->cache_size = size` of `research/sqlite/src/pragma.c:577`: the word is `sqlite3AbsInt32(sqlite3Atoi(zRight))`, so `-123` and `123` write 123, and text that names no number writes nought. Reading the pragma answers the word where it is above nought, its negation where it is below and −2000 where it is nought, which `getCacheSize` of `research/sqlite/src/pragma.c:555` and `sqlite3InitOne` of `research/sqlite/src/prepare.c:325` both do. `PRAGMA cache_size` stays a row of `HELD` and answers the word where the connection was told nothing, so a reopen reads the file and a `PRAGMA cache_size = N` stands over it for that connection alone. SQLite's own `pragma.test` goes from 47 cases passing to 58. The option not taken is keeping `default_cache_size` in `HELD` with a fixed fallback, which answers −2000 for a file another connection wrote a word into. | decided | 2026-09-18 |
| D-311 | The suite's own harness answers `sqlite3_exec` in TCL rather than refusing it, which `test_exec` of `research/sqlite/src/test1.c:421` answers as a list of the code and either the column names followed by every row's values or the message: `tools/suite/tester.tcl` runs the statement through the connection, reads the names through the `names` request, and answers them only where a first row arrived, which is what `exec_printf_cb` writes. The `names` request now answers for the last statement of a text that reads rows and not for the last statement of any kind, so `sqlite3_exec db {/* comment */;;; SELECT * FROM t1; /* comment */}` of `exec-1.2` carries the names of the `SELECT`. `exec.test` goes from 2 cases passing to 3 and `like.test` reaches the four cases that follow `sqlite3_exec db {PRAGMA case_sensitive_like=on}`. The option not taken is a request of its own for the whole call, which would put the shape of a TCL list into the engine's harness. | decided | 2026-09-18 |
| D-312 | A pragma writes the three words of the header `PragTyp_HEADER_VALUE` writes, reads the two it may not write, and refuses two values the C library refuses: `crate::change::Writer::pragma_write` writes `schema_version`, `user_version` and `application_id` through `crate::pragma::header_word`, which is `sqlite3Atoi(zRight)` of `research/sqlite/src/pragma.c:2340` kept as the bytes of a word of 32 bits; `freelist_count` carries `PragFlg_ReadOnly` and `PragTyp_PAGE_COUNT` reads where the name begins with `p`, so both answer the file whatever stands after the equals sign; `PragTyp_ENCODING` of `research/sqlite/src/pragma.c` reads the name against its own table before it reads whether the schema is there, so `PRAGMA encoding=bogus` answers `unsupported encoding: bogus` over a file that already holds a table; and `PragTyp_SYNCHRONOUS` answers `Safety level may not be changed inside a transaction` where the connection has one open. The cookie the pragma writes invalidates the schema bytes held beside it by construction, because those bytes carry the cookie they were read under. `SQLITE_DBCONFIG_DEFENSIVE`, which turns the write of `schema_version` into a no-op, is not here, so `pragma-8.1.3` still writes the word. SQLite's own `pragma.test` goes from 58 cases passing to 69. The option not taken is a write for every word of the header, which would let a statement set the page count the pages do not have. | decided | 2026-09-18 |
| D-313 | A connection is told one function that every statement is read against, which is `sqlite3_set_authorizer` and `sqlite3AuthCheck` of `research/sqlite/src/auth.c:196`: `crate::auth::Asking` is a bare function over `crate::auth::Asked`, which carries the action and the four arguments the C library passes, and answers `Ok`, `Deny` or `Ignore`. `crate::auth::Authorizer` reads one statement against the schema it runs over and asks the function once per action, in the order `resolveSelectStep` of `research/sqlite/src/resolve.c:1979` resolves the names: the `LIMIT`, the `ON` of every source, the result columns, the `HAVING`, the `WHERE`, the `ORDER BY` and the `GROUP BY`, with a statement written inside an expression read as a statement of its own. A denial refuses the statement with `not authorized`, a denied read with `access to table.column is prohibited` and a denied function with `not authorized to use function: name`, which are the three messages `sqlite3ErrorMsg` writes. An ignored action leaves the statement undone, which is the `if( sqlite3AuthCheck(...) )` every caller of it writes; an ignored read answers a null for that column, which `crate::db::Database::nulled` writes into the row of the side; an ignored column of an `UPDATE` keeps the value it had, which is `aXRef[j] = -1` of `research/sqlite/src/update.c:514`; an ignored `DELETE` writes the rows all the same, because the answer only leaves the whole-table shortcut, which this crate has none of. Reading one statement costs O(n) in its nodes and O(k) in the tables of the schema it names, and a connection told no function reads nothing. `SQLITE_ATTACH` and `SQLITE_DETACH` are not among the actions, because this crate holds one file per connection. SQLite's own `auth.test` goes from 4 cases passing to 142, `auth2.test` from 1 to 5, `auth3.test` from 4 to 6, `alterauth.test` from 1 to 6, `altercons2.test` from 44 to 48 and `savepoint.test` from 29 to 35. The temporary schema is what the rest of `auth.test` waits on: a table this crate writes under `TEMP` stands in `main`, so the action and the schema of a later statement over it name `main`. The option not taken is asking the function where each row is read, which would ask once per row where `sqlite3AuthCheck` asks once per statement. | decided | 2026-09-18 |
| D-314 | The suite's own harness holds a statement the tester prepared and reads its rows out of what the statement answered once, rather than one row per step: `crates/tools/xtask/src/suite.rs` keeps a `Prepared` per name, runs the whole statement at the first `sqlite3_step`, and answers `SQLITE_ROW` while the reader stands on a row. `sqlite3_prepare` reads past the semicolons and the comments no statement stands in, so the first statement of a text is the first one that carries meaning and the tail begins after the semicolon that ends it; a statement that reads is run once at prepare with every parameter unbound, so the names of its columns are known before the first step and a statement the engine refuses raises `(1) message`, which is what `test_prepare` of `research/sqlite/src/test1.c` writes. What the tester binds is written into the statement as a literal, at the place `sqlite3ExprAssignVarNumber` counts: a `?` takes the next place, a `?N` the place it names, and a name the place it was first written at, with a `$name(...)` carrying the brackets the TCL interface writes for a member of an array. `db_sqlite::change::Writer::began` answers `sqlite3_get_autocommit`. SQLite's own `capi2.test` goes from 6 cases passing to 59, `bind.test` from 18 to 75, `capi3c.test` from 2 to 28, `analyze3.test` from 10 to 27, `capi3.test` from 2 to 10 and `errmsg.test` from 2 to 4. A step of a statement that writes runs the statement, so a file that steps one row of a write and then reads the table sees every row written; the byte counts of `sqlite3_bind_text` and the 16-bit forms are not read, so `bind.test` still answers the whole text where the C library answers the first bytes of it. The option not taken is a statement that walks its rows as it is stepped, which needs the engine to hand out a cursor rather than a list of rows. | decided | 2026-09-19 |
| D-315 | `crate::normalize` writes one statement again with every literal as a `?`, in the two forms the C library answers: `normalized` is `sqlite3_normalize` of `research/sqlite/ext/misc/normalize.c:556`, which writes every word in lower case and answers nothing for a byte no rule accepts, and `normalized_sql` is `sqlite3Normalize` of `research/sqlite/src/tokenize.c:782`, which writes an identifier in lower case and every other word in upper case. Both read the statement once through `crate::token::token`, so each costs O(n) in its bytes. The right side of an `IN` becomes `?,?,?` unless a statement opens it; a `NULL` after `IS` or `NOT` is a word of the language and every other one a value; a name in double quotes is written bare where it is one identifier and quoted where it is not. `normalized_sql` takes the names the statement may read, which are the tables of the schema and their columns: a word in double quotes that is none of them is a text and is written as a `?`, which stands for `sqlite3VdbeUsesDoubleQuotedString` of `research/sqlite/src/tokenize.c:861`, and an `ATTACH` or a `DETACH` names a file rather than a column, so a word in double quotes there is a name whatever the names hold. SQLite's own `normalize.test` goes from 3 cases passing to 66 of 69; the two that are left read an error message the engine writes differently. The option not taken is carrying a mark from the parser for every word it read as a text, which is what the C library does and what needs the resolver to write into the tree. | decided | 2026-09-19 |
| D-316 | Every refusal carries the result code the C library answers for it, which `crate::db::Error::code` writes as `crate::db::Code`: the number and the name `sqlite3_errcode` answers and the ones `sqlite3_extended_errcode` answers, the names being the ones `sqlite3ErrName` of `research/sqlite/src/main.c` writes. A row a statement could not write carries `SQLITE_CONSTRAINT` with the constraint it broke, which is `SQLITE_CONSTRAINT_UNIQUE`, `SQLITE_CONSTRAINT_NOTNULL`, `SQLITE_CONSTRAINT_CHECK`, `SQLITE_CONSTRAINT_FOREIGNKEY` or `SQLITE_CONSTRAINT_DATATYPE`; a function the authorizer denied carries `SQLITE_AUTH`; a file the format forbids carries `SQLITE_NOTADB` where the header says the file is no database and `SQLITE_CORRUPT` where a byte under the header is damaged, with the messages `sqlite3ErrStr` writes for them; and every statement the engine could not read carries `SQLITE_ERROR`. The harness keeps the code of the last refusal per run, which `sqlite3_errcode`, `sqlite3_extended_errcode` and the message `sqlite3_prepare` raises read, and a statement `sqlite3_prepare` made answers `SQLITE_ERROR` for every refusal while one `sqlite3_prepare_v2` made answers the code it carries, which is what `SQLITE_OMIT_AUTORESET` and the v2 interface of `research/sqlite/src/vdbeapi.c` divide. A step past the last row resets the statement and answers its first row again. SQLite's own `capi2.test` goes from 60 cases passing to 69. The option not taken is one code per refusal with no extended code, which `capi2.test` reads both of. | decided | 2026-09-19 |
| D-317 | A statement answers the type the schema declares for every column it answers, which `crate::db::Answer::declared` carries one entry per column of: `crate::db::Column` holds the declared type of the column it came from, `shape_of` fills it out of the schema, and `declared_of` reads it for a result column that names one column of a side. A column a `*` stands for carries it because the walk clones the column; a column a statement written inside the `FROM` answers carries the type of the column it came from, which `columnTypeImpl` of `research/sqlite/src/select.c` reads through it; a bare `rowid` of a side that keeps its rows under a key carries `INTEGER`; and a column that came from an expression carries nothing. The harness answers `sqlite3_column_decltype` with it, and it answers `stepsql` of the suite's own tester by preparing and stepping every statement of a text in turn. SQLite's own `capi2.test` goes from 69 cases passing to 103, `schema2.test` from 5 to 34, `schema.test` from 5 to 14, `analyze3.test` from 27 to 29 and `errmsg.test` from 4 to 5. `sqlite3_column_table_name` and `sqlite3_column_origin_name` are not answered, because the column carries the type it was declared with and not the table it came from. The option not taken is reading the declared type where a column is read rather than where the shape is built, which reads the schema once per row. | decided | 2026-09-19 |
| D-318 | `X REGEXP Y` and `X MATCH Y` reach the function the application defined under that name, which is `sqlite3FindFunction` looking `regexp` and `match` up as it looks `like` and `glob` up: `crate::eval::like` reads the pattern and the value as the two arguments, in that order, calls the function the row holds, and reads what it answered as a truth value, so `NOT REGEXP` answers the rows the function did not hold for and a row of nothing answers nothing. A connection told neither name is refused `no such function: REGEXP` or `no such function: MATCH`, which is what the library answers for a name it holds no function under. SQLite's own `e_expr.test` goes from 374 cases passing to 7280, because the file stopped at the first `REGEXP` it read, `like.test` from 48 to 52 and `expr.test` from 619 to 625. The option not taken is a regular-expression engine of this crate's own, which the C library has none of either. | decided | 2026-09-19 |
