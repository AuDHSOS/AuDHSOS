# audhsos-abi

Definitions shared by the loader, the kernel, and the userland: error
codes, rights, object types, the handle encoding, and the layout constants
of the address space and the IPC buffer. Every value has a stable numeric
representation; changing one is a change of the ABI version.
