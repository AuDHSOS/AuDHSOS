# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 Manuel Baesler and contributors

[package]
name = "driver-virtio-blk"
description = "The virtio block device as logic: the common configuration, the feature set, the request framing of the block device, and the initialization sequence over virtio-queue"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

[features]
test-doubles = []

[dependencies]
virtio-queue.workspace = true

[dev-dependencies]
test-support.workspace = true
virtio-queue = { workspace = true, features = ["test-doubles"] }

[lints]
workspace = true
